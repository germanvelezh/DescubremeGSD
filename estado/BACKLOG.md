# BACKLOG — DescubreMe (priorizado)

**Owner:** German Velez Hurtado.
**Ultima actualizacion:** 2026-07-29 PM-23 (Claude Code — **AUDITORIA COMPLETA DE LAS 25 FILAS P1, la primera que revisa TODAS y no solo las que cierra. De 25, hay 5 P1 reales.** `Resultado:` **14 estaban HECHAS y sin registrar** (cada una con su evidencia `file:line`, o por efecto contra prod/DB) · **6 estan mal clasificadas** y llevan `[REVISAR PRIORIDAD -> P2/P3]` **sin moverse de seccion**, porque la prioridad la decide German · **5 son P1 legitimas**, y de esas **solo UNA es codigo que Claude Code pueda cerrar solo** (`[GAP-SIN-LOGOUT-SESION-PERSISTENTE]`); las otras son **1 de contenido Cowork** (el teaser), 2 de proceso/ops y 1 umbrella de UX. `Por que se hizo:` el archivo no podia responder "que falta" — el intento previo (#68) solo reconcilio las filas que cerraba, asi que el problema sobrevivio. **El fantasma mas caro era `[ENV-PHASE2-NOT-IN-PROD]`**, que hacia parecer que la Fase 2 no estaba desplegada cuando prod tiene **28 reportes de 10 usuarios reales**. `Metodo, uniforme para las 25:` verificar por **efecto** —grep del flag hasta el codigo o el test que lo implementa, con cita; y cuando el flag no aparece en el repo, medir el comportamiento (contra prod/DB, o por ausencia como `reportHref`)— nunca "no encontre el comentario, debe estar abierto". **>>> La auditoria se corrigio a si misma una vez:** `[GAP-TWIVI-ITEMS-ANCHORS-ES-CO]` quedo primero como abierta por **creerle a un comentario de un spec E2E** en vez de consultar la tabla `item`; la DB dice que los 20 stems neutros estan sembrados y las anclas cableadas. **Un comentario en el codigo envejece igual que una fila de backlog.** **Nada se arreglo durante la auditoria, a proposito:** auditar y arreglar en la misma pasada es como #68 se volvio un bucle. Ver PM-23 en `STATUS.md`.)

`[entrada previa]` **2026-07-29 PM (Claude Code — **RECONCILIACION DE ESTADO. El archivo no podia responder "que falta", y se estaba priorizando contra el.** Una lectura mecanica daba **73 filas abiertas**; el cierre vivia en **tres codificaciones incompatibles** (tachado · la palabra "CERRADO" en el cuerpo · prosa en el encabezado de seccion). `Lo que aparecia como vivo y no lo estaba:` **4 P0** —los 3 que el propio encabezado de P0 declara cerrados desde el 2026-06-13, mas `[GAP-RETURNING-USER-RESIGNUP-AGE]` (PR #23 mergeado)— y **dos P1 fantasma contra los que se priorizo trabajo real**: `[GAP-PERMA-BARS-VISUAL-PASS]`, que cerro **por partes en PRs distintos** sin que ninguno reescribiera la fila, y `[GAP-PERMA-CONTENTION-GUIDED-FLOW]`, que decia "codigo-completo en working tree, **SIN commit**" con el feature **en prod desde el 2026-07-23**. `Cerradas con evidencia verificable en la celda, 9 filas:` los 4 P0 + `[GAP-CI-E2E-DB-SUPABASE-ROLES]` + `[GAP-MICROCOPY-VOSEO-TO-ES-CO]` (regrepeado: **cero voseo** en `lib/i18n/microcopy`/`app`/`components`) + `[GAP-PERMA-BARS-VISUAL-PASS]` + las 2 filas duplicadas. **`[GAP-RETURNING-USER-RESIGNUP-AGE]` era el UNICO flag duplicado** de las 100 filas (estaba en P0, P1 y P3); ahora tiene fila autoritativa y dos punteros. `Lo que NO se cerro, y es el hallazgo:` **`[GAP-PERMA-CONTENTION-GUIDED-FLOW]` se RE-ALCANZO en vez de cerrarse** — el feature esta en prod, pero **el test que pide ADR-039 (condicion sistemica #3) no existe**: hay 3 capas de cobertura (gate puro unit · render de `PermaCareScreen` · `ContentionBanner` en E2E) y **ninguna afirma que el flujo guiado surfacee la pantalla en el choke point `allComplete`**, que es justo el "banner aislado" que el ADR excluye. **Es el unico item que mantiene ADR-039 abierto (9/9 criterios + 2/3 condiciones).** `Medicion antes/despues, con el mismo script:` **27 tachadas / 73 abiertas -> 36 / 64**; delta **+9 exacto**, **P0 abiertos = 0**, duplicados entre filas abiertas = 0. Se agrego la **convencion de estado** al inicio para que el marcado no se vuelva a bifurcar. **Sin tocar contenido de las filas vivas: solo estado.**) `[historico]` 2026-07-29 (Claude Code — **ADR-039 paso 4 COMPLETO -> `[GAP-TESTS-INTEGRACION-HUECOS]` cerrado ENTERO, no un paso. Ojo: el ADR queda en 9/9 criterios pero 2/3 condiciones sistemicas** — la #3 remite a `[GAP-PERMA-CONTENTION-GUIDED-FLOW]`, que sigue sin tachar, asi que **ADR-039 NO esta cerrado**. Los 3 criterios de Ley 1581 con test real, cada uno falsado con **inyecciones disjuntas**: **#61 `COMPL-08`** (el control es el filtro de query `.is("revoked_at", null)`, invisible al mock -> cobertura CERO real; se afirma por **delta de la misma entrada**), **#63 `COMPL-07/D1.5`** (**el contrato decia 7 tablas de cascade y son 6**; la 7a es `waitlist`, sin FK, por email; el centinela de la anonimizacion es `entity_id`, que el RPC no toca) y **#65 `COMPL-05/06`** (el E2E afirmaba `href="/api/me/data"` —que el **boton apunta bien**—: un export con las 6 claves **vacias** lo pasaba y violaba el derecho de consulta; ahora se afirma **completitud**). Gate 17 extendido con los 3 + docstring de alcance ampliado. **Aparte se cerro un defecto de producto que no estaba en el plan:** `[GAP-CONSENT-REVOKE-CHIP-SIN-CONFIRMACION]` (#64) — el acuse de revocacion **vivia dentro de la card que `revalidatePath` desmonta**, o sea **funcionaba por carrera**, en una accion **irreversible** de Ley 1581; el fix lo vuelve **estado de la fila** copiando lo que `/me/data` ya hacia. **-> ADR-041**, cuya regla excede el caso: **el acuse de una accion irreversible de Ley 1581 es estado de la fila, nunca estado de cliente efimero** (aplica a borrado de cuenta y export ARCO). `LO QUE NO SE CIERRA:` el **genero 4** de ADR-040 (`[GAP-TESTS-VACUIDAD-CONJUNTO-VACIO]` P2) sigue **sin barrer** — `>=1` es piso, no total. **Cifras verificadas contra el run `30410719805` sobre `main` = `a1ffc07`, no heredadas: E2E 33 · 0 flaky · lint 19 · unit `485 passed | 21 todo` (506).** El **0 flaky NO cierra** `[GAP-E2E-FLAKE-RESPOND-500-CONCURRENCIA]`: una corrida verde es una muestra de tamano 1 del lado favorable.) `[historico]` 2026-07-28 NOCHE (Claude Code — **ADR-040: el pase vacuo es una familia de CUATRO mecanismos y el gate 16 cubre uno.** Los tres cerrados el mismo dia: tautologia (#51/#52/#54), **import inexistente silenciado** (#56 — `audit-immutable` hacia `import("pg")` con `.catch()` y **`pg` no esta instalado**, asi que 3 tests pasaban sin tocar ninguna DB; probado corriendolos contra un host inexistente) y **retorno temprano** (#57 — los gates de RLS se ponian verdes **al desaparecer la migracion que vigilan**). El cuarto, **vacuidad por conjunto vacio**, queda abierto en `[GAP-TESTS-VACUIDAD-CONJUNTO-VACIO]` P2. `La decision que mas vale del ADR es NEGATIVA:` el barrido se corrio **antes** de escribir gate alguno —76 archivos, 468 bloques— y dio **poblacion 3, todas ya cerradas, con 9 falsos positivos de 13**, asi que **no se shippea gate**. Criterio que queda: **una regla sirve como gate solo si su pregunta es decidible estaticamente** — "¿hay asercion sustantiva?" lo es y merecio el gate 16; "¿las aserciones se ejecutan siempre?" y "¿este iterable puede ser vacio?" dependen de runtime y no. `Y la alarma que me tumbe solo:` estuve a un paso de reportar "COMPL-15 sin vigilante en CI" — **falso**, es vacuo en el step de lint y **real en el de unit**, porque `test:unit` es `vitest run` a secas y corre `tests/lint/**` tambien. **Antes de declarar un hueco, preguntar si el mismo archivo corre en otro lado.** **Cifras verificadas contra el run 30403722530, no heredadas: E2E 33 (31 passed + 2 flaky) · lint 19 · unit `475 passed | 28 todo` (503).**) `[historico]` 2026-07-28 PM (Claude Code — **`[GAP-COMPL17-FEEDBACK-IDOR-SIN-GUARD]` CERRADO.** Los **5 bloques de ownership** de `POST /api/feedback` pasaron de `it.todo` a **tests reales contra el stack**. `Las tres decisiones de diseño:` (1) el **service-role va REAL** — la mitigacion es *leer la fila de `assessment_session` y comparar el dueño*, asi que mockear el admin client (como hace `respond-multiscale`) probaria el `if` y no el control, con el lookup scripteado por el propio test; (2) se mockea **solo la identidad** (`next/headers` + `getSupabaseServerClient`), que es lo unico que no existe sin servidor HTTP — la frontera correcta es *simular quien llama*, no lo que el control consulta; (3) **centinela unico en `text`** por test, porque **`feedback_event` no tiene columna de sesion** y en el camino anonimo la fila queda `user_id = null` + `report_snapshot_id = null`, **indistinguible de cualquier otra fila anonima**: un `count(*)` global no podria afirmar ni que se inserto la propia ni que no se inserto ninguna. `Verificado por falsacion, no por pasar:` **tres inyecciones que enrojecen conjuntos DISJUNTOS** — desactivar ownership (`route.ts:104-120`) tumba los **2 de IDOR**; aceptar la sesion inexistente (`:100-102`) tumba **solo** el de no-leak; hacer que ownership **siempre** rechace tumba **solo** los 2 caminos felices. Que ninguna inyeccion las tumbe todas es lo que muestra que los 5 no miden lo mismo. Registro del **gate 17** volteado de `gap` a `covered` con sus 5 guards, que el gate resuelve contra el archivo real. `Cifras medidas con stack + db reset, predichas antes de correr y exactas:` unit **`470 passed | 33 todo` -> `475 passed | 28 todo`** (503 sin cambio) · lint **19** · typecheck limpio. **De ADR-039 queda solo el paso 4** (los 3 criterios Ley 1581). `[historico]` **ADR-039 pasos 1 y 2 EJECUTADOS: PR #51 mergeado (`e79bb03`), PR #52 abierto, ambos con CI verde.** Paso 1: asercion de esquema **QUAL-08** (la mitigacion T-01-08-02 tenia **cero vigilantes** — su unica constancia en el repo era un *comentario dentro de un test hueco*) + **gate 16** (`tests/lint/no-hollow-tests.test.ts`, detector por AST, allowlist temporal con ratchet en las dos direcciones). Paso 2 (**opcion A, decision German**): borrado del hueco de **COMPL-17** de `respond.test.ts` + **gate 17** (`tests/lint/compliance-guard-map.test.ts`), el registro criterio->guard que Cowork puso como condicion — es un *gate* y no un parrafo porque un comentario no se entera cuando el guard que nombra desaparece. **CORRECCION DE CIFRA DE LA AUDITORIA, y es la parte que vale:** los huecos son **31, no 24**. Los +7: **5 en `feedback-ownership.test.ts`** que la auditoria habia clasificado como **REALES**, mas **2 con compuerta invertida** `skipIf(HAS_DB)` (corren solo SIN DB; en CI son los `2 skipped`). **Los dos metodos "independientes" de la auditoria compartian el punto ciego** — su regla de tautologia cubria `hasDb` pero no los literales (`expect(true)`) — asi que **coincidieron por tener el mismo error**, y la coincidencia se leyo como confirmacion. **Dos metodos solo son independientes si sus puntos ciegos lo son.** De ahi sale el **P1 nuevo `[GAP-COMPL17-FEEDBACK-IDOR-SIN-GUARD]`**: COMPL-17 esta anclado en **dos rutas**, y los guards que el ADR cita cubren `/api/respond` pero **no** el IDOR de `/api/feedback`, que esta implementado y **sin ningun test**. Borrarlo "por redundante" habria dejado una mitigacion de seguridad sin vigilantes — el error de QUAL-08 otra vez, y peor, porque alla el control era el esquema y aca es codigo de runtime. **Cifras medidas con stack levantado y `db reset` previo, no heredadas: E2E 33 · lint 15 -> 19 · unit 493 -> `498 passed | 2 skipped | 3 todo`.** Falta el paso 3 (degradar los 30 restantes a `it.todo`; prediccion falsable **`468 passed | 0 skipped | 33 todo`**) y el paso 4 (los 3 criterios Ley 1581).) `[historico]` 2026-07-28 (Claude Code — **reseed ortografico de prod EJECUTADO y verificado por `md5(prod)==md5(seed)` (41 filas, ADR-038)** + `[GAP-GATES-SKIPPED-AUDITORIA]` cerrada con un hallazgo mayor que el flag: **24 bloques de `tests/integration/` reportan `passed` sin afirmar nada**, 18 de ellos convertidos de `skipped` a verde por la entrega 2 -> `[GAP-TESTS-INTEGRACION-HUECOS]` P1. **Nuevo P3** `[GAP-ONET-SEQ26-ESCENOGRAFIAS]` (residuo que #43 nunca toco, separado a proposito para no romper la firma de Cowork). **PR #48 mergeado** (`09a2004`) consolida las copias de `completeInstrument` — **eran 2, no 3**, y este BACKLOG se contradecia a si mismo al respecto. Cifras verificadas hoy: **E2E 33 · lint 15 · unit 493**.) `[historico]` 2026-07-27 (Claude Code — **CI E2E: ENTREGA 2 CERRADA Y VERIFICADA EN `main`. `[GAP-CI-E2E-DB-SUPABASE-ROLES]` CERRADO tras 3 semanas.** `main` = `0010e89`, corrida `30311737492` **`success`** (25 passed / 8 skipped / 0 failed): **el check esta verde por primera vez desde PR #5**. La suite paso de **12 fallas a 0** en 5 PRs (#32, #33 `E2E_LOCAL=1`, #34 diez specs, #36 borrado del funnel muerto, #37 runner a Node 24). **CORRECCION DE CIFRAS HEREDADAS, QUE ES LA MITAD DEL VALOR DE ESTA ENTRADA:** este BACKLOG y `STATUS.md` venian diciendo **"9 fallas / 5 archivos"**, y una version intermedia **"14"**. **Las dos eran falsas y por la misma causa:** se midieron contra un **volumen docker reusado** al que le faltaba un seed (96 ocupaciones contra 125 de un runner limpio), asi que fabricaban 404 que se leian como specs rotos. La cifra real era **12**, y dos de esas "fallas" **pasaban**. Regla que queda: **medir sin `supabase db reset` previo no vale**, y **un 404 en un E2E casi nunca es el spec, es el entorno**. **La causa real tampoco era la que decia el flag:** 8 de las 12 eran specs anteriores al `TestEntryGate` de Ola 2.2 —**una pantalla**, no ADR-029— que se metio delante del item 1 de todos los tests. **P3 nuevos:** `[GAP-E2E-FLAKE-RESPOND-500-CONCURRENCIA]`, `[GAP-E2E-SKIPS-E2E-LIVE]`. **Aparte:** `engines.node` decia `">=20.0.0"`, imposible desde que la suite exige el `WebSocket` global de Node 22 — corregido a `>=24` en PR propio. **5 ramas remotas** de PRs mergeados borradas, verificando contenido en `main` y no topologia (con squash ninguna es ancestro).) `[historico]` 2026-07-27 (Claude Code — **ADR-036 IMPLEMENTADO: `[GAP-TWIVI-BAND-DEFINICION-DOBLE]` P1 cerrado en codigo por PR #29** `fix/twivi-banda-unica-adr-036`, pendiente de merge. Las dos mitades: el scoring bandea por z sobre los 4 HOV centrados (MEDIO revive; las 4 narrativas muertas del seed vuelven a ser alcanzables) y el circumplejo deja de recomputar — lee `bands_by_dim`, como las barras siempre hicieron. Sin migracion: cada reporte queda coherente consigo mismo. **Nota de implementacion que el ADR no anticipaba:** BAJO se vuelve mas exigente (con 4 dims un pico infla la SD y comprime al resto hacia MEDIO). Gates CI verdes salvo E2E, que falla con **el mismo set de 9 specs que `main`, verificado spec por spec** — delta cero. **P3 nuevo:** `[GAP-ASSEMBLER-COMENTARIO-RADIO-STALE]`. **P2 nuevo:** `[GAP-A11Y-LECTOR-PANTALLA-REAL]` (ADR-036 le sube la prioridad: la contradiccion de bandas vivia justo en la tabla `sr-only`).) `[historico]` 2026-07-27 (Claude Code — **P0 NUEVO: `[GAP-RETURNING-USER-RESIGNUP-AGE]` elevado por German (lockout total del usuario que vuelve) — reproducido en prod y CERRADO EN CODIGO por **PR #23** `fix/returning-user-login-lockout`, pendiente de merge; ADR-035.** Salio de intentar el deploy-smoke del #20: ninguna de las dos rutas de acceso de una cuenta existente funciona (reenvio de enlace y re-signup, ambas → `/?error=age`), porque GoTrue descarta `options.data` para un email existente y el callback exige `dob_pending` siempre. El fix distingue LOGIN de SIGNUP por fila de `consent` activa. Gates: tsc 0 · lint 13/13 · **unit 450** · build OK. Corregidas 2 filas viejas del mismo flag (P1 "se cierra en Phase 2" era falso; P3 tenia el diagnostico y el fix correctos pero severidad equivocada). **P3 nuevo:** `[GAP-MAGIC-LINK-RESEND-COOLDOWN-THROTTLE]`. **Deploy-smoke del #20 = NO CORRIDO** (este PR lo desbloquea).) `[historico]` 2026-07-23 (Claude Code — **SMOKE A1 corrido en prod + fix ValueCircle #17 mergeado (`cf18343`) + reconciliación tracker/STATUS.** A1 verde en núcleo (check crítico BFI NEG PASA "intensidad" no "pulso estable"; cubre PR-A/B/C + overhaul #13/#16 en una corrida). ValueCircle P1 arreglado: 4 HOV centrados por MRAT, capar a `hovAxisOrder`, labels es-CO firmados por Cowork → cierra `[GAP-HOV-LABELS-ES-CO]` (Explorar/Destacar/Conservar/Aportar) + `[GAP-PERMA-DIM-LABELS-ES-CO]` (9 dims, N/Lon sin doble-flip); gates verdes, test:unit 422. **4 hallazgos nuevos:** 2×P1 (`[GAP-PERMA-CONTENTION-GUIDED-FLOW]` seguridad, `[GAP-PERMA-BARS-VISUAL-PASS]` epic) + 2×P2 (`[GAP-REPORT-INTERESES-MISLABEL]`, `[GAP-TEASER-BAND-NOT-SHAPE]`). C1 alcance ampliado (+teaser voseo +regex waitlist); D1 con fuente (mapa muestra minutos); A3 en espera (defaults ok, bordes no probados). ADR-032. Docs de reconciliación commit `82371fd` local. Ver `estado/SMOKE_A1_RESULTADOS_v1.0.md` + sección RECONCILIACIÓN del tracker.) `[historico]` 2026-07-07 PM (Claude Code — **OLA 1 del HANDOFF_UI EJECUTADA + MERGEADA A PROD + DESPLEGADA (PR #9 squash `12a6eb6`; Vercel Production READY `dpl_AdTjFKX3`, rollback-candidate).** "Primera impresión": dirección A (paper) scoped en las 5 pantallas de entrada (landing/`/intencion`/signup/`/consent`/`/onboarding/mapa`) vía wrapper `.dm-paper` — **36 pantallas nocturnas intactas** (decisión German AskUserQuestion: night-default + paper-scope). **ADR-021 respetado** (nombrado en el commit; `max-w-*` sigue en `var(--container-*)`, cero tokens en `--spacing-*`/`--container-*`). Capa `--dm-*` (paper+night) + `--dm-sage-deep` (AA §7) + `--dm-ease`; **Fraunces** vía next/font (Instrument Serif conservado, clímax Ola 3); `components/PaperShell.tsx`. 1.2 landing hero asimétrico + copy §2 + CTA→`/intencion`; 1.3 `/intencion` (intent como `?intent=`, sin persistencia server); 1.4 registro reskin (quita teaser anónimo ADR-029; intent→`user_metadata`; DOB/labels pineados intactos); 1.5 consent contenedor + `/consent` legal reskin (**texto legal byte-idéntico**); 1.6 `/onboarding/mapa` + recall (callback: fresco→mapa, returning→next test). **Gaps entregados:** `[GAP-CONSENT-UX-ONE-SCREEN]` (contenedor visual, sin tocar texto legal); avance de `[GAP-UX-FLOW-REDESIGN]` P1 (olas 0-1 hechas, faltan 2-3); flag (a) de Ola 0 cerrado (línea "No necesitas crear cuenta" retirada del landing). **Verif:** tsc 0 · lint 13/13 · build verde (19 páginas) · unit 355 pass (5 pre-existentes) · capturas 375/1440 en las 5 pantallas sin DB; **mapa (auth-gated) = deploy-smoke pendiente**. CI "Full suite" rojo SOLO por `[GAP-CI-E2E-DB-SUPABASE-ROLES]` (typecheck+lint pasaron; migraciones psql fallan por roles → resto skipped) — no bloqueó el merge. **DEUDA E2E:** `signup-consent.spec.ts` actualizado en lockstep; `pause-resume`/`full-flow-onet-anonymous`/`free-full-flow` (funnel pre-ADR-029) pendientes, documentado en `estado/PLAN_Ola1_Primera_Impresion_v1.0.md`.) `[historico]` 2026-07-07 (Claude Code — **OLA 0 del HANDOFF_UI EJECUTADA + MERGEADA A PROD (PR #8 squash `9530416`; Vercel Production READY `dpl_D2PDX5yZ`).** 0.1 voseo→es-CO tuteo en TODO el código renderizado: 18 microcopy `lib/i18n/microcopy/es-CO/*` (presente + imperativos) + hero landing hardcoded + seed RIASEC (132 narrativas, diptongación correcta) + seed BFI-2-S + metadata TwIVI (Scope B: el grep de aceptación incluye `necesitás`, presente en BFI); aserciones de tests migradas a tuteo. Grep repo-wide de voseo = **cero en código de producto**. 0.2 TwIVI verificado (MRAT 12/12, `items.sql` cero PLACEHOLDER) — no re-sembrado. 0.3 `MC_NIVEL_CLOSE_CTA`="Ver tu primer mapa →". Verif: tsc 0, lint 13/13, unit 355 pass (5 pre-existentes `callback-idempotent`). **RESEED DE PROD PENDIENTE:** las filas vivas de `narrative_template` (RIASEC/BFI) siguen en voseo hasta correr el reseed DELETE-first idempotente (`version 1.0`/`es-CO`, con OK German); el microcopy `.ts` sí rinde con el deploy. **Flags:** (a) `landing.ts:19` "No necesitas crear cuenta" FALSO bajo ADR-029 (solo voseo corregido; retirar = Ola 1.2); (b) el `→` del CTA rinde como texto en `reporte/page.tsx:327` (Ola 3 = patrón `aria-hidden`); (c) comentario DEPLOY-BLOCKER de `instrument-version.sql` obsoleto (PR #7). **Git:** los 3 commits docs de German (UX + pack TwIVI + ADR-030/031) siguen LOCALES sin pushear.) `[historico]` 2026-07-01 PM-4 (Cowork — **SIGN-OFF COMPLETO DEL PAQUETE DE MICROCOPY (German, §8 íntegro) + ALERTA DE GIT.** Firmado y listo para seed sin re-consulta: `MC_NIVEL_CLOSE_CTA`="Ver tu primer mapa →", voseo→es-CO (§2), hooks+intros+transición (§4), 14 plantillas de cruce + 12 arquetipos (§5), pantalla correo (§6), estados (§7), 57 frases reveladoras (§9). **Efecto en gaps:** el copy de `[GAP-MICROCOPY-VOSEO-TO-ES-CO]`, `[GAP-W6-HOOKS-1]`, `[GAP-FREE-TEST-INTRO-COPY]`, `[GAP-TEASER-CROSS-TEMPLATES-ES-CO]`, `[GAP-MICROCOPY-NIVEL-CTA]`, `[GAP-REVEAL-PHRASES-BY-BAND]` pasa a **APROBADO — pendiente solo integración CC** (HANDOFF_UI olas 0-3). **ALERTA:** Cowork iba a commitear el paquete docs y se DETUVO — working tree con 3 frentes entrelazados (UX Cowork + TwIVI CC sin commitear + ADR-030 German) y repo parado en `fix/free-transition-remove-report-link` (no main). **Triage de git = primera tarea de la próxima sesión CC** (detalle en STATUS bloque Cowork, UPDATE Cowork-3). Sin push.) `[historico]` 2026-07-01 PM-3 (Claude Code — **INTEGRACIÓN PACK TwIVI arrancada.** Cowork entregó pack (`implementation_packs/TwIVI_Implementation_Acquisition_Pack_v1.0.md`) + seed M/F (`estado/TwIVI_items_es-CO_SEED_v1.0.sql`) + correcciones PRD §8. **Decisión (ADR-030 addendum): variante NEUTRA ahora + branching él/ella DIFERIDO** (`[GAP-TWIVI-GENDER-SCHEMA]` nuevo P2) — verificado contra código+prod que la app NO captura género en ningún lado (user/signup/`assessment_session`; prod `item` sin `gender_variant`), así que las 3 rutas de branching dependen todas de construir captura de género + Ley 1581. Neutra = schema-limpia. **Hecho este turno (working tree, SIN push — main auto-deploya):** anclas es-CO reales en `lib/questionnaire/response-scales.ts` (const renombrado, unit test no rompe); PRD L124/L131 PVQ-21→TwIVI (L409 changelog intacto). **Verificado en prod (`tzhhqaducmbxfebuyvnv` vía MCP): `[GAP-MRAT-METADATA-READ]` + `[GAP-NARRATIVE-DIMBAND-SCHEMA]` YA hechos+vivos** (score-session lee value_map/hov_map; mig 015 + 12 textos HOV×banda) — el pack los listaba como tareas CC abiertas, era stale. **Stems neutros = Cowork:** brief `estado/BRIEF_Cowork_TwIVI_NEUTRAL_STEMS_v1.0.md` + scaffold SQL turnkey `estado/TwIVI_items_NEUTRAL_es-CO_SEED_SCAFFOLD_v1.0.sql`. **Falta:** Cowork llena 20 stems → CC aplica seed a prod (DELETE-first porque 20 placeholders en seq 1..20 bloquean el NOT-EXISTS; chequear `item_response` FK; gated con OK German) → re-correr `twivi-mrat-fixture` + smoke ValueCircle con datos reales.) `[historico]` 2026-07-01 PM-2 (Cowork — **`[GAP-REVEAL-PHRASES-BY-BAND]` ENTREGADO.** El flag propuesto en la nota anterior queda cubierto a nivel de contenido: `MICROCOPY_ES-CO_SIGNOFF` sube a **v1.1** con la nueva **§9** — librería de frases reveladoras por dimensión×banda para los mini-resultados del Free, **57 claves `MC_REVEAL_*`/`MC_MINIRESULT_*`**: BFI composición por fragmentos alta/baja (banda media excluida por diseño anti-Barnum; fallback de equilibrio + coda single), O*NET 15 pares top-2 + 6 picos únicos (empates via high-point code del pack), TwIVI 4 HOV + 4 pares adyacentes, PERMA 5 drivers + variantes `LOW_OVERALL` (con salvaguarda NFR-27/28: footer de contención obligatorio cuando overall=baja) / `BALANCED` / `INCOMPLETE`, + líneas fijas "Qué medimos"/"Por qué te importa" de los 4 tests (§9.5). Contrato de entrada explícito: bandas/saliencia/umbrales los define el pack/scoring, la librería solo consume (regla de oro intacta). Redacción verificada: cero adjetivos con marca de género aplicados al usuario, cero léxico clínico, banda baja como estilo (nunca déficit). **Nota descubierta:** el seed RIASEC de 132 narrativas (2026-06-08) está en voseo verbal → incluido en alcance de ola 0.1 del HANDOFF_UI. **Pendiente:** sign-off German (checklist §8 del pack, ahora incluye §9) + seed CC en ola 2.3. **Con esto NO queda copy autoral pendiente del Free.**) `[historico]` 2026-07-01 PM (Cowork — **REVISIÓN UX/UI COMPLETA + ADR-031 + paquete de handoff.** Se revisó el prototipo navegable de Claude Design (`DescubreMe Prototipo.dc.html`, proyecto `15f3319f`, 2026-06-30) de punta a punta: Free completo + Teaser/Paid/Integrador/Ikigai/B2B — materializa fielmente el BLUEPRINT v1.1. **Contradicción de flujo detectada y RESUELTA (ADR-031, decisión German):** cierre canónico Free = PERMA → nivel obligatorio → **teaser-constelación CON ocupaciones Job Zone dentro** (un solo clímax); reconcilia el recut 2.1 vivo en prod (nivel→ocupaciones) con el prototipo (constelación sin nivel/ocupaciones). La maquinaria 2.1 se conserva; cambia la composición de la superficie `?cierre=free`. **Entregables nuevos (en `auditoria-ux-ui/`):** `HANDOFF_UI_v1.0.md` (spec de implementación por olas 0-3 para CC: tokens `--dm-*` respetando ADR-021, Fraunces/Hanken vía next/font, motion spec, componentes con rutas y criterios, responsive §6, mapeo a gaps §8) + `MICROCOPY_ES-CO_SIGNOFF_v1.0.md` (paquete firmable: voseo→es-CO completo, hooks 4/4, intros por test, transiciones, **14 plantillas de cruce del teaser** `[GAP-TEASER-CROSS-TEMPLATES-ES-CO]` + 12 arquetipos, pantalla correo, estados; propuesta `MC_NIVEL_CLOSE_CTA`="Ver tu primer mapa →" **pendiente sign-off §8**) + BLUEPRINT actualizado a **v1.2** (cierre ADR-031 §7.7, pantalla "Revisa tu correo" §7.3.1, responsive móvil §3.7, pendientes del prototipo §16). **Decisiones German de la sesión:** prioridad = handoff a implementación (no iterar más el prototipo); mobile = spec en handoff (no prototipo aparte). **Estado de gaps:** `[GAP-TEASER-CROSS-TEMPLATES-ES-CO]` ENTREGADO (pendiente sign-off + seed); `[GAP-W6-HOOKS-1]` copy ENTREGADO; `[GAP-FREE-TEST-INTRO-COPY]` ENTREGADO; `[GAP-MICROCOPY-VOSEO-TO-ES-CO]` copy consolidado listo para aplicar (ola 0.1); `[GAP-FREE-NO-RESULTS-VISIBILITY]` resuelto por diseño (ADR-031); **flag nuevo propuesto** `[GAP-REVEAL-PHRASES-BY-BAND]` P1 Cowork (frases reveladoras por dimensión×banda para mini-resultados — único copy autoral del Free que queda). Gaps nuevos de experiencia detectados en la revisión (P2, en HANDOFF §3): pantalla "revisa tu correo" sin diseñar en prod (ola 2.5), fix UX del 404 `[GAP-CALLBACK-INCOMPLETE-SESSION-REPORTE-404]` → reanudar test pendiente (ola 2.6), auditoría AA del clímax nocturno, B2B admin selección de lentes ausente del prototipo. **Próximo:** (1) German firma §3+§5 del MICROCOPY; (2) CC arranca ola 0 (`HANDOFF_UI §3`) — puede correr en paralelo con la integración TwIVI ya en cola.**) `[historico]` 2026-07-01 (Claude Code — **Phase 2.1 planes 03/04 MERGED a prod (PR #5 squash `9e16a8a`, 2026-06-30). Smoke prod (German): mini-results por test + "Mis datos" `/reporte` full = VERDE; **el recut del cierre (`?cierre=free`, core de plan 03) VERIFICADO E2E** (corrida limpia German 2026-07-01: post-PERMA → nivel obligatorio → ocupaciones Job Zone + CTA, sin hexágono/bandas) → **planes 03+04 CERRADOS (código + comportamiento).** **BUG hallado + FIX (Decisión B, owner):** ese link varaba al usuario mid-funnel (página terminal, sin retorno) + filtraba profundidad Paid que el recut oculta → quitado el link + plomería `reportHref`; **PR #6 `fix/free-transition-remove-report-link`** (test nuevo, tsc limpio, 355 pass) MERGED a prod (squash, 2026-07-01, `descubreme-43lvhz65d` Ready); branch remota borrada. **Instrumento de valores: TwIVI CONFIRMADO** — resuelve contradicción PRD §8 L246 ("PVQ-40" plan-B abierto) vs `dossiers/24_PVQ-40_Consolidado.md:21` (rechaza PVQ-40, elige TwIVI) + `PVQ-21 pack:30` ("Plan-B TwIVI"); licencia TwIVI = uso libre explícito (sin fase 7). Acción doc: corregir PRD L246. Brief de pack TwIVI es-CO para Cowork (P0) en `estado/BRIEF_Cowork_TwIVI_es-CO_v1.0.md`. **Infra:** preview NO smokeable (env Vercel sólo Production + branch `verify/phase-1-deploy-strict`; sin staging cableado) → 2.1 se smokea en prod con Instant Rollback.**) `[historico]` 2026-06-29 (Claude Code — **SMOKE MANUAL Phase 2.1 CORRIDO (German). Gate compliance = VERDE; el deploy se queda (sin rollback).** German certifico con email nuevo los 3 gates rollback-criticos: aterriza en BFI 1º, modal NFR-27 bloquea el item 1, link NFR-28 muestra las 6 lineas CO con telefono. **404 diagnosticado y NO bloqueante** = `[GAP-CALLBACK-INCOMPLETE-SESSION-REPORTE-404]` P2: email reusado con sesion anonima incompleta → callback la manda a `/reporte` sin snapshot → notFound (auth SI funciona — logs auth verifyOtp 200 + DB sesion `0bea76ec` open/20/0-snap; email totalmente nuevo OK). **UX/contenido = lejos de aceptable**, capturado como flags: P1 `[GAP-W5W6-ORPHANED-FREE-FLOW]` (W5/W6 en /reporte, Free termina en /perfil-integrado → inalcanzables, VERIFICADO), P1 `[GAP-MICROCOPY-VOSEO-TO-ES-CO]` (voseo rioplatense → revertir a es-CO tuteo neutro, viola CLAUDE.md §13, VERIFICADO), P1 `[GAP-FREE-NO-RESULTS-VISIBILITY]` (no ve resultados, solo teaser), `[GAP-W6-HOOKS-1]` escalado a P1 (transicion = boton pelado), P1 `[GAP-UX-FLOW-REDESIGN]` (umbrella), P2 `[GAP-FREE-TEST-INTRO-COPY]`; `[GAP-TWIVI-ITEMS-ANCHORS-ES-CO]` confirmado VIVO EN PROD (placeholders renderizados). Decision German: cerrar+pushear el deploy 2.1 + capturar todo en BACKLOG primero (sin codear remediacion). Abierto: O*NET sigue 60 items — confirmar si debia cambiar.) `[historico]` 2026-06-26 (cont.3 — Claude Code — **FUNNEL INVERTIDO DEPLOYADO A PROD; SMOKE MANUAL PENDIENTE (German 2026-06-27).** Migraciones 016 (user/report cols) + 017 (reorder `product_stack` BFI=1) + seed_ext_v1.1 (29 occ Zona 3-5 → 125) aplicadas a prod Supabase `tzhhqaducmbxfebuyvnv` vía MCP (out-of-band, patrón 012/013, todas verificadas por query). German mergeó feat→main vía PR #4 (squash, `origin/main 81ca391`); Vercel Production READY (`dpl_6iA2...`); `descubreme.co` sirve signup-first (CTA→/signup, before-you-start fuera). Baseline confirmó BFI ethical_flags + contention CO 6/6-con-phone + audit_log.action texto-libre YA en prod. **PENDIENTE:** smoke manual del wiring callback→BFI→NFR-27 (cero cobertura automatizada → 1ra ejecución real es el deploy). Rollback = Vercel Instant Rollback a `4e93e75`. Commit de docs LOCAL en main (sin pushear hasta el smoke). Ver STATUS.md bloque top.) `[historico]` 2026-06-26 (cont.2 — Claude Code — **FUNNEL INVERTIDO: signup+consent PRIMERO + BFI gancho (ADR-029); supersede el mecanismo anonimo de W7; cierra `[GAP-ONET-ANON-SENSIBLE-PRECONSENT]` por diseño.** German decidio: cero tramo anonimo; landing personality-led (sin anticipo, taste no-datos = fast-follow); BFI 1er test autenticado, O*NET baja a 2º (sigue alimentando la rec Job Zone). Cambio tractable (exploracion): bloqueador unico = callback redirige a `/` sin sessionId (`callback/route.ts:284`) → cargar `product_stack.order` → `/test/{primerCode}`; signup ya no depende de sessionId; consent dual ya va antes de todo test; NFR-27/28 antes del 1er item BFI = HARD compliance. v0.1/v0.2 marcados superseded (no borrados). Postura de menores MEJORA (18+ antes de cualquier test). Desbloqueado para construir (sin gate legal anonimo; consent-text final = Fase 7). Proximo: `/gsd-plan-phase` wave. SOLO diseño + estado.) `[historico]` 2026-06-26 (cont. — Claude Code — **W7 DISEÑO v0.2 + decision de arquitectura (ADR-028).** German aporto lectura juridica preliminar → W7 viable bajo condiciones; eligio **Arquitectura 1 (server + consentimiento previo) + hardening** (TTL horas, sin IP, borrado <18-at-signup) como primaria, **Arquitectura 2 (local-first)** como escalada si el concepto legal formal la exige. Entregable `estado/DESIGN_W7_BFI_Anonimo_Ley1581_v0.2.md` (reemplaza v0.1, no borrado). **Gate legal formal INTACTO** (no implementar sin OK del paquete: concepto abogado es-CO + PIA + prueba tecnica de anonimizacion + protocolo menores + matriz de terceros). Nuevo **`[GAP-ONET-ANON-SENSIBLE-PRECONSENT]` P1** — el gancho O*NET vivo en prod ya recolecta sensibles Art. 5 anonimos pre-consentimiento (politica §3 vs guard divergen; puede ser mas urgente que W7). Decisiones abiertas (v0.2 §11.2): N del TTL, IP si/no, mitigacion interim O*NET. SOLO diseño; SIN commit. ADR-028.) `[historico]` 2026-06-26 (Claude Code — **PHASE 2.1 W5 (captura de nivel + filtro Job Zone) + W6-§5 (reveal ocupacional no-determinista) COMMITEADOS (`bbe073d`, sobre W1-W4 `20447d7`).** Items nuevos: **`[GAP-PHASE21-CALLBACK-TEST-STALE]` P2** — `tests/unit/auth/callback-idempotent.test.ts` (5 fallas en el suite) quedo stale vs la migracion 02-21 token_hash (el test manda `?code=` viejo → el callback da `error=invalid`); el BUG `[BUG-CALLBACK-NOT-IDEMPOTENT]` ya esta CERRADO, falta actualizar el test al flujo `verifyOtp`/`token_hash`. CC. **`[GAP-CONSENT-LEVEL-1.1.0]` P2** — enmendar el texto de consent §3 para divulgar education_level/career_stage en el inventario; requiere subir a 1.1.0 PERO el guard `lib/consent/guard.ts:89` re-promptea en bumps MINOR (`semverLt`) + el callback hardcodea `CONSENT_VERSION="1.0.0"` + sin ruta de re-consentimiento → un bump hoy daria 412 a usuarios vivos + rompe signups; el bump necesita guard major-only + callback lee el registry + ruta de re-consent. CC. **W7 (BFI-anonimo-primero) P1/P2** — German eligio insistir; diseño en `estado/DESIGN_W7_BFI_Anonimo_Ley1581_v0.1.md` (PROPUESTA, pendiente sign-off + **revision legal como posible STOP**: sensibles de posibles menores puede ser no-waivable aceptando riesgo → rutaria a la Opcion A). NO implementar hasta OK legal; es su propio Wave/plan (funnel desplegado + schema 017 `anonymous_consent` + cron TTL + threat model). German + CC. **`[GAP-W6-HOOKS-1]` P2** — los 4 hooks §1 + el cierre se cablean junto con la decision de orden de W7 (Cowork revisa "Empecemos"/"Ahora"; tocan `done/page.tsx`). CC + Cowork. **`[GAP-MICROCOPY-NIVEL-CTA]` P2** (label del boton de captura) + **`[GAP-MICROTAG-RIASEC-NAME]` P3** (pack §5 "Investigador" vs app "Investigativo") = sign-off Cowork. **`MC_REPORT_OCCUPATIONS_HEADING` P3** huerfano (ya no usado por /reporte tras W6-§5; microcopy Cowork, NO removido). Verificacion W5+W6: typecheck 0, test:lint 13/13, unit 342/5 (las 5 = el test stale de arriba). Funcional pendiente de deploy.**) `[historico]` 2026-06-13 (Claude Code — **PHASE 2 GAP-CLOSURE COMPLETA via `/gsd-execute-phase 2 --gaps-only` — machinery + E2E green LOCAL, NO bare "Complete".** `[GAP-AUTH-4TEST-RUNTIME]` P0 CERRADO en 4 piezas: 02-14 (session lifecycle + routing guiado /done), 02-15 (`/api/respond` multi-escala + cookie SSR + consent gate), 02-17 (scoring trigger on /done arrival), 02-18 (fix casing + transition modal). El E2E real surfaceo + cerro 3 bugs en vuelo: `[GAP-INSTRUMENT-CODE-CASING]` P0 (case-insensitive match, ADR-024), `[GAP-AUTH-TRANSITION-MODAL-UNWIRED]` P1 (TransitionScreen/NFR-27 wired), `[GAP-NFR28-DISTRESS-BANNER-UNWIRED]` P1 (02-19, derivable-minimum, ADR-025); `[GAP-ANONYMOUS-TEST-MOCK-ILIKE]` cerrado. **Free E2E 11 passed / 0 failed / 0 skipped (honest-green) + unit 296/0 + typecheck clean + 5 critical gates verdes (consent, NFR-27 modal, NFR-28 banner, teaser-lock, quality-flag).** ESTADO HONESTO: machinery-complete + E2E-green LOCAL (sobre `supabase db reset` efimero, NUNCA prod), NO "delivered" total. **ABIERTO:** contenido Cowork — FREE-04 (TwIVI items `[GAP-TWIVI-ITEMS-ANCHORS-ES-CO]`), FREE-12/FREE-13 (teaser cross-templates `[GAP-TEASER-CROSS-TEMPLATES-ES-CO]`); deploy a PROD (`[ENV-PHASE2-NOT-IN-PROD]`, Phase 7, migrations 014/015 + seeds nunca aplicados en prod); diferidos P2 `[GAP-NFR28-ITEM-LEVEL-TRIGGERS]` (claves N1/N3 item-level) + `[GAP-NFR28-BFI-FACET-DISTRESS]` (dormant-by-design, facetas son Paid). **NO auto-avanzar a Phase 3** con FREE-12 contenido + deploy abiertos. Proxima: `/gsd-verify-work 2` (UAT) o Cowork content o deploy. ADR-024/025.** `[historico]` 2026-06-12 (Claude Code — **PHASE 2 EJECUTADA (13/13 planes, 48 commits en `main`) pero NO completa: bloqueador P0 `[GAP-AUTH-4TEST-RUNTIME]` (runtime autenticado del journey de 4 tests, nunca planeado; ver P0). Maquinaria + schema construidos + aplicados (`supabase db reset` migrations 001-015 + 24 seeds SIN errores) + unit 273/0 + E2E anonimo verde. 7 gaps schema/wiring resueltos en vuelo. Cerrar `[GAP-AUTH-4TEST-RUNTIME]` (`/gsd-plan-phase 2 --gaps`) antes de verify/ship; NO auto-avanzar a Phase 3. Gaps de contenido Cowork (deploy, no plan): `[GAP-TWIVI-ITEMS-ANCHORS-ES-CO]`, `[GAP-TEASER-CROSS-TEMPLATES-ES-CO]`, `[GAP-DIMENSION-LABELS-ES-CO]`.** `[historico plan]` **PHASE 2 PLANEADA via `/gsd-plan-phase 2`: 13 planes / 6 waves, plan-checker PASS, ADR-023 (TwIVI + ethics reconciliation), `[GAP-PVQ-RR-FREE-FORM]` cerrado.** `[historico]` **PHASE 1 VALIDADA EN PRODUCCION. Browser smoke prod PASS (German): flujo completo + magic link OK en `descubreme.co`.** `[GAP-PROD-DOMAIN-WWW-CANONICAL]` CERRADO — el redirect apex->www no rompe el PKCE (308 preserva `?code=`; cookie code_verifier host-only en www donde aterriza el callback). No se cambio nada en prod; www-alignment descartado por innecesario. Quedan diferidos no-bloqueantes: `[GAP-MAGIC-LINK-EXPIRED-PREFETCH]`, `[GAP-RETURNING-USER-RESIGNUP-AGE]`, `[GAP-REPORT-OCCUPATIONS-DEPTH]` (Fase 2-3), `[GAP-MAIN-BRANCH-PROTECTION-BYPASS]` (proceso). Proximo: decision GSD — `/gsd-ship 1` (cierre formal) o arrancar Phase 2 (B2C Free). `[historico]` 2026-06-10 — **`/gsd-verify-work 1` COMPLETO 20/20.** Test 16 (delete) cerrado: `[GAP-DELETE-AUDIT-DIGEST-SEARCHPATH]` CERRADO (mig 013 a PROD, verificacion DB COMPL-07/09). Merge `verify/phase-1-deploy-strict` -> `main` = fast-forward LOCAL hecho; **push a origin/main HECHO + deploy Production READY** en `descubreme.co` (19 env vars provisionadas en Production). Pendiente manana: browser smoke del magic link en prod (ojo redirect apex->www, `[GAP-PROD-DOMAIN-WWW-CANONICAL]`). `[historico]` **`/gsd-verify-work 1` reanudada. `[GAP-UI-QUALITY-PREVIEW]` CERRADO via Fix 5 (ADR-021)**: causa raiz = colision de namespace Tailwind v4 `--spacing-{tshirt}` shadowea `--container-*` → colapso global de layout. Solucion Opcion B (raiz): escala 8pt named→numerica + Inter via next/font. 28 archivos, verificado en CSS compilado + render local. Commit en branch `verify/phase-1-deploy-strict`; merge a main tras tests 2-20. Proximo: continuar tests 2-20 del UAT contra preview reconstruido.
**Prioridad:** P0 (bloquea ahora) · P1 (prerequisito de una fase cercana) · P2 (necesario, no inminente) · P3 (deseable / futuro).
**Owner del item:** Cowork (research/producto) · CC (Claude Code, implementacion) · Adaptacion (proceso ITC con humanos) · German (decision/negociacion).

> Consolida las "carries" dispersas en los documentos. Si descubres un gap nuevo, anadelo aqui con su flag `[GAP-...]`. Fuente de verdad de pendientes del proyecto.

> **CONVENCION DE ESTADO — UNA SOLA, y es el tachado de la primera celda.** Un flag cerrado se escribe `~~`[GAP-X]`~~`. **Nada mas cuenta como cierre:** ni la palabra "CERRADO" dentro del cuerpo, ni una nota en el encabezado de la seccion, ni que el PR este mergeado.
>
> `Por que existe esta regla:` el 2026-07-29 una lectura mecanica del archivo devolvia **73 filas "abiertas"** porque el cierre vivia en **tres codificaciones incompatibles** a la vez. Entre esas 73 habia **4 P0 ya cerrados** y **dos P1 fantasma** —`[GAP-PERMA-BARS-VISUAL-PASS]` (cerrado por partes en PRs distintos, ninguno reescribio la fila) y `[GAP-PERMA-CONTENTION-GUIDED-FLOW]` (decia "SIN commit" con el feature en prod hace 6 dias)— **y se priorizo trabajo contra ellos.** El costo de no marcar no es cosmetico: es decidir sobre un inventario falso.
>
> `Regla de cierre:` un flag se tacha **con evidencia verificable en la celda** (PR mergeado, grep con su resultado, corrida de CI con su id). Si solo una PARTE cerro, **no se tacha: se re-alcanza** la fila diciendo que queda (ver `[GAP-PERMA-CONTENTION-GUIDED-FLOW]`, cuyo feature esta en prod y cuyo test falta). **Un flag vive en UNA sola fila**; si reaparece en otra seccion, esa otra es puntero a la autoritativa.

---

## P0 — Bloquea ahora

> **2026-06-13: NO hay P0 abierto.** Los 3 items de abajo (`[GAP-INSTRUMENT-CODE-CASING]`, `[GAP-AUTH-4TEST-RUNTIME]`, `[GAP-AUTH-TRANSITION-MODAL-UNWIRED]`) estan **CERRADOS** por la gap-closure 02-14..19 (Free E2E 11/0/0 honest-green + unit 296/0; ADR-024 casing, ADR-025 NFR-28). Tambien cerrado el P1 `[GAP-NFR28-DISTRESS-BANNER-UNWIRED]` (02-19, derivable-minimum) y `[GAP-ANONYMOUS-TEST-MOCK-ILIKE]`. Nuevos diferidos **P2** (no bloqueantes, rastreados en `.planning/.../deferred-items.md` + ADR-025): `[GAP-NFR28-ITEM-LEVEL-TRIGGERS]` (claves N1/N3 item-level del distress) + `[GAP-NFR28-BFI-FACET-DISTRESS]` (dormant-by-design, facetas son Paid). Las filas abajo quedan como historico del diagnostico. Lo que queda para shippable: contenido Cowork (FREE-04/12/13) + deploy a PROD (`[ENV-PHASE2-NOT-IN-PROD]`, Phase 7).

> **2026-07-27: P0 ABIERTO (cerrado en codigo, pendiente de merge)** — `[GAP-RETURNING-USER-RESIGNUP-AGE]`, elevado por German con evidencia de prod. Fila abajo.

| Flag | Pendiente | Producto/Fase | Owner | Nota |
|---|---|---|---|---|
| ~~`[GAP-RETURNING-USER-RESIGNUP-AGE]`~~ | **CERRADO Y EN PROD. `PR #23 MERGEADO`** (verificado 2026-07-29: `gh pr view 23` = `MERGED`; ADR-035). **Esta es la fila AUTORITATIVA de este flag** — aparecia en P0, P1 y P3 a la vez, y era el unico flag duplicado del archivo; las otras dos quedan como punteros historicos a esta. `[historico]` **ELEVADO a P0 el 2026-07-27 (German). REPRODUCIDO EN PROD + CERRADO EN CODIGO por PR #23** (`fix/returning-user-login-lockout`), pendiente de merge. **No era un edge de "re-signup con el mismo email": NO EXISTE ninguna ruta de login para un usuario ya registrado.** Las dos rutas posibles terminan en `/?error=age` — el boton "Reenviar enlace" (omite `options.data` por diseno) y el re-envio del formulario de signup (GoTrue DESCARTA `options.data` cuando el email ya existe). Verificado hoy con `permacare1` + `permacontrol2`: tras re-enviar el formulario completo con DOB + ambos consentimientos, `raw_user_meta_data` en prod queda SIN `dob_pending`, y el gate `callback/route.ts:158` lo exige siempre. `last_sign_in_at` SI se actualiza (el `verifyOtp` autentica) pero el rebote descarta la sesion y quema el token de un solo uso. Vivo en prod desde la Fase 1. **Fix:** distinguir LOGIN de SIGNUP por fila de `consent` activa (ADR-035). | Fundacion / auth | CC (codigo) + German (merge) | Bloqueo el deploy-smoke del #20. La fila P3 de 2026-06-10 tenia el diagnostico Y el fix correctos; el error fue la severidad. El E2E que lo habria atrapado (`pause-resume`) no corre desde PR #5 por `[GAP-CI-E2E-DB-SUPABASE-ROLES]`. |
| ~~`[GAP-INSTRUMENT-CODE-CASING]`~~ | **CERRADO 2026-06-13 por la gap-closure 02-14..19** — lo dice el propio encabezado de esta seccion ("NO hay P0 abierto"), pero la fila no lo reflejaba y salia como P0 vivo en cualquier lectura mecanica. Se conserva como historico del diagnostico. `[historico]` **NUEVO 2026-06-13 (descubierto en Plan 02-16 al correr el E2E autenticado de verdad por 1a vez). BLOQUEA el tail autenticado [GAP-AUTH-4TEST-RUNTIME] end-to-end.** `GET /test/TwIVI 500 — Instrument version not found for code=TWIVI: no rows`. Los seeds son MIXED-CASE (`TwIVI`, `PERMA-Profiler`; `ONET-IP-SF`/`BFI-2-S` SI son upper) pero el runner hace `code.toUpperCase()` (`app/(b2c)/test/[code]/page.tsx:155`, `done/page.tsx:57`) + lookup case-sensitive `.eq("instrument.code", "TWIVI")` (`lib/session/authenticated.ts` + `anonymous.ts`) que NO matchea; ademas `lib/free/score-on-done.ts:84` compara `=== instrumentCode` ya-uppercased → nunca scorea TwIVI/PERMA → teaser nunca desbloquea. El comentario `page.tsx:154` ("DB stores uppercase") es FALSO; ningun casing de URL unico funciona con `toUpperCase` (rompe O*NET o rompe TwIVI). Es un 500 user-facing VIVO en el flujo guiado real (`loadFreeOrderedCodes` devuelve el casing del seed → `redirect(/test/TwIVI)`). `page.tsx:83` (resolveGlobalPosition) SI es case-insensitive (no roto). **Fix (decision del owner, trade-offs):** (1) `.ilike` en los lookups DB + compare CI en score-on-done (cuidar `_`/`%` wildcards); (2) normalizar seeds a un casing canonico (migracion/re-seed); (3) dejar de uppercasear y resolver el code canonico desde la DB. Fix spec completo en `.planning/phases/02-.../deferred-items.md`. | Free / Fase 2 (cierre) | CC | Arquitectonico (Rule 4), cross-file 02-14/02-17. Reportado por 02-16, NO arreglado (CLAUDE.md §15 + SCOPE BOUNDARY). Owner: nuevo plan (e.g. 02-18) antes de re-correr 02-16. |
| ~~`[GAP-AUTH-4TEST-RUNTIME]`~~ | **CERRADO 2026-06-13 por la gap-closure 02-14..19** — lo dice el propio encabezado de esta seccion ("NO hay P0 abierto"), pero la fila no lo reflejaba y salia como P0 vivo en cualquier lectura mecanica. Se conserva como historico del diagnostico. `[historico]` **NUEVO 2026-06-12. Runtime 02-14/15/17 DONE; E2E 02-16 EJECUTADO (8 passed/3 failed/0 skipped, honest-green guard PASS) pero NO observable verde end-to-end por `[GAP-INSTRUMENT-CODE-CASING]` (arriba).** Waves 1-5 construyeron las PIEZAS; el runtime autenticado se cablo en 3 planes gap-closure: 02-14 (`getOrCreateAuthenticatedSession` + routing guiado en /done + hardening), 02-15 (`/api/respond` multi-escala + cookie SSR + `assertConsentActive`), 02-17 (scoring trigger on /done arrival). 02-16 des-skipeo + autoro los 3 E2E specs con cuerpos reales y los corrio de verdad: gate (a) consent 403/200, gate (d) teaser-lock, gate (e) [parcial] PASAN; el tail + gate (c) + gate (b) FALLAN honestamente — los 2 primeros por el casing bug, gate (b) por `[GAP-AUTH-TRANSITION-MODAL-UNWIRED]` (abajo). | Free / Fase 2 (cierre) | CC | **PROXIMA: arreglar `[GAP-INSTRUMENT-CODE-CASING]` + cablear el transition modal + corregir la superficie del spec de gate (b), luego re-correr 02-16.** NO auto-avanzar a Phase 3. |
| ~~`[GAP-AUTH-TRANSITION-MODAL-UNWIRED]`~~ | **CERRADO 2026-06-13 por la gap-closure 02-14..19** — lo dice el propio encabezado de esta seccion ("NO hay P0 abierto"), pero la fila no lo reflejaba y salia como P0 vivo en cualquier lectura mecanica. Se conserva como historico del diagnostico. `[historico]` **NUEVO 2026-06-13 (Plan 02-16, gate (b)). P1 funcional/etico (NFR-27).** `TransitionScreen`/`DisclaimerModal` (02-07) nunca se renderiza en el runtime autenticado — `app/(b2c)/test/[code]/done/page.tsx` solo hace `redirect()`, nada monta el interstitial de transicion. Asi que el modal NFR-27 (variant bfi\|perma) nunca aparece al cruzar a un instrumento sensible. OJO doble: el spec de gate (b) de 02-16 ademas asserta el modal en la pantalla de ITEM (`/test/[code]`), pero el modal solo monta tras click "Empezar" en el interstitial → la superficie del spec esta mal. El plan owner debe (a) cablear `TransitionScreen` en el boundary de transicion guiada y (b) reescribir el spec para manejar el interstitial real. Contrato probado en unit (`sensitive-ui.test.tsx`). | Free / Fase 2 (cierre) | CC | Reportado por 02-16, NO arreglado (wiring en runtime de otro plan). |

---

## P1 — Prerequisito de una fase cercana

| Flag | Pendiente | Producto/Fase | Owner | Nota |
|---|---|---|---|---|
| `[GAP-MIGRACIONES-MERGEADAS-SIN-LLEGAR-A-PROD]` | **NUEVO 2026-07-29. Una migracion puede quedar mergeada en `main` y NUNCA aplicarse a prod, sin que nada avise.** `Pasado en vivo el mismo dia:` con **#75 mergeado**, `computed_score.raw` en prod **seguia siendo `integer`** — el defecto de ADR-042 siguio vivo en produccion despues del merge, y se descubrio **solo porque se fue a mirar el schema**. `Causa, verificada:` el auto-deploy de `main` es de **Vercel**, que despliega **la app**; **no aplica migraciones de Supabase**. `ci.yml` es el **unico** workflow del repo y levanta un stack local **solo para los tests** — **no existe ningun step de `db push` a prod**, y el `supabase db push` que aparece en `README.md:161` esta en la seccion de **setup local**, no de deploy. `POR QUE ES P1 Y NO P2 — no es el caso, es la REINCIDENCIA:` **`estado/DECISIONS_LOG.md:120` ya registro esta leccion exacta** (*"la asumpcion implicita era 'main pushea -> Vercel deploya -> Vercel runtime conecta a Supabase que ya tiene state listo'"*) y **prescribio el remedio**: un checkpoint `supabase link --project-ref` + `db push` entre `/gsd-ship` y `/gsd-verify-work`. **Se aprendio, se escribio, no se implemento, y volvio a morder** — esta vez con una migracion de **compliance** (COMPL-05). El costo de la proxima es que un fix de datos se declare cerrado estando vivo. `Agravante, y cambia como se verifica:` **el ledger de migraciones de prod NO es fuente de verdad.** `list_migrations` devuelve 001-011, 014 y 015, pero **012, 013, 016 y 017 SI estan aplicadas** — verificado por sus **efectos** (indice unico de `item_response`; `search_path` de `audit_log_chain_hash`; las 4 columnas de 016; el orden `BFI=1, ONET=2, TwIVI=3, PERMA=4` de 017). O sea que **ni siquiera "mirar el ledger" detecta el hueco**: hay que comparar **efectos** contra el schema real. | Transversal / proceso (deploy) | German decide la via · CC ejecuta | `Dos formas, y la barata sirve ya:` (1) **un check de deriva** que compare las migraciones del repo contra el schema de prod **por efecto** y falle ruidoso — es lo unico que cierra el agujero del ledger; (2) el **checkpoint de ship** que ADR de `:120` ya prescribio, que es proceso y depende de que alguien se acuerde. `Precedente del repo para tocar prod:` ADR-038 — **prediccion falsable escrita ANTES + verificacion por md5**; asi se aplico la 018 (huella identica antes/despues). Relacionado: `[GAP-VERIFY-PROD-WRITE-IRREVERSIBLE]`, `[GAP-STEMS-ORTOGRAFIA-RESEED-PROD]`. |
| ~~`[GAP-COMPUTED-SCORE-MEDIAS-DESCARTADAS]`~~ | **NUEVO y CERRADO 2026-07-29 (mig 018 + ADR-042).** `computed_score.raw` era `integer not null` (mig `002:91`) y las formulas `mean` **no redondean** (`lib/scoring/formulas/mean.ts:20`), asi que toda media fraccionaria reventaba el INSERT con Postgres **`22P02`** y el error **se tragaba a proposito** (`score-session.ts:395`). `El defecto no era "no escribe": era "escribe a medias, en silencio".` La perdida **depende de lo que responde el usuario** — `sum` (BFI-2-S, O*NET-IP-SF) siempre da entero y es inmune; `mean` (PERMA 9 reglas, TwIVI 10) falla solo cuando la suma no es divisible. `Medido en prod, no inferido:` faltaban **15 filas** en 2 cuentas (`pruebasgvhv1+r6` PERMA 5/9 · `+r7` PERMA 3/9 y **TwIVI 5/10**) conviviendo con **4 usuarios con PERMA 9/9 completo** — por eso ningun smoke lo vio. `Por que los dos smokes de #40 dieron limpio:` los pares de TwIVI estan **10 apart** (seed `items.sql`: `1,11 CO \| 2,12 TR \| …`) y el vector conducido fue el ciclo `2,4,6,3,5`, de **periodo 5** — cada par recibio el mismo valor por construccion, asi que un vector que parecia variado era, para esta aritmetica, uniforme. `Alcance del dano:` **no observable en el reporte ni en el teaser** (los dos leen `report_snapshot`), **si en el export ARCO** (`app/api/me/data/route.ts:139`, COMPL-05, art. 15 Ley 1581): array `computed_scores` poblado pero incompleto, sin nada que declare cuanto falta. El borrado no se afecta. `Vivo desde 2026-06-12` (primer instrumento `mean` seedeado), no desde la mig 002. `Fix:` mig **018** `integer -> numeric` — no redondear, porque PERMA publica medias 0-10 y el centering MRAT de TwIVI opera sobre medias, y redondear le entregaria al titular un valor que el motor nunca calculo. **SIN backfill** (decision de German): son cuentas de prueba, ningun titular tercero. `Falsado rojo->verde:` `tests/integration/computed-score-fractional.test.ts` fallaba con `22P02` antes de la migracion y pasa despues, con la migracion como unica variable; afirma **igualdad exacta con 4.5** para cerrar tambien la puerta de un "fix" por redondeo. `Verificado punta a punta:` la suite E2E completa con `E2E_LOCAL=1` da **0 ocurrencias** de `computed_score_insert_failed` (eran 9 por corrida) y deja **27 filas fraccionarias de PERMA escritas** (9 dims x 3 proyectos) contra **0 en BFI y O*NET** — los `sum`, inmunes, como predecia el diagnostico. | Transversal / scoring · compliance | CC | `Lo que este cierre NO cierra:` el **E2E sigue sin cubrir la pata TwIVI** — su fixture tambien es par-uniforme (0 filas fraccionarias de TwIVI en la corrida). El test de regresion si la cubre, porque afirma sobre la **columna** y sobre cualquier regla `mean` del seed, no sobre un instrumento. Relacionado: ADR-042. |
| ~~`[GAP-SIN-LOGOUT-SESION-PERSISTENTE]`~~ | **CERRADO EN CODIGO 2026-07-30 — rama `feat/logout-sesion` (`d0dca30`), pendiente de merge.** `logoutAction` en `app/(account)/me/data/actions.ts` borra **dos** cosas por decision de German: la sesion de Supabase **y** la cookie `anonymous_session_id`, porque sin lo segundo en un dispositivo compartido el siguiente usuario heredaba la sesion anonima **hasta 7 dias** y el logout protegia menos de lo que aparentaba. `Costo aceptado y explicito en el helper de copy:` un usuario legitimo que vuelve pierde el progreso anonimo que aun no esta asociado a su cuenta. `Donde vive (la pregunta que la propia ficha dejaba abierta):` **solo en `/me/data`**, arriba del bloque destructivo y sin su separador — hoy **no hay header global**, y crear chrome global es scope de UX/Cowork, no de este fix. `Verificado ANTES de escribir, y cualquiera de los tres habria hecho un no-op silencioso:` la cookie se setea con **`path: "/"`** (`middleware.ts:55`), asi que el `delete` por defecto —que asume ese path— la borra de verdad; el minteo es **`absent => nanoid nuevo` y solo en rutas `/test/`** (`middleware.ts:33-37`), asi que redirigir a `/` no resucita la sesion previa; y **un Server Action SI puede mutar cookies** aunque un Server Component no. `Cobertura:` 5 unit que afirman **estado, no llamadas** (el cookie store es un Map real cuyo `delete` muta; se afirma que un `getUser()` posterior devuelve null) + 1 E2E que afirma que el boton **existe y es alcanzable**, lo que los unit no pueden ver. **Falsacion corrida, no declarada:** 3 inyecciones con conjuntos disjuntos. `Lo que NO se hizo, a proposito:` sin audit (no existen acciones de audit de sesion y el logout no modifica datos) y sin `revalidatePath` (el redirect ya remonta; montar un revalidate junto a un cambio de UI fue la causa del defecto de #64). `La decision de borrar la cookie anonima esta registrada:` **ADR-044**, que viaja en el mismo PR #85 — es un trade-off de privacidad, no una decision tecnica, y por eso lleva ADR propio. | Transversal / auth · B2C | CC (hecho) | La pregunta "donde vive" quedo resuelta a favor de `/me/data`; un header global sigue disponible como mejora de descubribilidad, con owner UX/Cowork. `[historico de la ficha original abajo]` **NUEVO 2026-07-29 (observado durante el smoke con cuenta de #40).** **La app no tiene forma de cerrar sesion.** `Verificado en codigo, no por impresion:` `signOut` aparece en **exactamente 3 sitios y ninguno es una accion del usuario` — `app/auth/callback/route.ts:217` y `:221` son **ramas de error** del callback, y `app/(account)/me/delete/actions.ts:117` corre **despues** de borrar la cuenta, cuando ya no hay a que volver. `Verificado en prod ademas:` `/me/data` expone solo `Guardar cambios` y `Descargar todos mis datos (JSON)`; cero link o boton de sesion en esa pantalla ni en el header. `Consecuencia:` la sesion del magic link **persiste**, asi que en un dispositivo compartido o prestado el siguiente que abra `descubreme.co` entra a la cuenta anterior — y lo que queda expuesto es el **perfil psicometrico completo** mas `/me/data`, que incluye el export ARCO y el borrado de cuenta. `Por que P1 y no P2:` no es comodidad de UX, es exposicion de datos sensibles de Ley 1581 sin ninguna mitigacion en producto (hoy la unica salida es borrar cookies a mano). `NO verificado, y afina la severidad:` la duracion efectiva de la sesion de GoTrue en prod — si expira corto, el peor caso es mas chico. Medirlo antes de dimensionar el fix. `Alcance:` es **anterior a #40** y no es regresion suya; se encontro haciendo su smoke. | Transversal / auth · B2C | CC | El fix minimo es un boton que llame `supabase.auth.signOut()` + redirect; lo que hay que decidir aparte es **donde vive** (header global vs `/me/data`) y si el B2B lo necesita distinto. Relacionado con `[GAP-RETURNING-USER-RESIGNUP-AGE]` (ADR-035): las dos filas son sobre el ciclo de sesion, entrada y salida. |
| ~~`[GAP-CONSENT-REVOKE-CHIP-SIN-CONFIRMACION]`~~ | **CERRADO 2026-07-28 por PR #64 (`74cb87b`), en prod.** La hipotesis de la fila de abajo (auto-dismiss o desmontaje) resulto **la segunda, y sin temporizador de por medio**: `revokeConsentAction` hace `revalidatePath("/me/consent")` en `actions.ts:80` y **devuelve el mensaje despues** (`:82`); el cliente lo guarda en estado de `ConsentCard` y lo pinta **dentro del `<article>` de esa misma card**, que la revalidacion **desmonta** al dejar `active = []`. **No era timing: el acuse lo destruia la revalidacion que dispara la propia accion, o sea funcionaba por carrera.** `La raiz era una asimetria entre dos paginas:` `/me/data` **ya listaba** los revocados con `MC_CONSENT_REVOKED_CHIP` (`page.tsx:168`); `/me/consent` filtraba solo activos. `El fix:` `/me/consent` lista **tambien** los revocados y el acuse pasa a ser **estado de la fila** — sin estado efimero no hay carrera, y sobrevive a un refresh; `result` se conserva **solo para el error**, donde la fila sigue activa. **Sin microcopy nueva** (reusa `MC_CONSENT_REVOKE_SUCCESS`, ya firmada). `Verificado por falsacion:` revirtiendo **solo** el componente y la page, el test falla **local y deterministicamente** en la asercion original. El spec suma un **`page.reload()`** — la asercion que el codigo viejo **no podia** satisfacer, y sin la cual podria volver a pasar por accidente. **-> ADR-041** (2026-07-29, decision de German): la regla que queda es que **el acuse de una accion irreversible de Ley 1581 es estado de la FILA, nunca estado de cliente efimero** — y aplica tambien a borrado de cuenta y export ARCO. Se documenta como ADR porque **filtrar activos + toast es la simplificacion que parece obvia leyendo la pagina, y reintroduce el defecto**. `[historico]` **NUEVO 2026-07-28 noche. Se venia contando como flaky de E2E y la traza dice otra cosa.** `account-delete-2-clicks.spec.ts:166` ("Per-consent revoke button opens sober modal + success chip") va por su **TERCERA aparicion**, y esta es la primera vez que hay artefacto que mirar — lo subio el paso nuevo de `upload-artifact` (PR #59). **El error NO es un timeout:** es `element(s) not found` sobre `getByText(/consentimiento revocado/i)`, y `grep -c "Test timeout of"` da 0. `Lo que muestra el snapshot de la pagina en el momento de la asercion:` **la revocacion SI funciono** —`paragraph: "No tienes consentimientos activos."`, la fila desaparecio— y el chip de exito no esta por ningun lado; la region `- alert` figura **vacia**. Fallo el intento 1 y el 2, paso el 3. `Hipotesis, no diagnostico cerrado:` el chip tiene auto-dismiss (o vive dentro de la fila que se desmonta al vaciarse la lista) y en un runner lento la asercion llega despues de que se fue. **Si es asi, el defecto no esta en el test: esta en que una accion irreversible de Ley 1581 se confirma con un mensaje que puede no llegar a verse** — el usuario revoca, la fila se esfuma y no recibe acuse. `Que falta para cerrarlo:` leer el componente de `/me/consent`, confirmar si el chip tiene temporizador y donde monta, y decidir entre persistir el acuse (banner que no se auto-cierra) o anclar el test a un estado estable. **No se toco nada todavia.** | Fundacion / Ley 1581 + UX | CC (diagnostico + fix) + German (decision de UX) | Distinto de `[GAP-E2E-FLAKE-RESPOND-500-CONCURRENCIA]`, que si es concurrencia. La regla de German —"si vuelve a salir merece flag propio"— se disparo en la corrida del PR #61. `Nota de metodo:` esta fila existe **porque #59 subio la traza**; con `if: failure()` no habria artefacto, porque un flaky pasa al reintento y la corrida concluye `success`. |
| ~~`[GAP-TESTS-INTEGRACION-HUECOS]`~~ | **CERRADO 2026-07-28: LOS CUATRO PASOS.** `Pero ADR-039 NO queda cerrado, y la distincion importa:` este flag cubre los **9 criterios** del ADR (9/9), no sus **tres condiciones sistemicas** — la **#1** se cumplio en #51 (gate 16), la **#2** se ejecuto y volvio con un **NO** deliberado (-> ADR-040), y la **#3 se cerro en #69** (`tests/integration/guided-contention-flow.test.ts`, 328 lineas: afirma el flujo guiado en el choke point `allComplete`, no el banner aislado). **Estado real de ADR-039 al 2026-07-29: 9/9 criterios + 3/3 condiciones — CERRADO.** El paso 4 —los 3 criterios de Ley 1581, que eran "el trabajo real"— entro en tres PRs, **cada uno falsado con inyecciones DISJUNTAS** (una por test; que ninguna las tumbe todas ES la evidencia de que no miden lo mismo): **#61 `COMPL-08`** — el control **es el filtro de query** `.is("revoked_at", null)` y `createMockSupabaseClient` devuelve su resultado **sin mirar la cadena**, asi que **ningun unitario podia tocarlo** (cobertura CERO real, como se sospechaba); la asercion es un **delta de la misma entrada** (pasa antes de revocar, lanza 403 despues, mismos argumentos) y sin la mitad "antes" un seed roto da 403 igual y el test no verifica nada. **#63 `COMPL-07/D1.5`** — **el contrato escrito estaba mal: decia 7 tablas por cascade y son 6** (`pg_constraint`, `confdeltype='c'`); la 7a es `waitlist`, **sin FK**, borrada por email. El centinela de la anonimizacion es **`entity_id`**, que `anonymize_user_audit` **NO** toca. **#65 `COMPL-05/06`** — la cobertura previa afirmaba `href="/api/me/data"`, o sea **que el boton apunta bien**: un export con las 6 claves **vacias** la pasaba y violaba el derecho de consulta; ahora se afirma **completitud** (conteo por area), que el texto plano **no** este en el envelope cifrado, y que el 401 **no serialice** el email. `Gate 17` quedo extendido con los 3 criterios y sus guards **y su docstring de alcance ampliado** (nacio cubriendo criterios BORRADOS; ahora tambien los IMPLEMENTADOS) — no tocarlo habria reproducido QUAL-08. **Cifras verificadas contra el run `30410719805` sobre `main` = `a1ffc07`, no heredadas: E2E 33 · 0 flaky · lint 19 · unit `485 passed \| 21 todo` (506).** `LO QUE ESTE CIERRE NO CIERRA, y hay que leerlo explicito:` este flag es **uno de los cuatro mecanismos de ADR-040**, no la familia entera. **El genero 4 (vacuidad por conjunto vacio) sigue ABIERTO en `[GAP-TESTS-VACUIDAD-CONJUNTO-VACIO]` P2** — la instancia se cerro en #58, el **genero nunca se barrio**, y `>=1` es un **piso, no un total**. Un verde del gate 16 **no** significa "no hay pases vacuos". `[historico]` **PASOS 1, 2 y 3 CERRADOS (2026-07-28); queda SOLO el paso 4.** Cuatro PRs mergeados en orden: **#51** (asercion de esquema QUAL-08 + **gate 16**, el detector por AST) · **#52** (borrado del hueco de COMPL-17 + **gate 17**, el registro criterio->guard) · **#53** (flag) · **#54** (degrada los 30 restantes a `it.todo`; allowlist del gate 16 **30 -> 0**). `La cifra que esto corrige:` de **`493` inflados por ficcion** a **`470 passed` + `33` declarados pendientes** — total 503 sin cambio, porque **nada se borro salvo el hueco de COMPL-17**. No es una regresion: es el conteo dejando de mentir. Despues **#55** cerro `[GAP-COMPL17-FEEDBACK-IDOR-SIN-GUARD]` con 5 guards reales -> **`475 passed | 28 todo`**. **Paso 4 = los 3 criterios de Ley 1581 con test real** (`COMPL-08`, el unico con cobertura CERO -> `COMPL-07/D1.5` -> `COMPL-05/06`), **y German pidio acordar alcance antes de escribir una linea.** `[historico]` **NUEVO 2026-07-28 (resultado de `[GAP-GATES-SKIPPED-AUDITORIA]`, que Cowork pidio al cerrar D3.3).** La auditoria empezo por `plugin-swap.test.ts:140` (un `it.skip` de cuerpo vacio cuya razon "requires DATABASE_URL" ya no aplica) y encontro que **ese skip es 1 de 18**. `El hallazgo:` **24 bloques de `tests/integration/` no afirman nada** — su cuerpo es un plan en comentarios mas `expect(hasDb).toBe(true)`, que dentro de la rama `itIfDb` es una **tautologia**. **18 de ellos REPORTAN `passed` en CI**, no `skipped`, porque `DATABASE_URL` existe desde la entrega 2; los otros 6 son los `it("contract documented; runtime gated on DATABASE_URL")`, sin gate, que pasan siempre. `Medicion (dos metodos independientes que coinciden):` analisis estatico por bloque = 24 huecos; y correr `tests/integration` con y sin `DATABASE_URL` da **33 -> 57 passed**, con identidad por nombre de **26 ganados / 2 perdidos** (`33 + 26 - 2 = 57`). De los 26 ganados **solo 8 son reales** (`audit-immutable` 3, `feedback-ownership` 5). **La entrega 2 no encendio cobertura: convirtio 18 skips honestos en 18 verdes que no afirman nada**, y los 18 cuentan dentro de los `493 passed`. Archivos: `baremo-fallback` (5), `baremo-telemetry` (4), `ethics-middleware` (4), `data-rights` (5), `respond` (3), `consent-revoke` (2), `plugin-swap` (1). `Hallazgo lateral:` `audit-immutable` **si** afirma de verdad, pero sus 3 tests abren con `if (!c) return;` (L60/L81/L100) — si el cliente no conecta **pasan sin afirmar** en vez de fallar. `ALCANCE DE LA AUDITORIA, explicito para que nadie la lea como mas amplia de lo que es:` el barrido cubrio **`tests/integration/` y la familia de gates `hasDb`/`HAS_DB`/`DATABASE_URL`**. **Otras formas de test hueco** (`expect(true).toBe(true)`, `expect(typeof x).toBe("function")` y similares) **fuera de `tests/integration/` — en particular todo `tests/unit/` — NO se barrieron.** Es probable que haya mas. **Decision de German 2026-07-28: solo documentar por ahora**; implementar los 18 es su propia fase. **PLAN CERRADO Y FIRMADO 2026-07-28 -> ADR-039.** Cowork firmo orden, exclusiones y cierre provisional. **De los 9 criterios, 3 NO requieren escribir test:** `COMPL-17` se **borra** (redundante con unitarios reales, con mapeo criterio->guard registrado), `QUAL-08` se **reemplaza por una asercion de esquema** (no se borra: el control es el esquema y hoy **nadie lo vigila** — la unica mencion en el repo esta en un **comentario dentro del propio test hueco**, `baremo-telemetry.test.ts:35`), y `QUAL-06` se **difiere** (funcion no lanzada, no criterio muerto). **El trabajo real son 3 criterios de Ley 1581, en este orden: `COMPL-08` (unico con cobertura CERO) -> `COMPL-07/D1.5` -> `COMPL-05/06`.** Mas 3 condiciones sistemicas: meta-lint como **gate 16**, extender el detector a `tests/unit/`, y cerrar el riesgo de contencion en `[GAP-PERMA-CONTENTION-GUIDED-FLOW]` con un test **del flujo guiado**. `Secuencia:` nada se degrada hasta aplicar borrado/reemplazo/diferido — **una sola pasada**. `El resultado VOLVIO a Cowork` (fue su accion derivada del ADR-037): `estado/BRIEF_Cowork_Auditoria_Gates_Huecos_v1.0.md` + el intercambio en `estado/COWORK_PROMPTS_GATES_HUECOS_v1.0.md`. **Lo que lo hace asunto de Cowork y no solo de ingenieria:** los 18 huecos vigilan **nominalmente** `COMPL-05/06/07/08/12/13/17`, `QUAL-06/08` y `FOUND-06` — **7 de 9 codigos son de compliance**. Sin asercion hoy: borrado transaccional de Ley 1581, bloqueo de escritura tras revocar consentimiento, `disclaimer`+`contention` ante malestar emocional, rechazo de `user_id` inyectado, y no-filtracion de PII en telemetria. `Sin exagerar:` no dice que esten rotas —varias tienen cobertura por otras vias— dice que **el test que se presenta como su guardia no lo es**. La alternativa barata y honesta, si se quiere cerrar antes, es degradarlos a `it.todo` (dejarian de decir "passed"; la suite iria a ~475 passed + 18 todo). | Transversal / calidad (tests) | German decide, CC ejecuta | Es la misma familia que la **compuerta vestigial** de `[GAP-E2E-SKIPS-E2E-LIVE]`, un paso peor: un `skip` al menos se declara ausente en el reporte; una tautologia se declara **presente**. **Y es UNO de cuatro mecanismos, no el defecto entero -> ADR-040**, que tabula los cuatro y deja explicito que **el gate 16 cubre solo este**. Los otros tres: import silenciado (#56), retorno temprano (#57) y conjunto vacio (`[GAP-TESTS-VACUIDAD-CONJUNTO-VACIO]`, abierto). |
| ~~`[GAP-COMPL17-FEEDBACK-IDOR-SIN-GUARD]`~~ | **CERRADO 2026-07-28.** Los **5 bloques de ownership** pasaron de `it.todo` a **tests reales contra el stack**: siembran `assessment_session` por **postgres.js**, dejan el **service-role REAL** (la mitigacion es *leer la fila y comparar el dueño* — mockear el admin client probaria el `if`, no el control) y mockean **solo la identidad** (`next/headers` + `getSupabaseServerClient`), que es lo unico que no existe sin servidor HTTP. `Detalle que obligo a un centinela:` **`feedback_event` no tiene columna de sesion**, asi que en el camino anonimo la fila queda `user_id = null` + `report_snapshot_id = null` e **indistinguible de cualquier otra fila anonima** — un `count(*)` global no podria afirmar ni que se inserto la propia ni que no se inserto ninguna; cada test manda un centinela unico en `text` y consulta por el. `Verificado por falsacion, no por pasar:` **tres inyecciones que enrojecen conjuntos DISJUNTOS** — desactivar ownership (`route.ts:104-120`) tumba los **2 de IDOR**; aceptar la sesion inexistente (`:100-102`) tumba **solo** el de no-leak; hacer que ownership **siempre** rechace tumba **solo** los 2 caminos felices. Que ninguna las tumbe todas es lo que muestra que no miden lo mismo. Registro del **gate 17** volteado a `covered` con los 5 guards. `Cifras medidas con stack + db reset, predichas antes de correr:` unit **`470 passed | 33 todo` -> `475 passed | 28 todo`** (503 sin cambio), lint **19**. `[historico]` **NUEVO 2026-07-28 (paso 2 de ADR-039, PR #52).** La mitigacion **IDOR de `POST /api/feedback`** esta implementada y **no la testea nadie**. `El control existe:` `app/api/feedback/route.ts:96-120` verifica propiedad de la sesion y devuelve **404** al no-dueño (nunca 403, que filtraria existencia). `Lo que falta es el guard:` los **5 bloques** de `tests/integration/feedback-ownership.test.ts` son **huecos** — cuerpo = plan en comentarios + `expect(true).toBe(true)` — y ese archivo es el **unico de toda la suite que toca `/api/feedback`**. `Como se encontro:` la auditoria de `[GAP-TESTS-INTEGRACION-HUECOS]` habia clasificado esos 5 como **REALES** (eran 5 de sus "8 reales"); el detector por AST del gate 16 los marco como huecos. **Los dos metodos de la auditoria compartian el punto ciego** — su regla de tautologia cubria el simbolo-compuerta (`hasDb`) pero no los **literales** (`expect(true)`), asi que coincidieron **por tener el mismo error** y la coincidencia se leyo como confirmacion. `Poblacion corregida:` 31 huecos, no 24 (`expect(hasDb)` x18 · `expect(typeof hasDb)` x6 · `expect(true)` x5 · cuerpo vacio x2). **POR QUE IMPORTA PARA EL PASO 2:** ADR-039 manda borrar "el hueco de COMPL-17" citando guards de **`/api/respond`** — pero COMPL-17 esta anclado en **dos rutas con amenazas distintas**, y esos guards **no cubren `/api/feedback`** (verificado: `tests/unit/api/respond-multiscale.test.ts:138` importa `@/app/api/respond/route` y ninguna otra; y `feedback` **no aparece en ninguna forma** en `tests/e2e/`). Borrar los 5 habria dejado una mitigacion de seguridad implementada con **cero vigilantes**, repitiendo el error que el propio ADR documenta para QUAL-08 — y peor, porque alla el control era el **esquema** y aca es **codigo de autorizacion en runtime**, mas facil de romper en un refactor. **DECISION German 2026-07-28: opcion A** — borrar solo el hueco de `/api/respond`; los 5 de feedback **no se tocan** y pasan al paso 3 (degradar a `it.todo`). `Registrado y verificado por CI:` entra en `tests/lint/compliance-guard-map.test.ts` (gate 17) con `status: "gap"`, que exige flag de BACKLOG — esta fila es ese flag. `La amenaza no es hipotetica:` salio de un security review sobre el commit `4f09666` (usuario A mandando feedback contra la sesion de B contamina las metricas del reporte de B y crea desajuste de atribucion). `Trabajo pendiente:` escribir el test real de ownership (404 al no-dueño autenticado, 404 al anonimo con cookie ajena, happy path D3.4, 404 a sessionId inexistente sin filtrar existencia). | Transversal / calidad (tests) + seguridad | German prioriza, CC ejecuta | Hijo directo de `[GAP-TESTS-INTEGRACION-HUECOS]`. **No dice que el control este roto** — dice que el test que se presenta como su guardia no lo es. Ref: ADR-039 paso 2, PR #52. |
| ~~`[GAP-TWIVI-BAND-DEFINICION-DOBLE]`~~ | **CERRADO Y VERIFICADO EN PROD 2026-07-27 — LAS DOS MITADES.** Ver el detalle al final de la celda. `[historico]` **NUEVO 2026-07-27 (smoke PR #24/#26).** **El seed autoro 3 bandas y el scoring solo puede producir 2:** TwIVI tiene 12 narrativas (4 HOV × ALTO/MEDIO/BAJO) pero `bandFromMrat` (`lib/scoring/mrat.ts:117`, usada en `score-session.ts:435`) es un **test de signo** — MEDIO exige empatar el MRAT propio con 1e-9, asi que **las 4 narrativas MEDIO son inalcanzables**. Sintoma visible: el reporte de Valores **se contradice consigo mismo**: la tabla `sr-only` del circumplejo dice `Destacar → Medio` y el parrafo de esa misma dimension dice "el logro personal pesa menos" (BAJO). Son **dos definiciones distintas de banda** sobre los mismos 4 HOV: narrativa = signo (`bands_by_dim` del snapshot), circulo = corte por z (`computeIpsativeBands` recomputada en `visual-dimensions.ts:105`). Aritmetica verificada contra la snapshot real. **La segunda regla parece accidental:** su comentario la justifica con "no se reusa la de los 10 valores", premisa **nunca cierta** — las 6 snapshots TwIVI vivas tienen siempre 4 claves, y `bandFromMrat` (`e2572d8`, 2026-06-12) es **anterior** a `visual-dimensions.ts` (`cf18343`, 2026-07-23). **Las dos rutas NO son simetricas:** Opcion B (el visual lee `bands_by_dim`) es 1 linea y aplica ya a los 6 reportes existentes, pero conserva la regla de signo; Opcion A (el scoring pasa a z) hace existir MEDIO pero `bands_by_dim` **esta guardado en el snapshot** → cola de migracion para las 6 snapshots vivas. **DECIDIDO 2026-07-27 (German): A+B — ADR-036.** El scoring pasa a `computeIpsativeBands` (MEDIO revive) **y** el visual deja de recomputar: lee `bands_by_dim` (fuente unica). Con las dos, cada reporte queda coherente **consigo mismo** sin re-scorear: los 6 viejos con la regla de signo en sus dos superficies, los nuevos con z en las dos → **la migracion es opcional, no prerequisito**. **MERGEADO Y EN PROD (`main 7e347da`). Mitad B VERIFICADA EN PROD 2026-07-27** (deploy-smoke sobre `96fe99d5`, la snapshot del ADR): la tabla `sr-only` del circulo dice ahora `Destacar → Bajo` donde decia `Medio`, las 4 filas reproducen `bands_by_dim` verbatim y la narrativa de esa dimension coincide — **la contradiccion esta muerta**; ADR-034 intacto (radios 70/51.6/47/36.3, identicos al smoke del 24/7). **Mitad A VERIFICADA con una corrida NUEVA de TwIVI** (Ruta 1: cuenta `+adr036`, signup de German, navegacion directa a `/test/twivi` = 20 items en vez de 110, perfil del ADR reproducido). No se podia sobre un reporte existente — las 6 snapshots previas guardan bandas de la era del signo, que es la propiedad diseñada que evita la migracion, y `getOrCreateAuthenticatedSession` (`lib/session/authenticated.ts:93-106`) devuelve siempre la sesion existente. **La prueba es un delta de misma entrada:** la snapshot nueva `c98e28e0` tiene `scores_by_dim` **byte-identico** al de las 3 viejas y da `SEN=MEDIO` contra el `SEN=BAJO` de aquellas — la celda exacta que se predijo, sin aritmetica intermedia que auditar. **Y las 4 narrativas MEDIO, que eran contenido muerto, renderizan:** la de Destacar dice ahora "La autopromocion pesa de forma pareja...", donde el reporte viejo con los mismos puntajes decia "el logro personal pesa menos". NO se re-scoreo ninguna snapshot vieja. Ref: `estado/SMOKE_ADR036_RESULTADOS_v1.0.md`. `[historico]` **IMPLEMENTADO EN CODIGO por PR #29** (`fix/twivi-banda-unica-adr-036`). A: `score-session.ts` bandea con `computeIpsativeBands` sobre los 4 HOV centrados — las dos ramas del dispatch quedan con la misma regla, difiriendo solo en QUE bandean. B: `projectCircumplexDimensions` recibe `bandsByDim` y lee la banda del payload (mas el call site de `assembler.ts`); de paso `centeredHovScores` sale de ese archivo porque solo alimentaba un juego de claves que `rawHovScores` da identico. `bandFromMrat` **no se borro**. **Nota de implementacion (no estaba en el ADR):** BAJO se vuelve mas exigente — con 4 dimensiones un pico solitario infla la SD y comprime al resto hacia MEDIO (el fixture del spike SD=6/resto=1 pasa de tres BAJO a tres MEDIO). Es la contracara aritmetica de revivir MEDIO. **Verificado sin gap aguas abajo:** el teaser deriva banda dominante de `bands_by_dim` y el seed `integrator-rule/teaser` **ya trae variantes MEDIO para TwIVI**, asi que no cae al fallback; y el mini-resultado no se toca porque `composeDominantOrPair` elige por magnitud centrada, nunca por banda. Gates CI: typecheck/lint/unit/Vercel en verde; E2E rojo con **las mismas 9 fallas preexistentes de `main`, set identico spec por spec** (delta cero, baseline run 30295479316 vs 30296892702). | Reporte Free / TwIVI | CC (mergear + smoke) | No es defecto de #24 — #24 hizo VISIBLE un desacuerdo latente (antes la seccion estaba vacia). Ref: `estado/SMOKE_PR24_PR26_RESULTADOS_v1.0.md` §2.1 + ADR-036 + PR #29. |
| ~~`[GAP-TWIVI-REPORT-NARRATIVE-EMPTY]`~~ | **CERRADO Y VERIFICADO EN PROD 2026-07-27** (PR #24 `f61e4a9`; deploy-smoke sobre `96fe99d5`): "Que sugiere esto sobre ti" trae **4 parrafos** y cada uno empareja con su banda real (CSV BAJO / OCH ALTO / SEN BAJO / STR MEDIO), acentuados y en tuteo. Se verifico la **correspondencia**, no solo el conteo. `[historico]` **NUEVO 2026-07-27 (smoke PR #20).** El reporte de Valores renderiza el encabezado "Que sugiere esto sobre ti" **con NADA debajo** (medido: 27 chars entre ese heading y el bloque siguiente; los otros 3 reportes si traen narrativa). **Causa exacta:** `lib/report/assembler.ts:403` deriva `dims` de `Object.keys(payload.scores_by_dim)`, y para TwIVI ese objeto trae los **10 valores Schwartz**, mientras las narrativas estan keyed por los 4 HOV (que viven en `bands_by_dim`). Verificado en el snapshot real de prod (`96fe99d5`): `scores_by_dim` = AC,BE,CO,HE,PO,SD,SE,ST,TR,UN vs `bands_by_dim` = CSV,OCH,SEN,STR. Las 12 narrativas HOV **existen en prod y estan en tuteo**; nunca se muestran. Misma trampa que el handoff de PR-C documento para el composer (el composer la respeto reconstruyendo HOV; el assembler nunca se ajusto). **Fix:** derivar `dims` de `bands_by_dim` en el path circumplejo; revisar que el default `?? "MEDIO"` de `assembler.ts:473` no siga enmascarando claves inexistentes. | Reporte Free / TwIVI | CC | Promesa vacia en pantalla: el usuario ve el titulo de una seccion que no existe. Ref: `estado/SMOKE_PR20_RESULTADOS_v1.0.md` §2.1. |
| ~~`[GAP-ONET-ANON-SENSIBLE-PRECONSENT]`~~ | **RESUELTO POR DISEÑO 2026-06-26 (ADR-029):** el funnel invertido (signup + consentimiento PRIMERO, cero tramo anonimo) elimina toda recoleccion anonima → O*NET tambien queda post-consent; el gap desaparece estructuralmente (no se mitiga, se elimina). `[historico]` **NUEVO 2026-06-26 (descubierto al revisar W7 v0.2; advisor + verificado con fuente primaria).** El gancho anonimo VIVO en prod (O*NET IP-SF) recolecta respuestas de intereses que la politica de consent v1.0.0 §3 (`lib/consent/text/1.0.0.md:44-46`) clasifica **datos sensibles Art. 5** (Ley 1581), de forma **anonima y pre-consentimiento** (el dual-consent va en signup, despues de los 60 items O*NET). El guard `lib/consent/guard.ts:85` solo exige consent-sensible para instrumentos `sensitivity='high'`, y O*NET es `'normal'` → la clasificacion Art. 5 de la politica y el enforcement del guard DIVERGEN. Es el caso "fila 1" del abogado ("respuestas al backend antes del consentimiento = no recomendable"). Exposicion ACTUAL baja (pre-lanzamiento, ~4 usuarios de prueba, sin trafico real) pero estructural. **Mismo fix que v0.2 §5.5: mover O*NET al tramo autenticado.** Opciones: (a) O*NET→autenticado; (b) consent-sensible anonimo para O*NET; (c) reclasificar intereses en §3 si el legal lo respalda. El paquete legal de W7 debe cubrir este funnel, no solo BFI. | Free / compliance (pre-trafico real) | CC + German + legal | Puede ser MAS urgente que W7 por estar LIVE. Decidir mitigacion interim. Ref: `estado/DESIGN_W7_..._v0.2.md` §2.7/§5.5/§11. |
| ~~`[ENV-PHASE2-NOT-IN-PROD]`~~ | **CERRADO 2026-07-29 (auditoria P1 PM-23) — era un FANTASMA de mes y medio, y era el que hacia parecer que la Fase 2 seguia abierta.** Cada afirmacion verificable de esta ficha es falsa hoy, medida contra la Supabase de PROD: decia `instrument_count=1 (solo ONET)` -> hay **4 instrumentos**; decia `sin integrator_rule` -> hay **14 reglas**; decia `sin visual_type` -> **4 versiones con visual_type**; decia `origin/main = 8d58fc5 (Phase 1), 48 commits de Phase 2 sin pushear` -> `main` lleva la Fase 2 mergeada desde semanas. `La evidencia que lo cierra sin ambiguedad:` prod tiene **28 `report_snapshot` de 10 usuarios distintos** y **186 narrativas** — o sea usuarios reales completaron el journey Free contra el schema de Fase 2. Ademas la mig 018 fue aplicada y verificada el 2026-07-29 y el smoke de #40 dio 15/15. `Enunciado original abajo, conservado:` **NUEVO 2026-06-13 (descubierto en `/gsd-verify-work 2`).** La superficie de Phase 2 NO esta en prod: origin/main = `8d58fc5` (Phase 1), las 48 commits de Phase 2 sin pushear; Supabase prod (`tzhhqaducmbxfebuyvnv`) en Phase-1 (migrations hasta 011, instrument_count=1 ONET, sin integrator_rule/visual_type). La app local apunta a prod (`.env.local`: APP_URL=www.descubreme.co + SUPABASE_URL=prod). **Para el UAT completo en prod (decision German), prerequisito secuencia:** (1) `/gsd-execute-phase 2 --gaps-only` (construir el runtime P0); (2) `git push origin main` (despliega Phase 2 a la web publica — ojo: revisar que no quede a medias visible); (3) aplicar migrations 014/015 + 24 seeds a la Supabase de PROD (cambio de schema en produccion, semi-destructivo, requiere autorizacion explicita + runbook); (4) UAT completo en prod (magic link real + journey 4-test + teaser). **Efecto secundario ya visto:** correr codigo Phase-2 contra schema Phase-1 produce falsos-fallos (el "progreso de 0" del UAT). | Free / Fase 2 (cierre + UAT prod) | CC + German | No es code-gap; es secuencia de deploy/ops. El UAT del flujo autenticado NO es validable hasta (1). |
| ~~`[GAP-UI-QUALITY-PREVIEW]`~~ | **CERRADO 2026-06-10 (Fix 5, ADR-021):** causa raiz unica = colision de namespace Tailwind v4 `--spacing-{tshirt}` shadowea `--container-*`, colapsando `max-w-*`/`w-*` (max-w-3xl→64px, max-w-xs→4px). De las hipotesis: (a) era correcta en direccion pero el mecanismo era namespace-collision, no "residuo"; (b) confirmada como issue secundario real (Inter nunca cargada) y arreglada; (c)/(d) NO eran la causa. Solucion Opcion B (raiz): migracion escala 8pt named→numerica + borrado de tokens `--spacing-*`/`--container-*` + Inter via next/font. Verificado en CSS compilado + computed styles + render local (landing+BYS) + typecheck. 28 archivos. | Fundacion / Fase 1 verify-work | — | Resuelto. Branch `verify/phase-1-deploy-strict`; merge a main tras cerrar tests 2-20. |
| ~~`[GAP-VERIFY-TESTS-2-20-PENDING]`~~ | **CERRADO 2026-06-10: 20/20 tests PASS (Test 16 el ultimo en cerrar).** 18 tests de `01-UAT.md` ejecutados tras resolver UI quality: BYS visual + 60 items O*NET + pause-resume + done page hexagono + signup dual consent + magic link + callback claim + reporte 3-capas + email transactional + survey + waitlist + 4 derechos Ley 1581 + 2 technical checks + 2 coverage checks. | Fundacion / Fase 1 verify-work | CC + German | UAT.md tiene tests definidos. Reanudar via `/gsd-verify-work 1` que detecta el UAT activo. |
| ~~`[GAP-VERIFY-BRANCH-MERGE-TO-MAIN]`~~ | **RESUELTO 2026-06-10: merge fast-forward + PUSH a origin/main HECHO + deploy Production READY (`dpl_HDaUnC8...`) en `descubreme.co`/`www.descubreme.co`.** `[historico]` merge = FAST-FORWARD LOCAL hecho (27 commits, 0 divergencia con main). PUSH a origin/main PENDIENTE: dispara deploy Production (dominio `descubreme-gsd-git-main-*` existe); env vars scoped a Preview no Production + Supabase Auth prod (Site URL/SMTP/NEXT_PUBLIC_APP_URL) sin replicar => aprovisionar Production antes de pushear, o aceptar primer deploy prod roto. Reversible con `git reset --hard` mientras no se pushee.** `[historico]` Decidir si los 3 commits de `verify/phase-1-deploy-strict` (`a2d4142`, `2ae3740`, `9542672`) se mergean a `main` ahora O tras completar los 18 tests pendientes. Mergeo ahora: `vercel.json` + Tailwind v4 fix + Next.js 16 cookie fix llegan a main pero verify-work no esta cerrado. Mergeo despues: branch crece y posible drift con main. | Fundacion / Fase 1 | German | Decision post UI quality fix. |
| `[GAP-VERIFY-PROD-WRITE-IRREVERSIBLE]` | 11 migrations + 8 seeds ya estan aplicados a PROD Supabase tzhhqaducmbxfebuyvnv via MCP (sesion 2026-06-08 PM). No rollback automatico. Si futura migracion 012+ requiere down migration de 010 o 011, hay que escribir SQL manual. | Fundacion / Fase 1 onwards | CC + German | Documentar en deploy runbook para Phase 2. |
| ~~`[GAP-AUTH-URL-CONFIG]`~~ | **CERRADO (auditoria P1 PM-23), verificado POR EFECTO contra prod.** Los fixes de codigo se aplicaron el 2026-06-10; lo que quedaba era la config de Supabase Auth (Site URL + allowlist), que no se puede leer desde el repo. `La evidencia por efecto:` prod tiene **28 `report_snapshot` de 10 usuarios distintos**, y ningun usuario llega a tener snapshot sin atravesar el callback del magic link. Si el redirect siguiera cayendo a `localhost:3000`, ese numero seria 0. Corroborado por el smoke de #40 (15/15, magic link real). `Enunciado original abajo, conservado:` **Descubierto 2026-06-10 (verify Test 8-9):** el magic link de signup redirige a `localhost:3000` (con `otp_expired`) en vez del origen deployado. Doble causa: (1) `NEXT_PUBLIC_APP_URL` no estaba en el env de Vercel Preview → `emailRedirectTo` quedaba relativo (`/auth/callback`) → Supabase lo descarta y cae al **Site URL** del proyecto, que es `localhost:3000` (default). (2) Supabase Auth **Site URL + Redirect URLs allowlist** no configurados para los entornos deployados. **Fixes aplicados 2026-06-10:** (1) strip de trailing slash en `actions.ts` + `NEXT_PUBLIC_APP_URL` agregada a Preview (branch alias); (2) usuario configuro Supabase dashboard (Site URL + Redirect URLs allowlist con `/auth/callback`); (3) **bug latente descubierto al llegar bien el redirect: 404** — el callback handler vivia en `app/(auth)/callback/route.ts` y el route group `(auth)` quita `auth` del path, asi que servia `/callback`, no `/auth/callback`; movido a `app/auth/callback/route.ts`. **Pendiente:** retest E2E del usuario (link fresco) que confirme callback → sesion → `/reporte`. **PROD necesita la misma config Supabase + `NEXT_PUBLIC_APP_URL` en env Production.** | Fundacion / Fase 1 verify + deploy prod | CC (codigo, hecho) + German (Supabase dashboard, hecho) | Bloqueaba Test 8-9. Codigo + config listos; falta retest E2E. PROD: replicar config. |
| ~~`[BUG-RESPOND-DUP-CLAIM]`~~ | **CERRADO 2026-06-10 (Fix durante verify Test 9, commit `7a35f69`):** `/api/respond` hacia `INSERT` plano (no upsert pese al comentario "upsert shape") => re-responder un item Likert creaba filas `item_response` duplicadas (user_id NULL). Al firmar, `claimAnonymousSession` seteaba user_id en todas a la vez => violaba el indice unico parcial `(user_id, item_id)` (mig 002) => callback caia a `/?error=signup`. Confirmado por runtime logs (err con claim+item_response+violates). **Fix:** `/api/respond` upsert onConflict(session_id,item_id) (last answer wins) + migracion 012 (dedup existentes keep-latest + UNIQUE INDEX `item_response_session_item_uniq`). Migracion aplicada a PROD via conexion directa (DELETE 10 dups => 51 filas limpias, indice creado). Habria roto signup para cualquier usuario real que cambie una respuesta. | Fundacion / Fase 1 verify | — | Resuelto. PROD: schema actualizado (012 aplicado manual via psql, no registrado en schema_migrations — idempotente). |
| ~~`[BUG-CALLBACK-NOT-IDEMPOTENT]`~~ | **CERRADO 2026-06-10 (fix idempotente + test):** el consent insert ahora trata el `23505` sobre `consent_user_product_active_idx` como exito (esa es la unica unique index de la tabla consent => un 23505 ahi solo puede ser "consent activo ya existe"). **Swallow scoped al call site del consent insert, NUNCA al catch externo** — un 23505 del claim sobre `item_response (user_id,item_id)` sigue cayendo a `/?error=signup` (no se corrompe el claim en silencio). Lockeado con `tests/unit/auth/callback-idempotent.test.ts` (3 casos: happy-path, idempotent-23505, scoping-guard-23502; RED->GREEN verificado). Typecheck limpio. **Pendiente confirm runtime:** retest con gervel33 (su consent activo residual ES el caso de repro real — el happy path con email fresco no ejercita el swallow) => el runtime log ya NO debe mostrar `consent insert ... violates`; ojo que gervel33 puede tirar en el paso de claim por estado residual (error distinto, no regresion). **Contexto original (ABIERTO 2026-06-10, verify Test 9, confirmado runtime logs 17:33):** el callback `/auth/callback` no es idempotente. El `INSERT` de consent (paso 6) re-corre en cada click del magic link; si el usuario ya tiene consent activo (de un intento parcial previo, como gervel33), viola el indice unico parcial `consent_user_product_active_idx` `(user_id, product_code) WHERE revoked_at IS NULL` => callback `catch` => `/?error=signup`. **Fix propuesto:** consent insert idempotente (upsert ON CONFLICT (user_id, product_code) WHERE revoked_at IS NULL DO NOTHING/UPDATE, o check-existing-active antes). Idealmente revisar todo el callback para idempotencia (user upsert ya es idempotente onConflict id; claim ya es no-op-safe; consent es el unico no-idempotente). **Data:** gervel33 tiene user+consent+sesion-sin-claimear residuales en PROD de intentos parciales; limpiar O testear con email nuevo sin estado previo. | Fundacion / Fase 1 verify | CC | Bloquea Test 9 para usuarios con estado previo. Real para double-click / retry. |
| `[GAP-MAGIC-LINK-EXPIRED-PREFETCH]` | `[REVISAR PRIORIDAD -> P2, auditoria P1 PM-23]` No se movio de seccion: la prioridad la decide German. **Por que no es P1:** **`STATUS.md` ya lo lista entre los "diferidos que no bloquean producto MVP"**, y su propia ficha dice "NO construir interstitial". Es causa externa (Outlook Safe Links) con workaround. **ABIERTO — evidencia de prefetch REFORZADA 2026-06-10 (retest post-fix-Bug1):** gervel33@**hotmail** (Outlook/Microsoft Safe Links, prefetcher agresivo de links) → "tu link ya expiro"; pruebasgvhv1@**gmail** con click inmediato → atraveso el callback completo (sesion claimeada OK en PROD). El diferencial Hotmail-falla vs Gmail-inmediato-pasa es el patron clasico de prefetch del scanner consumiendo el link de un solo uso. Confianza alta = prefetch (Outlook Safe Links es el culpable de manual). **NO construir interstitial hasta que el camino al reporte funcione (Bug 3/4).** **Contexto original (verify Test 8, email pruebasgvhv1):** `exchangeCodeForSession` falla con `error=expired` ("El link no es valido / ya se uso") en links frescos. Hipotesis: el scanner/prefetcher del cliente de email (Gmail) pre-visita el link de un solo uso y consume el codigo PKCE antes del click humano; o mismatch de cookie code_verifier (browser distinto). **Fix a investigar:** flujo `verifyOtp({type, token_hash})` en vez de `?code=` PKCE (mas robusto a prefetch), o interstitial "click para continuar" que dispare el exchange solo en interaccion humana, o config de Supabase. Conocido gotcha de magic links + email scanners. | Fundacion / Fase 1 verify + deploy | CC + German | Bloquea Test 8-9 en emails con prefetch. Workaround temporal: pegar el URL del link manual / click inmediato. |
| ~~`[GAP-REPORT-SCORING-NOT-TRIGGERED]`~~ | **CERRADO (auditoria P1 PM-23), verificado por codigo.** El scoring se dispara en dos puntos: `app/auth/callback/route.ts:322` (best-effort, para que el usuario no aterrice en un 404) y `lib/free/score-on-done.ts` (`scoreCompletedSessionIfNeeded`, con test propio `score-on-done.test.ts`). Documentado en `lib/scoring/score-session.ts:10`. `Enunciado original abajo, conservado:` **FIX APLICADO 2026-06-10 (codigo + tests, pending retest E2E):** extraido `lib/scoring/score-session.ts` (`scoreSession(admin, sessionId)`, pasos 4-14 de /api/score) reusado por el route + el callback; cableado **best-effort en el callback post-claim** (try/catch propio, log-no-throw — lockeado con 2 tests "scoring jamas rompe auth"). `/api/score` ahora delega en scoreSession (mismos error codes/status). Completeness por `count(distinct responses) >= item_count`. Pendiente: retest E2E con sesion fresca + completa => /reporte renderiza. **Descubierto 2026-06-10 (verify retest, /reporte 404):** el snapshot del reporte NUNCA se genera en el flujo real. `/api/score` solo persiste `report_snapshot` cuando `session.user_id != null` (post-claim), y **nadie lo invoca** (solo aparece en comentarios; grep = 0 callers). El callback redirige a `/reporte/{id}` sin generar el snapshot => `composeReport` tira `report_snapshot_not_found` => `notFound()` => 404. Confirmado en PROD: sesion 1426aad2 claimeada OK pero `snapshots=0`. **Fix (advisor):** extraer `scoreSession(admin, sessionId)` (pasos 4-14 de /api/score) reusable por el route + el callback; llamarlo **best-effort en el callback POST-claim** (try/catch propio, log-no-throw — el scoring NUNCA debe romper el auth) antes del redirect. Follow-up robusto: generate-if-missing en /reporte (Test 11 email linkea ahi + Gmail prefetchea) con unique index en report_snapshot(session_id) + swallow violation (patron Bug 1). | Fundacion / Fase 1 verify | CC | **Bloquea Tests 9-10 (la "magia" del reporte). El happy-path al reporte nunca estuvo cableado E2E.** |
| ~~`[BUG-PROGRESS-DRIFT-ON-REANSWER]`~~ | **CERRADO (auditoria P1 PM-23), verificado por codigo Y por test.** El gate de completitud cuenta `item_response` DISTINTOS, no el contador `progress` (`lib/scoring/score-session.ts:22`), y la navegacion mantiene las respuestas contiguas (`lib/free/runner-navigation.ts:7`). **Lo pinean 2 tests**: `tests/unit/scoring/score-session.test.ts` (59 respuestas contra 60 items -> `session_incomplete`) y `tests/unit/session/anonymous.test.ts`. `Enunciado original abajo, conservado:` **FIX APLICADO 2026-06-10 (codigo + test, pending retest E2E):** `advanceProgress` ahora setea progress = `count(distinct item_response)` (no progress+1 por submit) => idempotente bajo re-answer, getNextItemForSession no salta items. `scoreSession` gatea completitud por count distinto (no por el contador progress). Lockeado con test count-based en anonymous.test.ts + gate test en score-session.test.ts. Pendiente: retest E2E (sesion fresca, sin reusar cookie vieja). **Descubierto 2026-06-10 (verify retest):** `/api/respond` hace `upsert` item_response y luego `advanceProgress` (progress+1) **incondicional**. Re-responder un item (upsert UPDATE, sin fila nueva) igual incrementa progress => progress cuenta submits, no cobertura distinta. Confirmado en PROD: sesion 1426aad2 con progress=60 pero **59 respuestas distintas** (dim C=9). **En critical path:** el scoring suma C1..C10; con C10 ausente, `score()` rompe en la dim corta. **Fix (advisor):** juzgar completitud por `count(distinct item_response) >= item_count` (no el contador progress) + no incrementar progress en upsert-UPDATE (solo en insert nuevo). NO hacer `score()` tolerante a items faltantes (jamas puntuar data psicometrica incompleta). | Fundacion / Fase 1 verify | CC | Critical path para scoring E2E. Recurre en cualquier resume/back/double-tap. |
| ~~`[GAP-SESSION-COMPLETE-UNWIRED]`~~ | **CERRADO (auditoria P1 PM-23), verificado por codigo.** `lib/scoring/score-session.ts:559` — paso 13, ride-along: con el `report_snapshot` ya escrito, marca `status='completed'` + `completed_at`. No-fatal a proposito (el reporte ya esta persistido y el scoring usa el conteo de respuestas, no el status). `Enunciado original abajo, conservado:` **FIX APLICADO 2026-06-10 (ride-along con Bug 3, pending retest E2E):** `scoreSession` setea `status='completed'` + `completed_at=now()` (non-fatal) justo despues de persistir el report_snapshot. **Descubierto 2026-06-10 (verify retest):** nadie escribe `assessment_session.status='completed'` ni `completed_at` (grep = 0 writers). Las sesiones quedan `open`/`completed_at=NULL` para siempre. NO bloquea scoring (usa progress/count), pero es gap de data-hygiene + la transicion done-page. **Fix (advisor):** setear `status='completed'` + `completed_at` cuando se genera el snapshot (ride-along con Bug 3). | Fundacion / Fase 1 verify | CC | No bloquea; ride-along con scoring. |
| ~~`[GAP-DELETE-AUDIT-DIGEST-SEARCHPATH]`~~ | **CERRADO 2026-06-10 (Test 16 PASS): mig 013 aplicada a PROD (CREATE FUNCTION; has_013=t, search_path=''), borrado real de +r2 en la UI -> /me/delete/done, verificacion DB COMPL-07/09 (public.user=0, auth.users=0, 15 audit rows anonimizadas a NULL, 1 user_data_delete_completed actor_id NULL @ 22:38:13Z).** Test 16: borrar cuenta "no hacia nada" (modal abierto). **Causa raiz confirmada** (via RPC en ROLLBACK que corrio German): el trigger `audit_log_chain_hash()` (mig 004) llama `digest(...)` de pgcrypto SIN calificar schema; pgcrypto vive en `extensions` (no `public`). Los INSERT normales funcionan (search_path de sesion incluye extensions), pero el INSERT `user_data_delete_completed` dentro de `anonymize_user_audit` (mig 009, `SET search_path = public, pg_temp`, sin extensions) => `ERROR: function digest(text, unknown) does not exist` => el RPC `delete_user_account` aborta => usuario no se borra. El error caia a `{ok:false}` renderizado FUERA del modal (invisible tras el scrim => "no hace nada"). **Fix:** (1) `supabase/migrations/013_fix_audit_chain_hash_search_path.sql` recrea el trigger con `SET search_path = ''` + `extensions.digest` (hash identico, cadena intacta); (2) `DeleteAccountFlow.tsx` mueve el error DENTRO del modal. **Pendiente:** aplicar 013 a PROD (psql, German) + retest Test 16. Los tests del DELETE eran **stubs DB-gated** => nunca ejercitaron el RPC (por eso no se atrapo antes). | Fundacion / Fase 1 verify (COMPL-07 Ley 1581) | CC + German (aplicar 013) | Bloquea Test 16 (derecho de supresion). PROD necesita 013. |
| ~~`[GAP-PROD-DOMAIN-WWW-CANONICAL]`~~ | **CERRADO 2026-06-12 (browser smoke prod PASS — German confirmo flujo completo + magic link OK en `descubreme.co`).** El redirect apex->www **no rompe** el PKCE: verificado con `curl -sI` que `descubreme.co/auth/callback?code=...` responde **308** preservando path + query string completos (el `?code=` sobrevive el hop), y la cookie `code_verifier` es host-only en `www` (unico dominio que sirve la app, ya que el apex 308 siempre a www) => esta presente cuando `exchangeCodeForSession` corre en el callback de www. No se cambio nada: `NEXT_PUBLIC_APP_URL` queda en apex. **www-alignment (`NEXT_PUBLIC_APP_URL` -> www) descartado** como innecesario: solo eliminaria un hop inofensivo y forzaria redeploy (var `NEXT_PUBLIC_` inlined en build-time). Reversible si Phase 2+ lo amerita (P3 higiene opcional). | Fundacion / Fase 1 deploy prod | — | Resuelto. Apex funciona tal cual en prod. |
| `[GAP-PVQ21-ITEMS-ES-CO]` | `[REVISAR PRIORIDAD -> P3, auditoria P1 PM-23]` No se movio de seccion: la prioridad la decide German. **Por que no es P1:** la propia ficha dice **"YA NO BLOQUEA Phase 2"** (ADR-022: se activo el plan-B). Solo vive para un swap eventual, que es cambio de seeds. **YA NO BLOQUEA Phase 2** (2026-06-12, ADR-022): se activo plan-B **PVQ-RR** como instrumento de valores del Free. Sigue vivo solo para un eventual swap a PVQ-21 (cambio de seeds). Original: extraer/verificar los 21 items de PVQ-21 en es-CO + variante de genero. | Free / swap futuro | Cowork + Adaptacion | No inventar; NoDerivatives sobre adaptacion |
| `[GAP-PVQ21-ANCHORS-ES-CO]` | `[REVISAR PRIORIDAD -> P3, auditoria P1 PM-23]` No se movio de seccion: la prioridad la decide German. **Por que no es P1:** mismo caso que `[GAP-PVQ21-ITEMS-ES-CO]`: PVQ-21 no esta en el stack del Free. **YA NO BLOQUEA Phase 2** (ADR-022, plan-B PVQ-RR). Vivo solo para swap futuro. Original: verificar las 6 anclas verbales es-CO de PVQ-21 verbatim. | Free / swap futuro | Cowork | Analogo a RESPONSE_ANCHORS_es-CO |
| ~~`[GAP-TEASER-CROSS-TEMPLATES-ES-CO]`~~ | **CERRADO EN REPO (#83, 2026-07-29) Y EN PROD (seed aplicado 2026-07-30).** Cowork entrego las 12 reglas faltantes (4 frases simples + 5 cruces `MEDIO+MEDIO` + 3 `BAJO+BAJO`) -> **26 reglas, 12 de 12 celdas de frase simple** (4 instrumentos x 3 bandas). `Los dos pasos son distintos y hay que leerlos asi:` #83 desplego el **codigo**; **mergear no siembra datos**, asi que hasta el 2026-07-30 prod seguia con 14 reglas y el fix no surtia efecto. `Verificado en prod tras sembrar:` `INSERT` devolvio **exactamente los 12** `template_id` predichos; conteos **26 / 12 simples / 14 cruces**; las **26 huellas `md5(template_id‖conditions‖template_text‖requires_dimensions)` identicas a local, una por una** (cero deriva en las 14 preexistentes — importaba porque `NOT EXISTS` solo keyea `(tier, template_id, lang, version)` y `conditions` es justo lo que #83 arreglo). | Free / Fase 2 | Cowork (entregado) · CC (seed a prod) | Prediccion falsable escrita ANTES de mutar, patron de `[[reseed-prod-md5-por-item]]`. Idempotencia **medida**, no asumida: el mismo payload contra local (que ya tenia las 26) devolvio `INSERT 0 0` |
| ~~`[GAP-PVQ-RR-FREE-FORM]`~~ | **CERRADO 2026-06-12 (`/gsd-plan-phase 2` research + sign-off German, ADR-023): forma = TwIVI (20 items).** Validado, licencia 100% libre (sin bloqueador Phase 7), cabe en 5-7 min, rinde los 4 HOV, es la forma que el pack designa para Free. El full 57 rompe el budget ~2x; el reducido es forma no-validada con el mismo bloqueador de licencia. Motor form-agnostic (swap = seed). Consecuencia: se pierde el reuso opcional Free->Paid de valores (Phase 3 re-administra); nuevo GAP de contenido `[GAP-TWIVI-ITEMS-ANCHORS-ES-CO]`. | Free / Fase 2 | — | Resuelto. Ver ADR-023 Parte A. |
| ~~`[GAP-TWIVI-ITEMS-ANCHORS-ES-CO]`~~ | **CERRADO 2026-07-29 — CORRECCION DE LA PROPIA AUDITORIA PM-23, que primero lo dejo abierto por error.** `Verificado contra la DB, que es lo que habia que mirar desde el principio:` los **20 stems es-CO estan sembrados y son texto real** (largo 60-154 chars, ninguno placeholder) y **0 de 20 contienen genero** (`el/ella/ellos/ellas`) — o sea que la **variante NEUTRA esta viva**, que era justo lo que pedia el brief. **Las 6 anclas es-CO estan cableadas** en `lib/questionnaire/response-scales.ts:123-125` (escala de semejanza de 6 puntos, asimetrica, extraida del instrumento oficial Sandy et al. 2017). `Cadena de entrega, completa:` el pack (`implementation_packs/TwIVI_Implementation_Acquisition_Pack_v1.0.md:8`) declara **"Resuelve `[GAP-TWIVI-ITEMS-ANCHORS-ES-CO]` (lado contenido)"** y marca items y anclas como **RESUELTO**; Cowork entrego la variante neutra tras `estado/BRIEF_Cowork_TwIVI_NEUTRAL_STEMS_v1.0.md` (2026-07-01) y quedo en `estado/TwIVI_items_es-CO_SEED_NEUTRO_v1.1.sql`, **ya sembrada**. `Lo que NO cierra esta fila, y son otra cosa:` (1) el **piloto cognitivo es-CO** (ITC 2017: back-translation + panel + N=6-8) que el pack marca pendiente — el wording es *candidato*, no aprobado en firme; eso es **validacion, no redaccion**, y no es contenido que Cowork pueda simplemente escribir. (2) El **branching el/ella**, diferido por decision de German (2026-07-01) porque la app no captura genero declarado -> `[GAP-TWIVI-GENDER-SCHEMA]`. `>>> POR QUE ME EQUIVOQUE, que es lo que hay que no repetir:` la clasifique como abierta por **creerle a un comentario de un spec E2E** (`tests/e2e/free-full-flow.spec.ts:183`, *"TwIVI stems are placeholders"*) **en vez de consultar la tabla `item`**. Un comentario en el codigo **describe el momento en que se escribio**, no el estado de hoy — envejece igual que una fila de backlog. **Es exactamente el error que esta auditoria existia para eliminar, cometido dentro de ella.** `Deuda que deja:` ese comentario del spec quedo **obsoleto** y va a volver a engañar a quien lo lea; se nombra aca y no se toca (este PR es solo-docs). `Enunciado original abajo, conservado:` **NUEVO 2026-06-12 (plan Phase 2, ADR-023).** Stems es-CO de los 20 items TwIVI + anclas Likert verbatim + particion 10 valores basicos -> 4 HOV (Schwartz). Ningun pack los cubre (el pack §5 SI cubre los textos de interpretacion HOV es-CO, que se reusan). Determinista, trazable. **Bloquea el deploy del test de valores funcional + su E2E, NO la maquinaria form-agnostic del plan** (plan 02-10 siembra con stems placeholder claramente marcados, deploy-gated). | Free / Fase 2 | Cowork | Surge de elegir TwIVI; analogo a RESPONSE_ANCHORS + narrativas. Owner Cowork. **VIVO EN PROD 2026-06-29** (smoke German, screenshot 3: "PLACEHOLDER — Self-Direction item 1... [GAP-TWIVI-ITEMS-ANCHORS-ES-CO] Punto N (placeholder)" renderizado al usuario en el test de valores). Sube a bloqueador de lanzamiento real. **2026-07-01: TwIVI CONFIRMADO como instrumento (German).** Resuelve contradicción PRD §8 L246 ("PVQ-40" plan-B abierto — desactualizado) vs `dossiers/24_PVQ-40_Consolidado.md:21` (recomienda NO PVQ-40, elige TwIVI) + `PVQ-21 pack:30` ("Plan-B TwIVI"); licencia TwIVI = uso libre explícito (Sandy et al. 2017), la más limpia del stack. **Acción doc: corregir PRD L246 (PVQ-40→TwIVI)** — owner Cowork/German. Brief de pack es-CO escrito: `scratchpad/BRIEF_Cowork_TwIVI_es-CO_v0.1.md` (20 ítems extraídos de fuente + anclas + narrativa nivel-HOV + ficha, reusando `PVQ-21 pack §2/§5/§7` + `dossier 24 §1/§4/§7`; sólo los 20 portraits son propios de TwIVI). Producir el pack cierra este gap + el voseo de este test. No existe dossier/pack TwIVI (sólo scoring `twivi-mrat-fixture`). **2026-07-01 PM-3 (CC — pack integrado):** pack v1.0 + seed M/F entregados. **Anclas es-CO: HECHO** (working tree `response-scales.ts`). **Decisión ADR-030 addendum: variante NEUTRA ahora, branching diferido** (`[GAP-TWIVI-GENDER-SCHEMA]`). Los 20 stems neutros = Cowork (brief `estado/BRIEF_Cowork_TwIVI_NEUTRAL_STEMS_v1.0.md` + scaffold `estado/TwIVI_items_NEUTRAL_es-CO_SEED_SCAFFOLD_v1.0.sql`, seq 4/6/16 pre-llenos). **2026-07-02: CONTENIDO CERRADO + STEMS EN PROD.** Cowork entregó `estado/TwIVI_items_es-CO_SEED_NEUTRO_v1.1.sql` (verificado adversarialmente: orden oficial, 0 marcas de género, fiel al M/F). CC lo aplicó al seed committeado `db/seeds/instruments/TwIVI/items.sql` (working tree) + **reseed a prod vía UPDATE in-place** (elegido ante 60 responses de prueba + FK NO ACTION; verificado 20/20 neutro, orden oficial, 60 responses preservados). Fixture MRAT 6/6 + anclas 7/7 + lint 13/13 verdes. **FALTA SOLO (consistency): deploy del código de anclas** (`response-scales.ts`, working tree) — prod muestra stems reales pero anclas placeholder hasta el deploy. Owner: CC (deploy) + German (cadencia/OK). |
| `[GAP-PVQRR-SKIP-FEATURE]` | `[REVISAR PRIORIDAD -> P3, auditoria P1 PM-23]` No se movio de seccion: la prioridad la decide German. **Por que no es P1:** la propia ficha dice **DIFERIDO** y **"moot bajo TwIVI"**; es salvaguarda secundaria y el instrumento conserva footer + disclaimer. **NUEVO 2026-06-12 (plan Phase 2, ADR-023 Parte B) — DIFERIDO.** Detector NFR-28 por omision de items sensibles (del pack PVQ-RR) requiere una feature skip-item no construida. Diferido: es salvaguarda secundaria (pack §7.2) y sus 15 items sensibles son PVQ-RR-especificos -> moot bajo TwIVI. El instrumento de valores conserva footer de contencion + disclaimer post-test (cumple §8). Reevaluar si un swap futuro vuelve a PVQ-RR completo. | Free / futuro (swap) | CC | No bloquea Phase 2. Ver ADR-023 Parte B. |
| ~~`[GAP-RETURNING-USER-RESIGNUP-AGE]`~~ | **DUPLICADO — ver la fila autoritativa en P0 (cerrada, PR #23 mergeado).** Esta fila predecia mal el cierre y se conserva como historico del diagnostico. `[historico]` ~~**Se cierra en Phase 2** (discuss D-F3.1): el flujo Free detecta instrumentos ya completados y enruta al pendiente.~~ **FALSO — corregido 2026-07-27.** Phase 2 construyo el RUTEO del usuario que vuelve, pero nunca su LOGIN: el callback lo rechazaba antes de llegar a ese ruteo. Ver la fila P0. | Free / Fase 2 | CC | El ruteo existia y era correcto; nadie llegaba a el |
| `[GAP-IKIGAI9-ITEMS-ES-CO]` | `[REVISAR PRIORIDAD -> P2, auditoria P1 PM-23]` No se movio de seccion: la prioridad la decide German. **Por que no es P1:** es de **Fase 5**, e **Ikigai Premium esta diferido a Q1 2027** por decision macro. P1 significa "prerequisito de una fase CERCANA". Adaptacion formal es-CO de los 9 items de Ikigai-9 (ITC 2017: traduccion doble, retrotraduccion, panel, piloto cognitivo) + permiso de autores | Ikigai / Fase 5 | Adaptacion + German | No existe validacion es; items EN publicos (Fido 2020) |
| `[GAP-IKIGAI9-ANCHORS-ES-CO]` | `[REVISAR PRIORIDAD -> P2, auditoria P1 PM-23]` No se movio de seccion: la prioridad la decide German. **Por que no es P1:** mismo caso: Fase 5, diferida a Q1 2027. Anclas es-CO (escala 5 puntos) de Ikigai-9 | Ikigai / Fase 5 | Cowork + Adaptacion | Verbatim de fuente |
| ~~`[GAP-AUTH-HOOK-API]`~~ | **CERRADO 2026-06-05 (signature verificada RESEARCH §Gate 1 docs verbatim) + APLICADO 2026-06-07 (Plan 01-05 Task 2):** `supabase/migrations/005_jwt_auth_hook.sql` con `org_ids := '{}'` Phase 1 B2C-only + Phase 4 extension documentada como comment + GRANT execute supabase_auth_admin + REVOKE authenticated/anon/public; `supabase/config.toml` `[auth.hook.custom_access_token]` enabled=true. **Hard Gate 1 RESOLVED HIGH.** | Fundacion / Fase 1 | — | Resuelto |
| ~~`[GAP-RLS-JSONB]`~~ | **CERRADO 2026-06-05 (plan-phase 1):** operadores jsonb con `(select auth.jwt())` wrapping verificados en RESEARCH.md §Gate 2 (docs + benchmark 99.94% improvement). Aplicado en Plan 01-04. | Fundacion / Fase 1 | — | Resuelto |
| ~~`[GAP-SUPABASE-REGION]`~~ | **CERRADO 2026-06-05 (discuss-phase 1):** us-east-1 lockeada en `01-CONTEXT.md` D1.1. CCM Supabase queda para Phase 7. | Fundacion / Fase 1 | — | Resuelto |
| ~~`[GAP-TAILWIND-V4-COMPAT]`~~ | **CERRADO para el objetivo de Fase 1 (auditoria P1 PM-23), verificado por efecto.** `package.json` declara **`tailwindcss ^4.3.0`** y la compat esta demostrada por el hecho de que el **build de produccion pasa** y los **33 E2E renderizan** (incluidos los specs visuales de barras y circumplejo). `Lo que NO cierra:` la consolidacion de Fase 6 (tokens/design system con `ui-ux-pro-max-skill`), que es otro alcance y vive en `[GAP-UX-FLOW-REDESIGN]` + Fase 6. `Enunciado original abajo, conservado:` Verificar compat Tailwind CSS 4.x + `ui-ux-pro-max-skill` (config CSS-first vs `tailwind.config.js`) | UX transversal / Fase 1 (resolver) + Fase 6 (consolidar) | CC | Si incompat, quedarse en v3 |
| ~~`[GAP-RIASEC-NARRATIVES-ES-CO]`~~ | **CERRADO 2026-06-08 (Cowork delivery v1.0 commit `ee62bf5`):** `db/seeds/narrative-templates/RIASEC/seed.sql` con 132 plantillas (120 top_3_phrase 6P3 + 6 dimensional_high + 6 dimensional_low), voseo verbal + tu/tus posesivos, motor compositivo (dominante abre, secundaria matiza, terciaria cierra). Aplicado a Supabase local (INSERT 0 132 + slot count 120/6/6 verificado runtime). Lint prohibited-phrases GREEN. Idempotencia via DELETE+INSERT; ver `[GAP-RIASEC-NARRATIVES-UNIQUE-INDEX]` para migration a ON CONFLICT puro. | Fundacion / Fase 1 | — | Resuelto |
| ~~`[GAP-ONET-OCCUPATIONS-LATAM]`~~ | **CERRADO 2026-06-08 (Cowork delivery v1.0 commit `ab8f606`):** `db/seeds/occupations/LATAM/seed.sql` con 96 ocupaciones LATAM (16/dim RIASEC, JZ split 2/9/21/48/16). Fuente O*NET OnLine explore-by-interest 2026-06-08 (high-point RIASEC verbatim, no inventado). Schema es 4-cols (sin descripcion) — Capa 3 muestra solo nombre + dim. Aplicado a Supabase local (INSERT 0 96 verificado runtime). Lint GREEN. Decisiones de crosswalk documentadas en ADR-019. | Fundacion / Fase 1 | — | Resuelto |
| ~~`[GAP-AWS-KMS-SETUP]`~~ | **CERRADO 2026-06-06 (Plan 01-01 Task 1):** German provisiono cuenta AWS dedicada `descubreme-prod` (account ID ending ...2030) + Identity Provider OIDC `oidc.vercel.com/germanvelezhs-projects` + KMS key `alias/descubreme-prod-pii-kek-v1` (symmetric, single-region, rotacion anual) + IAM role `descubreme-prod-encrypt` con trust policy (StringEquals aud + StringLike sub para production+preview) + permission policy inline (4 ops KMS) + key policy del KMS lista el role como key user. `AWS_ROLE_ARN` + `AWS_REGION=us-east-1` guardados en `~/secrets/descubreme/.env.secrets`. | Fundacion / Fase 1 | — | Resuelto |
| ~~`[GAP-CONSENT-TEXT-V0.1]`~~ | **CERRADO 2026-06-08 (Cowork review + integration commit `8c61911`):** v1.0.0 finalizado con todos los fixes plegados (no semver bump — zero legacy users): §1 domicilio Bogota; §2 anti-determinismo "no predecimos tu exito"; §3 nombre opcional + audit IP/UA + voluntariedad sensible Art. 5; §4 Sentry agregado como 6to subprocesador + wording cifrado-vs-control-acceso corregido; §5 backup retention; §6 plazos 10d consultas/15d reclamos + derecho "prueba de autorizacion" Art. 8 lit. b; §8 alineado con versions.ts. Sentry sincronizado en `MC_CONSENT_SUBPROCESSORS_LIST` (consent.ts). Tildes es-CO aplicadas. Hash regenera auto. Revision legal externa formal sigue para Phase 7 (voluntariedad checkbox sensible, domicilio fisico, plazo purga respaldos). | Fundacion / Fase 1 | — | Resuelto |
| ~~`[BUG-PII-STORAGE-PLAN-07]`~~ | **CERRADO 2026-06-07 (Plan 01-12 Task 1):** `supabase/migrations/011_pii_storage_full_envelope.sql` (commit `9d4cbd7`) consolido columnas a `user.{name,date_of_birth}_encrypted jsonb` con shape `{ciphertext, dek_ciphertext, iv, tag, kid, v}` per ADR-009 §9.4 (Opcion B jsonb por flexibilidad rotation key version). Drizzle schema + callsites en `/api/me/data` GET + `/(auth)/callback` UPSERT + 2 sibling Server Actions adaptados (commit `8f1f109`). Safe wipe+repopulate (0 users prod aun, documented en migration comment). | Fundacion / Fase 1 | — | Resuelto |
| ~~`[GAP-DELETE-ATOMIC-TX]`~~ | **CERRADO 2026-06-07 (Plan 01-12 Task 1):** `supabase/migrations/010_delete_user_account.sql` (commit `6cf90c3`) emite `public.delete_user_account(uuid)` SECURITY DEFINER que ejecuta `anonymize_user_audit + DELETE public.user + DELETE auth.users` en UNA transaction Postgres atomica per ADR-009 §9.3 (Opcion B SECURITY DEFINER atomic). Caller verification dual (role + JWT sub). Route handler `/api/me/data` DELETE switcheado a `.rpc('delete_user_account')` (commit `8f1f109`). `tests/lint/audit-modification-callers.test.ts` (commit `7f3e61a`) enforces que solo `/api/me/data` invoca la funcion (defense-in-depth). | Fundacion / Fase 1 | — | Resuelto |
| ~~`[GAP-E2E-FULL-FLOW-ANONYMOUS]`~~ | **CERRADO (auditoria P1 PM-23), verificado por spec.** `tests/e2e/free-full-flow.spec.ts:75` — `test.describe("Free full flow — anonymous head ([GAP-E2E-FULL-FLOW-ANONYMOUS])")`. El spec declara en su cabecera que **absorbe** este gap diferido de Fase 1. `Enunciado original abajo, conservado:` `tests/e2e/full-flow-onet-anonymous.spec.ts` 2 tests fallando en 3 browsers (mobile-chromium + desktop-chromium + mobile-webkit): (a) `anonymous user can land, go to BYS, start the test, and answer 3 items` (timeout en radio.check()), (b) `mobile viewport renders anchors vertically (one per row, not 5-col grid)` (layout assertion). Pre-existente Plan 01-06 UI — NO regresion de Task 1 (que no toco UI components). Descubierto Plan 01-12 Task 2 sub-flow (primera vez Phase 1 corre E2E con dev server real + seeds aplicados). Hipotesis: radio button state management bug en `ItemForm.tsx` o anchor layout responsive issue. | Fundacion / Fase 2 o gap-closure 01-13 | CC | Phase 1 ship NO bloqueado (backend GREEN); bloquea verify-strict-full. |
| ~~`[GAP-E2E-PAUSE-RESUME]`~~ | **CERRADO (auditoria P1 PM-23), verificado por spec.** `tests/e2e/free-pause-resume.spec.ts:58` — `test.describe("Free pause/resume — anonymous ([GAP-E2E-PAUSE-RESUME])")`, mas el tramo cross-instrument autenticado (`:195`). Corre en CI dentro de los 33. `Enunciado original abajo, conservado:` `tests/e2e/pause-resume.spec.ts:46` 1 test fallando en 3 browsers: `user answers 3 items, closes browser, reopens within 7d, lands on resume screen at item 4` — falla en `radio.check()` linea 63 con timeout. Pre-existente Plan 01-06 (pause/resume cookie nanoid(30) flow). Misma raiz hipotetica que `[GAP-E2E-FULL-FLOW-ANONYMOUS]` (radio button interaction). Descubierto Plan 01-12 Task 2 sub-flow. | Fundacion / Fase 2 o gap-closure 01-13 | CC | Phase 1 ship NO bloqueado (mecanica resume cubierta por integration tests + cookie semantics); bloquea verify-strict-full UI. |
| ~~`[GAP-E2E-SIGNUP-AGE-BLOCK]`~~ | **CERRADO (auditoria P1 PM-23), verificado por spec.** `tests/e2e/free-full-flow.spec.ts:148` — `test.describe("Free full flow — signup age gate ([GAP-E2E-SIGNUP-AGE-BLOCK])")`. Ademas `signup-consent.spec.ts:79` afirma el gate 18+ en la capa UI (`max` + helper). `Enunciado original abajo, conservado:` `tests/e2e/signup-consent.spec.ts:69` 1 test fallando en 3 browsers: `server action returns MC_SIGNUP_AGE_BLOCK for DOB <18` — timeout en `getByLabel(/tu email/i).fill(...)` post-fix isAtLeast18 split. Pre-existente Plan 01-07 UI rendering bug — el form se monta pero algun elemento se cae en estados especificos. NO es el bug de Build Error que arregle (ya GREEN); este es UI state diferente. Descubierto Plan 01-12 Task 2 sub-flow. | Fundacion / Fase 2 o gap-closure 01-13 | CC | Phase 1 ship NO bloqueado (`isAtLeast18` cubierto por unit tests + integration); bloquea verify-strict-full UI. |
| ~~`[GAP-W5W6-ORPHANED-FREE-FLOW]`~~ | **[VERIFICADO + CERRADO 2026-07-01 (German, corrida limpia en prod). Plan 03 confirmado E2E:** 4 tests con email nuevo → post-PERMA rutea a `/reporte/{onet}?cierre=free` → nivel OBLIGATORIO (sin skip) → ocupaciones Job Zone + CTA, SIN hexágono/bandas/ficha. W5/W6 ahora alcanzables para el Free. Código: PR #5 (merged prod). El link que confundía el smoke inicial se quitó en PR #6 (`[GAP-FREE-TRANSITION-DEADEND]`).]** `[historico]` **[2026-07-01 — MERGED a prod (PR #5); recut pendiente de corrida limpia.]** **[2026-06-29 PM — CÓDIGO COMPLETO, branch `feat/phase-02.1-ux-remediation`, plan 02.1-03 (Opción B): `done/page.tsx` allComplete → `/reporte/{onet}?cierre=free` (nivel obligatorio → reveal ocupacional); pendiente deploy+smoke.]** **NUEVO 2026-06-29 (smoke Phase 2.1, German). VERIFICADO.** W5 (`LevelCapture`: nivel educacion+experiencia) y W6 (reveal ocupacional por Job Zone) viven SOLO en `app/(b2c)/reporte/[sessionId]/` (LevelCapture.tsx + actions.ts `target_job_zone`), pero el funnel Free invertido termina en `/perfil-integrado` (teaser), que NO renderiza ninguno → features recien construidas (`bbe073d`) INALCANZABLES en el flujo Free real. Por eso "las preguntas de nivel nunca aparecieron". Confirmado por grep (LevelCapture/target_job_zone solo en /reporte; perfil-integrado no los importa). **Fix (decision producto + CC):** decidir la superficie correcta de W5/W6 en el funnel invertido (captura de nivel antes del teaser; reveal Job Zone en el teaser o en un reporte O*NET dedicado) y re-rutear. | Free / Fase 2.1 (post-deploy) | CC + producto | Bloquea el valor de W5/W6 (en prod pero muertos para Free). |
| ~~`[GAP-MICROCOPY-VOSEO-TO-ES-CO]`~~ | **CERRADO END-TO-END. Reverificado 2026-07-29:** `grep -rEc "necesitás\|tenés\|querés\|podés\|sentís\|elegís"` sobre `lib/i18n/microcopy`, `app` y `components` da **cero**, y el reseed de las narrativas de prod quedó verificado por `md5(prod)==md5(seed)` (C1, 2026-07-24). `[historico]` **CÓDIGO CERRADO 2026-07-07 (OLA 0, PR #8 `9530416`, desplegado):** voseo→es-CO tuteo en 18 microcopy + hero landing + seeds RIASEC/BFI + metadata TwIVI; grep repo-wide de voseo limpio en código de producto. CC lo ejecutó sobre el copy ya firmado (MICROCOPY §2 "aplicación total"), no Cowork. **FALTA (deploy, NO bloquea la app): reseed de prod de `narrative_template` RIASEC/BFI** (DELETE-first idempotente, con OK German + chequeo FK) — las filas vivas del reporte siguen en voseo hasta entonces; el microcopy `.ts` ya rinde tuteo con el deploy. **NUEVO 2026-06-29 (smoke Phase 2.1, German). VERIFICADO (screenshots).** Todo el copy user-facing usa **voseo rioplatense** ("quien sos", "leelo", "te sentis", "te adaptas", "Avisame", "Sigamos con el siguiente paso") — REVIERTE la decision de `[GAP-MICROCOPY-FASE1]` (que entrego "voseo + tu/tus") y **viola CLAUDE.md §13** (español Colombia, tuteo cordial neutro). German: cambiar a es-CO tuteo neutro. Alcance: 14 archivos `lib/i18n/microcopy/es-CO/*.ts` + stems de items. **Owner Cowork** (re-redaccion); CC solo wira. Mantener rigor + lint prohibited-phrases. | Free / Fase 2.1 (contenido) | Cowork | Reversion explicita de decision previa. |
| ~~`[GAP-FREE-NO-RESULTS-VISIBILITY]`~~ | **CERRADO (auditoria P1 PM-23), verificado por codigo Y por test.** La maquinaria (`lib/free/last-scored-session.ts`) **se consume en la UI**: `app/(b2c)/test/[code]/done/page.tsx:225` resuelve la ultima sesion scoreada y compone el mini-resultado (frase reveladora del composer, recap §4.3, footer de contencion) para la pantalla de transicion. Test: `tests/integration/transition-screen-mini-result.test.tsx`. `Ojo:` la umbrella `[GAP-UX-FLOW-REDESIGN]` sigue abierta — cerrar este sub-item no cierra el rediseño. `Enunciado original abajo, conservado:` **[2026-06-29 PM — MAQUINARIA PARCIAL, branch `feat/phase-02.1-ux-remediation`: 02.1-03 da cierre con resultados (nivel→ocupaciones) + 02.1-04 da mini-result por test; la decisión de producto "qué ve el Free" SIGUE ABIERTA. Pendiente deploy+smoke.]** **NUEVO 2026-06-29 (smoke Phase 2.1, German).** El usuario completa los 4 tests y NO ve ningun resultado propio: el "final" es el teaser `/perfil-integrado` ("Un primer espejo de quien sos"), no un reporte con lo que salio en cada test. German: "no esta bien que no hay reporte final / ver lo que salio". **Decision de producto:** definir que ve el Free — resultado por test, un reporte Free agregado antes del upsell, o solo teaser (status quo). Tension con el modelo Free=teaser / Paid=perfil profundo (PRD §1). | Free / Fase 2.1 (diseño producto) | Cowork + German + CC | Decision de producto, no bug. |
| ~~`[GAP-W6-HOOKS-1]`~~ | **CERRADO (auditoria P1 PM-23), misma evidencia que `[GAP-FREE-NO-RESULTS-VISIBILITY]`**: `resolveLastScoredSessionId` se consume en `app/(b2c)/test/[code]/done/page.tsx:225` y alimenta el mini-resultado de la transicion, con test de integracion. `Ojo:` sigue siendo sub-item de `[GAP-UX-FLOW-REDESIGN]`, que NO se cierra con esto. `Enunciado original abajo, conservado:` **[2026-06-29 PM — MAQUINARIA COMPLETA, branch `feat/phase-02.1-ux-remediation`, plan 02.1-04: `TransitionScreen` muestra mini-result del test recién cerrado (visual por `visualType` + frase reveladora + link al reporte) en el branch `nextCode`. FALTA copy del hook por-test (Cowork) + deploy+smoke.]** **ESCALADO a P1 2026-06-29 (smoke Phase 2.1, German). Era P2 (header 06-26).** La transicion test→test es solo un boton pelado ("Sigamos con el siguiente paso / Empezar / Continuar"): no muestra el resultado del test recien hecho ni anticipa el proximo. German: "el flujo UX es terrible; mostrar resultados del test que acabas de salir y despues a donde vas a entrar". Cablear los 4 hooks §1 + recap-de-resultado + preview-del-proximo en el boundary de transicion (`done/page.tsx` / `TransitionScreen`). | Free / Fase 2.1 | Cowork (copy/spec) + CC | Vivo en prod; UX core. |
| ~~`[GAP-FREE-TRANSITION-DEADEND]`~~ | **CERRADO (auditoria P1 PM-23) — el fix ERA quitar la plomeria, y esta quitada.** La decision B del owner (2026-07-01) fue eliminar el link "Ver reporte completo" y todo `reportHref`. `Verificado por ausencia, que es la afirmacion correcta para este fix:` **0 ocurrencias de `reportHref`** en `app/`, `lib/` y `components/`. `Enunciado original abajo, conservado:` **NUEVO + FIX 2026-07-01 (smoke Phase 2.1, German).** El link "Ver reporte completo" del mini-result (plan 02.1-04) apuntaba al reporte full (`/reporte/{id}` sin `?cierre=free`) = página terminal sin retorno → varaba al usuario mid-funnel (tuvo que dar back del navegador) + filtraba profundidad Paid (hexágono+bandas+ficha) que el recut del cierre oculta, adelantando el reveal. **Decisión B (owner):** quitar el link + toda la plomería `reportHref` (TransitionScreen tipo, `done/page` feed, microcopy `MC_TRANSITION_REPORT_LINK`). Reporte full sigue alcanzable en el cierre (recut) + "Mis datos"; captura de nivel intacta (vive en el cierre, no en el link). Test `tests/integration/transition-screen-mini-result.test.tsx` lockea el contrato. **PR #6 `fix/free-transition-remove-report-link`, pendiente merge de German.** | Free / Fase 2.1 | CC | tsc limpio, 355 pass (5 reds = stale `[GAP-PHASE21-CALLBACK-TEST-STALE]`). |
| `[GAP-UX-FLOW-REDESIGN]` | **NUEVO 2026-06-29 (smoke Phase 2.1, German). Umbrella.** El flujo UX/UI completo del Free necesita evaluacion + rediseño (German: "el UX es pero que malo", "el UI es bien bien feo"). Engloba la intro por test (`[GAP-FREE-TEST-INTRO-COPY]`), la jerarquia visual, la transicion (`[GAP-W6-HOOKS-1]`) y la falta de resultados (`[GAP-FREE-NO-RESULTS-VISIBILITY]`). Requiere pasada de UX research + UI (Cowork spec + CC + diseño); sub-items con flag propio. | Free / Fase 2.1 | German + Cowork + CC | Paraguas de los sub-items de arriba. |
| ~~`[GAP-PERMA-CONTENTION-GUIDED-FLOW]`~~ | **CERRADO (auditoria P1 PM-23), verificado por codigo Y por test.** Wired: `app/(b2c)/test/[code]/done/page.tsx:46` importa y usa `resolveGuidedContention`; la decision vive en `lib/free/contention-gate.ts` (`shouldSurfaceContention`) con unit test escrito ANTES del wiring (`contention-gate.test.ts`) y un test de integracion del flujo (`tests/integration/guided-contention-flow.test.ts`). Cerrado ademas como condicion #3 de ADR-039 en **#69**. `Enunciado original abajo, conservado:` **RE-ALCANZADO 2026-07-29 — sigue P1, pero por otra cosa que la que decia. El FEATURE esta CERRADO y en prod** (`PR #18` mergeado, desplegado, deploy-smoke de ADR-033 primario + control PASAN, `estado/SMOKE_ADR033_PERMA_CARE_RESULTADOS_v1.0.md`). La fila decia "codigo-completo en working tree, **SIN commit**", lo que hacia creer que el interstitial **no existia en prod**, y existe desde el 2026-07-23. **CERRADO 2026-07-29 en #69** (`tests/integration/guided-contention-flow.test.ts`): era la condicion sistemica #3 de ADR-039, el ultimo item que mantenia ese ADR sin cerrar — **ADR-039 queda 9/9 criterios + 3/3 condiciones.** `[historico]` **LO QUE QUEDA ABIERTO ES EL TEST, y es la condicion sistemica #3 de ADR-039 — el unico item que mantiene ese ADR sin cerrar (queda en 9/9 criterios + 2/3 condiciones).** `Medido 2026-07-29, hay tres capas de cobertura y ninguna es la que se pide:` (1) el gate puro `shouldSurfaceContention`, con unit tests; (2) `tests/integration/perma-care-screen.test.tsx`, que **renderiza el componente con props** (`render(<PermaCareScreen …/>)`); (3) `tests/e2e/free-critical-gates.spec.ts:169`, que afirma **`ContentionBanner`**. ADR-039 pide explicitamente **un test DEL FLUJO GUIADO, no del banner aislado**: que un perfil de bienestar bajo, al completar los 4 tests, **aterrice en la pantalla de cuidado en el choke point `allComplete`** antes del cierre. **Eso no lo afirma nadie.** `Por que importa y no es burocracia:` es NFR-28, seguridad — la superficie existe, pero nada impide que un refactor del routing guiado la saltee sin que CI se entere. `[historico]` **[EN CURSO 2026-07-23: fix Opción A código-completo en working tree, SIN commit — ADR-033.** Interstitial safety-only en el choke point `allComplete` de `done/page.tsx`: gate puro `lib/free/contention-gate.ts` (`shouldSurfaceContention`, 7 unit tests) + orquestador `lib/free/guided-contention.ts` (reusa el read del branch `nextCode`, no lo duplica) + `PermaCareScreen.tsx` (reusa `ContentionBanner` intacto) + copy Cowork firmado en `nfr28.ts` (`MC_PERMA_CARE_SCREEN_*`, body sin-repetir). NO toca la composición del cierre (ADR-031) → separado de A2. **Pendiente: deploy-smoke** (perfil bienestar bajo → ve la pantalla antes del cierre; perfil normal → mismo destino de cierre, sin pantalla nueva).]** **NUEVO 2026-07-23 (smoke A1). P1 seguridad.** La contención NFR-28 de PERMA nunca se surfacea en el flujo guiado del Free. El mecanismo funciona (banner prominente `aside[role=complementary]` con las 6 líneas CO en `/reporte/{permaSessionId}`, verificado en vivo), pero el recorrido guiado no pasa por ahí: PERMA es el 4º → sin transición → close = sesión de O*NET (no sensible) → `/perfil-integrado` (no menciona bienestar). Snapshot de prod: `distress severity:moderate showContention:true` y el usuario no ve nada; alcanzable solo vía `/me/data` (donde los 4 reportes se llaman igual, ver `[GAP-REPORT-INTERESES-MISLABEL]`). **Defecto con arreglo propio, SEPARADO de A2 `[GAP-PERMA-MINIRESULT-SURFACE]`** (resolver A2 con una superficie de PERMA lo cerraría, pero no depende de A2). Cerrar aunque A2 se difiera a OLA 3. | Free / seguridad (Ola 2-3) | German (prioriza) + CC | La señal de cuidado no llega en el Free. Ref: `estado/SMOKE_A1_RESULTADOS_v1.0.md`. |
| ~~`[GAP-PERMA-BARS-VISUAL-PASS]`~~ | **CERRADO. Verificado en codigo 2026-07-29, parte por parte** — el epic tenia tres, y ninguna sigue viva: **(a) el bug del `max`** lo cerro **PR #19 / ADR-034**: `DEFAULT_MAX` **ya no existe en el repo** (grep repo-wide = 0) y el ancho lo maneja `BAND_RATIO` (`BarsWithBands.tsx:46`, aplicado en `:84`); deploy-smoke 3/3 el 2026-07-24. **(b) el acoplamiento `invertBand`-`value` NO se "arreglo": quedo DISUELTO**, y esa es la parte que vale — el ancho se calcula `BAND_RATIO[d.band] * 100`, o sea **desde la banda y no desde el `value`**, y `projectBarsDimensions` ya voltea la banda antes (`visual-dimensions.ts:74`). La divergencia banda-correcta-con-largo-equivocado que describia la fila **es irrepresentable** en el diseno nuevo. **(c) el restructure de layout** (bloques de bienestar vs senales adicionales, `hap` atenuado, Salud como autovaloracion) **se separo a `[GAP-PERMA-BARS-SENSITIVE-GROUPING]` P3** al firmar ADR-034 — es decision de producto/Cowork, no de este epic. `Por que estuvo P1 fantasma:` cerro por partes en PRs distintos y **ninguno reescribio esta fila**. `[historico]` **NUEVO 2026-07-23 (smoke A1 + fix #17). P1 epic.** El visual de barras de PERMA necesita un pase; tres cosas juntas: (a) **bug del `max`** — el assembler nunca setea `max` en `ReportVisualDimension` → `BarsWithBands` cae a `DEFAULT_MAX=5` y clampea a 1 → las 5 barras de BFI (12/18/19/30/22) y las de PERMA salen al 100%, la longitud no comunica nada; (b) **acoplamiento invertBand-value** — `projectBarsDimensions` voltea la banda (`flipBand`) pero no el `value` (latente hoy porque (a) lo enmascara; al arreglar (a), la barra de NEG queda banda-correcta con largo-equivocado); (c) **restructure de layout** (propuesta Cowork 2026-07-23): separar "Bloques de bienestar" (P-E-R-M-A) de "Señales adicionales" (H/hap/N/Lon), `hap` como marcador atenuado (ítem único, confiabilidad baja), `Salud` marcada como autovaloración (no dato médico). Rigor + ética. | Free / reporte (secuenciar OLA 3) | German (decide) + CC | Los labels de barra ya los cerró #17; esto es el visual. Ref: ADR-032, SMOKE_A1. |

---

## P2 — Necesario, no inminente

| Flag | Pendiente | Producto/Fase | Owner | Nota |
|---|---|---|---|---|
| ~~`[GAP-COMPUTED-SCORE-BAND-NORMALIZED-NUNCA-ESCRITAS]`~~ | **CERRADO PARCIAL 2026-07-29 (#79) — se parte en las dos filas de abajo.** `Lo que se cerro:` **`band` ya se escribe** para BFI-2-S, ONET-IP-SF y PERMA-Profiler = **122 de las 197 filas**. `La causa no era la que decia el enunciado:` no era "falta agregar la columna al insert" sino un **ORDEN DE DEPENDENCIA** — el INSERT vivia **dentro** del loop del paso 10 (una fila por `scoring_rule`), y `band` sale de `computeIpsativeBands`, que es una **z intra-perfil sobre el vector completo**: no existe hasta que TODAS las reglas se puntuaron (paso 11). Un valor que depende del conjunto entero no se puede escribir dentro del loop que todavia lo esta construyendo. **El fix difiere la ESCRITURA, no el calculo** (paso 11a), conservando los INSERT por fila con su catch por fila (ADR-042/#75). **Sin migracion**: `band` ya era `text` nullable desde `002_user_data.sql:93`. `Y una correccion factual a esta misma ficha:` decia que el riesgo concreto era *"la via agregada B2B … leeria `band` y recibiria `null`"* — **esa via NO existe**: `006_aggregate_view_placeholder.sql` solo agrega la FK `organization_id`, ninguna vista. Verificado por grep: el **unico** lector de `computed_score` en la app es el export ARCO (`app/api/me/data/route.ts:139`), que vuelca la fila entera; el teaser lee `report_snapshot.html_payload.bands_by_dim` (`teaser-data.ts:105-107,126`), **nunca** esta tabla. | Transversal / scoring | CC — **hecho** | ADR-043. Ver `[GAP-COMPUTED-SCORE-TWIVI-BAND]` y `[GAP-COMPUTED-SCORE-NORMALIZED-SIN-DEFINIR]`. |
| `[GAP-COMPUTED-SCORE-TWIVI-BAND]` | **NUEVO 2026-07-29 (#79). Las 75 filas de TwIVI siguen con `band = null`, y NO es el mismo defecto que se acaba de cerrar.** TwIVI tiene `centering_strategy = 'mrat'`: bandea los **4 valores de orden superior MRAT** (`OCH/SEN/CSV/STR`), no las **10 dimensiones Schwartz** que son las que tienen fila en `computed_score`. **No existe una banda por dimension que copiar.** `Medido contra prod, no inferido:` contando snapshots cuyos dos espacios de claves difieren -> **TwIVI 8/8 difieren; BFI-2-S 0/6, ONET-IP-SF 0/8, PERMA-Profiler 0/6**. `Por que NO se resolvio con lo obvio:` `hov_map` mapea HOV -> dimensiones miembro, asi que invertirlo y estampar en cada dimension Schwartz la banda de su HOV padre es **mecanicamente trivial** — y es justamente lo que no se hizo. Afirmar que una dimension "es ALTA" porque su grupo lo es **es una afirmacion psicometrica disfrazada de copia de datos**, y cae en la misma familia que `normalized`. **El test lo pinea en `null`** (`tests/unit/scoring/computed-score-band.test.ts`) para que nadie lo "arregle" por reflejo. | Transversal / scoring · psicometria | **German decide** · CC ejecuta | Tres vias planteadas: (a) queda `null` **[elegida 2026-07-29]**; (b) heredar la banda del HOV padre; (c) columna/via aparte para bandas por HOV, con migracion. Relacionado: ADR-036, ADR-043, `[GAP-TWIVI-GENDER-SCHEMA]`. |
| `[GAP-COMPUTED-SCORE-NORMALIZED-SIN-DEFINIR]` | **NUEVO 2026-07-29 (#79) — la mitad que NO se toco, a proposito. `normalized` sigue en 0 de 197 filas.** A diferencia de `band`, **no se calcula en ningun lado**: no hay percentil ni z que la alimente, asi que **antes del codigo hay que decidir que debe contener** (percentil del baremo? z ipsativo? z poblacional?) — decision psicometrica, no tecnica. Complica la eleccion que **el percentil no siempre se muestra**: `shouldShowPercentile` lo gatea por alpha, poblacion del baremo y `latam_status`, o sea que una columna "percentil" quedaria poblada en unos casos y vacia en otros por una razon **distinta** de la de hoy. **Hay un test que lo pinea en ausente**, para que agregarla obligue a pasar por esta decision. | Transversal / scoring · psicometria | **German decide** · CC ejecuta | Misma familia que `TEASER_PHRASE_FLOOR` en `[GAP-TEASER-COBERTURA-BANDAS]`: **algo declarado que nada consume**. Relacionado: ADR-042, ADR-043. |
| ~~`[GAP-TEASER-COBERTURA-BANDAS]`~~ | **CERRADO EN REPO (#83, 2026-07-29) Y EN PROD (seed aplicado 2026-07-30).** `Efecto medido sobre los usuarios REALES de prod`, simulado con el evaluador **real** (`lib/integrator/teaser.ts`) y con el arnes validado antes contra un oraculo independiente (reproduce exacto la tabla de #83: todo-`ALTO` 4f+2c · todo-`MEDIO` 3f+1c->4f+2c · todo-`BAJO` 1f+0c->4f+2c). De 10 usuarios, **6 pasan el gate de 4 instrumentos y los 6 estaban degradados**; tras sembrar **quedan 2**, y esos 2 por su quality-flag, no por cobertura (`160db916` 2f+0c->3f+2c con TwIVI flaggeado; `1169a9a7` GAP->GAP con los 4 flaggeados). **Cambian 5 de 10.** `Dato que corrige la ficha original:` **ninguno de los 10 usuarios de prod es todo-`ALTO`** — el perfil "que nunca estuvo roto" no existe en produccion, asi que esto no era un borde sino el caso comun. `El piso ya no degrada mudo:` `TEASER_PHRASE_FLOOR` quedo cableado en #83 (`app/(b2c)/perfil-integrado/page.tsx` loguea `teaser_below_phrase_floor` con el patron de bandas, nunca el perfil del usuario). Ver la fila nueva `[GAP-TEASER-FLOOR-INALCANZABLE-CON-FLAG]`: el piso **seguira** disparando para usuarios flaggeados y eso NO es este defecto. `[historico de la ficha original abajo]` **NUEVO 2026-07-29 (explicado en el smoke de #40, flag abierto hoy).** **Un perfil promedio se queda casi sin teaser, y degrada en silencio.** `Medido en el smoke, con prediccion falsable escrita ANTES de abrir la pantalla:` con bandas `BFI MEDIO · O*NET MEDIO · PERMA MEDIO · TwIVI BAJO` se predijo **2 frases y 0 cruces** y renderizo exactamente eso. `Las dos causas, verificadas contra el seed:` (1) **5 de los 6 cruces exigen `ALTO+ALTO`** (`teaser_cross_apertura_intereses`, `_autonomia_intereses`, `_apertura_valores`, `_bienestar_valores`, `_bienestar_personalidad`); el 6o —`_equilibrio_intereses_valores`— pide `MEDIO+MEDIO`, y fallo solo porque valores cayo en BAJO. Sobre el total de reglas las condiciones se reparten **14 ALTO / 5 MEDIO / 1 BAJO**, o sea el seed cubre **2 de 3 bandas** por instrumento. (2) **Degrada en silencio:** `TEASER_PHRASE_FLOOR` esta **exportado en `lib/integrator/teaser.ts:228` y no lo consume nadie** (verificado: cero referencias en el repo) — el piso existe y no vigila nada, asi que un teaser pobre se ve igual que uno sano. `Por que P2 y no P1:` no hay perdida de datos ni riesgo de compliance; es el climax del flujo Free rindiendo por debajo para el perfil **mas comun**. `El trabajo es de CONTENIDO, no de motor:` el propio seed declara `CONTENT IS PLACEHOLDER` y que "no pack covers inter-instrument crosses" — **Cowork escribe las reglas es-CO faltantes** (las de banda media/baja) y recien despues se consume el FLOOR para que la falta falle ruidoso. | Free / integrador (Ola 3) | Cowork escribe contenido · CC consume el FLOOR | **ADR-036 lo empeora**: con banda unica de TwIVI hay menos superficie por donde entrar a un cruce. Relacionado: `[GAP-TEASER-CROSS-TEMPLATES-ES-CO]` (mismo seed placeholder) y `[GAP-TEASER-BAND-NOT-SHAPE]`. |
| ~~`[GAP-TESTS-VACUIDAD-CONJUNTO-VACIO]`~~ | **CERRADO 2026-07-29: BARRIDO COMPLETO, poblacion final 5 — no 1.** `Evidencia:` **#58** (1, de paso) + **#67** (3: `no-hardcoded-instruments`, `no-hardcoded-strings`, `prohibited-phrases` — una sola forma copiada de `walk()` + `existsSync`) + **#70** (2: `consent-markdown.test.ts:63`, el guardia del P0 de #40, piso **103** = las lineas efectivamente comparadas, el mismo `103/103` verificado en prod; y `rls-enabled.test.ts:61` / COMPL-15, contador que **reusa la misma consulta menos el predicado de violacion**). `Las 2 de #70 falsadas por inyeccion, no argumentadas:` truncando el `.md` a 15 de 134 lineas el guardia queda **VERDE** y enrojecen los otros 3 del archivo; con `'public'` -> `'publik'` contra la DB local poblada, **sin contador VERDE / con contador ROJO**. `Lo que el barrido dejo dicho:` de las dos vias a vacio de COMPL-15, la **DB vacia** ya la cubria rio arriba el step de verificacion de migrations (no se duplico) y **la deriva de la consulta no la cubria nadie** — "esto ya lo cubre CI" era verdad de una via y falso de la otra. **Cuarta vez que un `>=1` de este repo resulta ser un piso** ("18 huecos" -> 31, "8 reales" -> 0, "73 filas" -> 64, ">=1 instancia" -> 5). `Contexto original:` **NUEVO 2026-07-28 noche (ADR-040, mecanismo 4 de 4).** `El mecanismo:` un test cuya asercion final **si** es incondicional, pero se evalua sobre un **acumulador que puede quedar vacio porque el insumo lo esta**. Instancia: `tests/lint/rls-policies-syntax.test.ts` **Test 2** — recorre `MIGRATION_DIRS`, junta `violations` y cierra con `expect(violations).toEqual([])`; **con cero directorios o cero policies pasa habiendo verificado nada**. `Por que no lo atrapa el gate 16, y no es un bug del gate:` el detector busca **ausencia de asercion sustantiva**, y aca la asercion existe y es correcta. El vacio no esta en la asercion, esta en el **insumo** — **es ciego estructural a este genero**, no distraido. `Por que NO se escribe detector:` "el conjunto que recorre nunca es vacio" **no es decidible estaticamente** (criterio de ADR-040); el barrido que se hizo para los mecanismos 2 y 3 dio 9 falsos positivos de 13 por exactamente esa razon. Se cierra **por instancia**: una asercion sobre el total recorrido, no solo sobre las violaciones. | Transversal / calidad (tests) | CC | **`>=1` es un piso, no un total** — mismo error de medicion que convirtio los "18 huecos" de la auditoria en 31 y los "8 reales" en 0. La instancia se cierra en PR propio; **lo que queda abierto en este flag es el barrido del genero**. |
| `[GAP-D33-OCCUPATIONS-HEADING-SIN-CONSUMIDOR]` | **NUEVO 2026-07-28 (lo encontro el gate E2E al encenderse, PR #42).** `MC_REPORT_OCCUPATIONS_HEADING` ("Areas donde gente con tu perfil suele encontrar engagement") sigue definido en `lib/i18n/microcopy/es-CO/report.ts` y **marcado ahi mismo como "VERBATIM (no tocar — acceptance gate D3.3)"**, pero tiene **CERO consumidores** en `app/` y `lib/` (grep verificado). La Fase 02.1 Wave 5 lo sustituyo por `MC_NIVEL_REVEAL_TITLE` ("Campos que podrian resonar contigo") en **las dos** ramas de ocupaciones del reporte. `Decision pendiente (tuya + Cowork, NO de un spec):` o D3.3 sigue vigente y hay que restaurarlo, o quedo superado por el reveal de nivel y hay que **retirar la marca de verbatim** del microcopy para que deje de afirmar un contrato que no existe. Deliberadamente **no** se restauro desde el test. `Mitigacion actual:` el copy que si se renderiza es igual de anti-determinista, y el test conserva la asercion etica dura (el reporte nunca dice "tu carrera ideal"). | Reporte Free / etica-copy | German + Cowork | **Es el hallazgo que justifica toda la entrega:** un requisito verbatim se cayo y era **imposible** notarlo, porque el unico test que lo vigilaba estaba `skipped`. El costo real de la cobertura declarada y no ejercida no es que no corra: es que lo que vigilaba se puede haber caido. |
| `[GAP-STEMS-ORTOGRAFIA-RESEED-PROD]` | **NUEVO 2026-07-28 (PR #43 arregla los seeds; prod NO).** **41 de 90 stems** —**13** de los 30 de BFI-2-S y **28** de los 60 de O*NET-IP-SF— tenian tildes faltantes y, en 4 casos, **ñ** (formas de *enseñar* y *niños*). `No se citan los stems aca:` la politica editorial de la raiz (§6) prohibe reproducir texto literal de items en documentacion; van por `item_code`/`sequence_number` en `estado/BRIEF_Cowork_D33_y_ortografia_items_v1.0.md`. El usuario los lee **literalmente** en el runner y O*NET es el **primer** test del Free. No es encoding: PERMA tiene sus 23 correctos. `Por que no cierra con el merge:` los dos seeds insertan con `WHERE NOT EXISTS`, asi que correrlos contra una DB que ya tiene las filas **pasa de largo en silencio** — mismo footgun de reseed de siempre. **Prod necesita un `UPDATE` scopeado, con OK explicito de German.** `Sin verificar:` el estado real de prod — el MCP de Supabase bloqueo la lectura; lo medido es local + los archivos de seed. Es la **quinta** superficie del problema es-CO, hermana de `[GAP-FICHA-WHAT-MEASURES-ES-CO]` (que ya anotaba lo mismo para `what_it_measures` de BFI/PERMA). | Instrumentos / seeds | CC (reseed, con OK German) + Cowork (firma: son items de instrumentos validados) | Solo ortografia — ninguna palabra cambia, ningun item se reformula. La distincion importa: reformular un item es decision psicometrica, ponerle la tilde no. |
| `[GAP-COPY-ACENTOS-CODIGO]` | **NUEVO 2026-07-28 (sexta superficie del problema es-CO; sale del escaneo de #41/#43).** Dos cadenas **user-facing en codigo** siguen sin acentos, y **no van en ningun PR abierto**: (1) `app/layout.tsx:37` — `metadata.description` dice "rigor **psicometrico**"; se sirve como `<meta name="description">` en **todas** las paginas (resultados de Google, previews al compartir). (2) `lib/i18n/microcopy/es-CO/delete.ts` — `MC_DELETE_SUCCESS_HEADING: "Tu cuenta **esta** borrada."`. **>>> TRAMPA, LEER ANTES DE TOCAR (2):** el spec de #42 afirma `getByRole("heading", { name: /tu cuenta esta borrada/i })`. Ponerle la tilde **pone el gate en rojo** y el que lo haga no va a saber por que. Hay que cambiar las dos cosas **en el mismo commit**. Es exactamente el patron de `lib/i18n/microcopy/es-CO/waitlist.ts` (sin acentos **a proposito** por un regex de E2E) — y la unica razon por la que ese no se rompio en #41 es que **alguien lo habia anotado**. | Transversal / copy es-CO | CC | Trivial en tamano, no trivial en orden. Hermanas: `[GAP-STEMS-ORTOGRAFIA-RESEED-PROD]`, `[GAP-FICHA-WHAT-MEASURES-ES-CO]`. |
| ~~`[GAP-E2E-COMPLETEINSTRUMENT-3-COPIAS]`~~ **CERRADO 2026-07-28 (PR #48, `09a2004`)** | **ERAN 2 COPIAS, NO 3 — y este flag se contradecia a si mismo.** El titular decia "existe **tres veces**" contando `free-critical-gates.spec.ts`, mientras la nota de abajo (correcta) decia que ese archivo es uno de los **consumidores** de la compartida. Verificado: `free-critical-gates` **importa** del fixture en la linea 33, junto con `anon-cannot-read-item-response` y `full-flow-onet`. Copias locales reales: **`free-full-flow.spec.ts:110` y `free-pause-resume.spec.ts:193`**. PR #48 las retira. `Lo que no era cosmetico:` la copia de `free-pause-resume` **no tenia la guarda `items.length === 0`** — consolidar la hace fallar ruidoso en vez de completar cero items en silencio. Se retiran ademas `sessionFor`, `itemsFor`, `type Admin` y el import de `APIRequestContext`, huerfanos por el cambio. Sigue vivo el genero: `ONET_STEMS` de `free-pause-resume.spec.ts:46` duplica datos sembrados y **rompio en CI** al corregirle la ortografia al seed. `Fuera de alcance, anotado:` `adminClient()` + `type Admin` siguen duplicados en los 2 specs (no los orfaniza el cambio). | Transversal / proceso (E2E) | CC | Barato y mecanico, como decia. El riesgo real de la duplicacion no es LOC: es que las copias se desincronizan en silencio — y aca una ya habia perdido una guarda. |
| `[GAP-MICROCOPY-COMENTARIO-FICHA-STALE]` | **NUEVO 2026-07-28.** `lib/i18n/microcopy/es-CO/report.ts:12` afirma en su bloque de contratos verbatim: `"ficha tecnica" en TRIGGER -> /ficha tecnica/i (NO "técnica")`. **Es falso:** la linea 29 del mismo archivo dice `MC_REPORT_FICHA_TRIGGER: "Ver ficha técnica del instrumento"` — con tilde. Un comentario que declara un contrato verbatim inexistente, **en el mismo bloque donde vivia D3.3**. | Reporte Free / microcopy | CC | Una linea. Es la misma clase exacta que `[GAP-D33-OCCUPATIONS-HEADING-SIN-CONSUMIDOR]` y que `[GAP-ASSEMBLER-COMENTARIO-RADIO-STALE]`: comentarios plausibles que afirman cosas que nadie verifico. Ya van tres. |
| `[GAP-A11Y-LECTOR-PANTALLA-REAL]` | **NUEVO 2026-07-27 (implementando ADR-036).** Ninguna corrida ha ejercitado el reporte con un **lector de pantalla de verdad** — todos los smokes leyeron el DOM. ADR-036 le sube la prioridad explicitamente: la contradiccion de bandas de TwIVI (el circulo decia `Destacar → Medio` y la narrativa de esa misma dimension decia lo contrario) **vivia entera en la tabla `sr-only`** del circumplejo, la superficie que solo un usuario de lector percibe completa. Un usuario vidente veia el circulo y el parrafo por separado; uno de lector oye las dos afirmaciones seguidas. Alcance minimo: VoiceOver o NVDA sobre un reporte de Valores y uno de bienestar, verificando la tabla `sr-only` de los dos visuales, el `<desc>` de `BarsWithBands`/`ValueCircle` y el banner NFR-28. | Reporte Free / a11y | CC (o German) | Los otros huecos de cobertura declarados en STATUS siguen igual: reduced-motion, movil 360/375, corrida de 4 tests nueva. **Avance parcial 2026-07-28 (PR #42):** el input de fecha de nacimiento de `/me/data` era el **unico** campo del formulario sin nombre accesible (`div`+`span` en vez de `label`) — un lector lo anunciaba sin etiqueta. Arreglado. Lo destapo el E2E al encenderse, no una auditoria: `getByLabel` fallaba porque el arbol de accesibilidad decia la verdad. **No cierra este flag** — sigue faltando la corrida con lector real. |
| `[GAP-FICHA-WHAT-MEASURES-ES-CO]` | **NUEVO 2026-07-27 (smoke PR #24/#26). Cuarta superficie del problema es-CO**, no cubierta por OLA 0 (microcopy `.ts` + `narrative_template`) ni por #26 (teaser). El campo "Que mide" de la ficha tecnica sale de `instrument_version.psychometric_status->>'what_it_measures'`. Estado medido en prod: **TwIVI** = "Muestra que valores pesan mas **para vos**" — **voseo VIVO en prod** mientras el seed (`db/seeds/instruments/TwIVI/instrument-version.sql:142`) ya dice "para **ti**" → **fila stale, mismo patron exacto que el teaser**, se cierra con UPDATE scopeado por `instrument_version_id` (no DELETE-first: solo cambia texto). **BFI-2-S** y **PERMA-Profiler** = tuteo pero **sin tildes** ("evaluacion clinica", "areas", "animo", "diagnostico", "sentias") — aqui el defecto esta **en el seed**, no es deuda de reseed. **ONET-IP-SF** = `null`, nunca sembrado → cae al generico (ver `[GAP-FICHA-QUE-MIDE-PREFIJO-DOBLE]`, se cierran juntos). | Reporte Free / ficha tecnica | CC (reseed TwIVI, con OK German) + Cowork (acentuar BFI/PERMA, redactar O*NET) | El "para vos" es lo mas grave: es voseo en prod con el seed ya arreglado. Ref: `estado/SMOKE_PR24_PR26_RESULTADOS_v1.0.md` §2.2. |
| ~~`[GAP-TEASER-VOSEO-SIN-ACENTOS]`~~ | **CERRADO EL 2026-07-28 CON OK DE GERMAN, Y LA FILA NUNCA SE TACHO.** No es una ficha que envejecio por deriva: es **trabajo hecho, verificado y no registrado** — la misma clase que las 14 que destapo la auditoria PM-23, sobreviviendo **dentro del mes en que se audito**. `Lo dice el propio `STATUS.md` (handoff PM-17, 2026-07-28):` reseed de prod **aplicado y verificado** — voseo **5 -> 0**, acentos **0 -> 12/14** (2 no llevan por su redaccion), mojibake 0, y **`md5(prod) == md5(seed)`**; `UPDATE` scopeado por `template_id`, y **PR #26** llevo la parte de codigo sin la cual `db reset` y CI regeneraban el texto viejo. `Re-medido en prod el 2026-07-30, y cuadra exacto:` **26 filas · 0 voseo · 23 con acentos · 3 sin acentos, los 3 legitimamente** (revisados uno por uno; ninguna palabra requiere tilde) = 12 de las 14 originales + 11 de las 12 nuevas del seed de hoy. La ficha seguia diciendo *"14 filas, 5 en voseo, 14/14 SIN un solo caracter acentuado"*: **las tres cifras falsas.** `Como aparecio:` de paso, buscando en que linea del BACKLOG editar. | Free / teaser | — (cerrado 2026-07-28) | `Pendiente relacionado y VIVO, no confundir:` `[GAP-COPY-ACENTOS-CODIGO]` sigue abierto — ese es copy en **codigo**, no en el seed; esta fila cubria solo `integrator_rule`. `Y la secuenciacion que la ficha proponia (plegar a OLA 3) quedo sin objeto:` se resolvio por reseed puntual, la opcion (b). `[historico de la ficha original abajo]` **NUEVO 2026-07-27 (smoke PR #20), cuantificado en prod:** `integrator_rule` tiene 14 filas, **5 en voseo** (`sentis`/`tenes`/`podes`) y **14/14 SIN un solo caracter acentuado**. Renderizado real en `/perfil-integrado`: "te **sentis comodo**", "**areas**", "**hipotesis**". Es el **primer espejo** que ve el usuario al terminar el Free y contrasta con los 4 reportes, que si quedaron en es-CO acentuado tras C1. Alcance ampliado de C1 que quedo pendiente. **DECISION DE SECUENCIACION (German):** esas 14 filas son probablemente las mismas que OLA 3 reemplaza con las 14 plantillas de cruce + 12 arquetipos ya firmados (`[GAP-TEASER-BAND-NOT-SHAPE]` / D2) → arreglar el voseo ahora seria trabajo tirado si el reemplazo esta cerca. Opciones: (a) plegar a OLA 3 [recomendado si arranca pronto]; (b) reseed puntual ~30 min, DELETE-first scopeado, patron C1, si OLA 3 se corre. | Free / teaser | German (secuencia) + CC | Ref: SMOKE_PR20 §2.3. |
| `[GAP-REPORT-BAND-LEGEND-INTERES]` | **NUEVO 2026-07-27 (smoke PR #20). Sexta superficie del mislabel que #20 no cerro.** `lib/i18n/microcopy/es-CO/report.ts:43` dice "ALTO significa que ese **interes** es de los mas fuertes dentro de tu propio perfil…" y se renderiza en los **cuatro** reportes — verificado en vivo en el de bienestar y en el de personalidad. #20 cerro 5 superficies (/me/data, titulo, email, assembler, mapa de labels); esta quedo afuera. **Fix:** mismo patron de #20 — neutralizar el sustantivo ("esa dimension"/"ese aspecto") o parametrizar por `instrumentCode`. Copy de Cowork. | Reporte Free | Cowork (copy) + CC | Barato. Ref: SMOKE_PR20 §2.2. |
| `[GAP-ONET-PEAK-GAP-THRESHOLD]` · `[GAP-TWIVI-ADJACENCY-THRESHOLD]` · `[GAP-PERMA-OVERALL-THRESHOLD]` · `[GAP-PERMA-BALANCED-THRESHOLD]` | **NUEVO 2026-07-08 (Ola 2 PR-C, composer §9).** §9 dice "los umbrales los fija el pack" pero no existen en repo. **Decisión German: defaults documentados + flags, afinar en deploy-smoke.** Valores en `lib/i18n/microcopy/es-CO/reveal-phrases.ts` (`family.thresholds`): O*NET `peakGap=5` (dif. de suma cruda 10-50, ≈0.5 Likert/ítem para "pico"); TwIVI `adjacency=0.5` (dif. de score centrado HOV, escala 1-6, para "par cercano"); PERMA `lowOverall=5.0` (**anclado a Kern "Languishing"**, = el mismo corte que `distress_thresholds.moderate PERMA_total<5.0` en el seed) + `balancedSpread=1.5` (max−min de {P,E,R,M,A} en 0-10, "sin driver claro"). Afinar con datos reales del smoke; ninguno bloquea (defaults razonables). | Free / Ola 2 (afinado post-smoke) | CC + German | Datos, no lógica: cambiar el valor no toca el motor genérico. **2026-07-23 (A1): EN ESPERA** — los 4 defaults se comportaron bien con el perfil A1, pero se diseñó con margen amplio → NO prueba los bordes. No afinar sin un perfil de borde. |
| `[GAP-PERMA-MINIRESULT-SURFACE]` | **NUEVO 2026-07-08 (Ola 2 PR-C).** El mini-resultado del composer se muestra en la **transición** (branch `nextCode` de `done/page.tsx`), que solo existe para tests 1-3 (hay "siguiente test"). PERMA es el **último** → cae al branch `allComplete` (close `?cierre=free`), que **no** muestra el mini-resultado del composer. Efecto: la frase reveladora de PERMA + su footer de contención LOW_OVERALL (§9.4 / §8 handoff) **no tienen superficie hoy**. El composer YA maneja PERMA correcto (LOW_OVERALL, contención por servidor) — falta decidir DÓNDE se muestra (¿mini en el close antes del teaser? ¿bloque en el reporte de cierre?). Fuera de scope de PR-C (el close es zona no-tocar §9 handoff). | Free / Ola 2-3 (cierre) | CC + German | Decisión de superficie, no de lógica. El composer está listo para cuando se decida. **2026-07-23 (A1):** verificado en vivo — PERMA cae al close (O*NET, no sensible) sin superficie de mini-resultado; snapshot prod `showContention:true` no visible. Sigue decisión de German. SEPARADA del defecto de contención `[GAP-PERMA-CONTENTION-GUIDED-FLOW]` (ese tiene arreglo propio, no espera A2). |
| `[GAP-TRANSITION-EST-TIME]` | **NUEVO 2026-07-08 (Ola 2 PR-C).** El §4.3 menciona "tiempo estimado" en la plantilla de transición, pero no hay fuente firmada de minutos por test (item_count existe: BFI 30 / O*NET 60 / TwIVI 20 / PERMA 23, pero el mapeo a minutos es contenido). El prompt de PR-C pidió recap/dots/hook/intent-recall (implementados) — **no** tiempo. Diferido: derivar de item_count con heurística documentada o firmar minutos por test con Cowork. | Free / Ola 2 | Cowork + CC | P3. No bloquea; la transición ya orienta con recap+dots+hook. **2026-07-23 (A1): YA TIENE FUENTE** — `/onboarding/mapa` muestra minutos por test (~4/~6/~3/~3); no necesita Cowork, es cablear la fuente existente (heurística ya visible al usuario). |
| `[GAP-NFR28-BANNER-HEADING-GENDER]` | **NUEVO 2026-07-23 (Cowork, al integrar el copy de ADR-033). P2.** `MC_NFR28_BANNER_HEADING` (`lib/i18n/microcopy/es-CO/nfr28.ts`) dice "Si estás pasando un momento difícil, no estás **solo**." — marca de género masculina, en tensión con §9 (sin adjetivos con marca de género aplicados al usuario). Está **firmado y compartido**: se renderiza en el `ContentionBanner` de los 3 instrumentos sensibles (BFI/PERMA/VALUES) y ahora también en la pantalla de cuidado de PERMA (ADR-033), ante usuarios en malestar. **No se plegó en ADR-033** (scope: ese fix es de ruteo, no de copy firmado). Fix = reword neutro por Cowork (p.ej. "no tienes que pasarlo en soledad" / "hay quien puede acompañarte"), luego CC cambia el string (trivial). | Contención (transversal) | Cowork (copy) + CC (wire) | Copy firmado; reword neutro. Descubierto integrando ADR-033. |
| `[GAP-TWIVI-GENDER-SCHEMA]` | **NUEVO 2026-07-01 (integración pack TwIVI; ADR-030 addendum). DIFERIDO.** Ramificado él/ella de los portraits TwIVI (el instrumento oficial recomienda branching de pronombre). Verificado que la app **no captura género declarado** (ni `user`, ni signup, ni `assessment_session`; prod `item` sin `gender_variant`), así que ramificar exige construir: (1) un paso de captura de género en onboarding; (2) su tratamiento Ley 1581 (género ≈ dato sensible Art. 5, misma superficie que `[GAP-CONSENT-LEVEL-1.1.0]`; considerar framing "¿cómo prefieres que te hablemos?" = preferencia de visualización, no identidad, para bajar el peso legal); (3) la ruta de schema (pack §4: A token en render / B columna `gender_variant`+40 filas / C `stem_f`). El seed M/F ya existe (`estado/TwIVI_items_es-CO_SEED_v1.0.sql`) como fuente para cualquier ruta. **MVP arranca con variante NEUTRA** (schema-limpia, sin género) → este gap es aditivo después, no bloquea lanzamiento. | Free / post-MVP1 | CC + German + legal | Diferido por decisión (neutra ahora). Aditivo. |
| `[GAP-AUTH-EMAIL-SMTP-BRANDING]` | **ESCALADO a bloqueador de verify 2026-06-10 (Test 8):** dos issues, mismo fix. (1) El email del magic link llega desde **Supabase** con plantilla default sin marca ("Confirm your email / finish signing up"), no la transaccional DescubreMe — `signInWithOtp` => Supabase Auth envia con su SMTP/template default; `lib/email/transactional.ts` (Resend) es para otros correos. (2) **El email service built-in de Supabase tiene un cap horario bajo (~2-4/h, solo testing). Confirmado por reproduccion: `over_email_send_rate_limit` HTTP 429 "email rate limit exceeded"** tras ~10+ envios hoy => bloquea Tests 8-10 (signInWithOtp falla, UI muestra el generico "Intenta de nuevo"). **Fix (desbloquea AHORA + permanente + branding):** configurar **custom SMTP (Resend)** en Supabase Auth (Authentication > Emails > SMTP Settings): host `smtp.resend.com`, port 465, user `resend`, password = RESEND_API_KEY (la de Vercel env / secrets), sender `noreply@descubreme.co` "DescubreMe". Quita el cap del built-in + usa el dominio verificado. Luego personalizar templates Magic Link/Confirm signup con marca + NFR-27 (Cowork copy). PROD: mismo SMTP. | Fundacion / Fase 1 verify (escalado de Fase 7) | German (Supabase dashboard) + Cowork (templates) | Bloquea Tests 8-10. No es bug de codigo (flujo correcto). |
| ~~`[GAP-ANON-TEST-STALE-FIX4]`~~ | **CERRADO 2026-06-10 (mismo dia, decision German "arreglar ahora"):** Test 1 reescrito al contrato read-only (cookie sembrada como minteada por middleware -> INSERT sin re-setear cookie) + nuevo Test 1b (sin cookie -> throw con guidance, nunca mintea desde Server Component). `anonymous.test.ts` 5/5; suite vitest 165 passed / 0 failed. **Contexto (Descubierto 2026-06-10 corriendo suite durante verify Bug 1):** `tests/unit/session/anonymous.test.ts` Test 1 (y la premisa del Test 2) quedaron stale tras Fix 4 del verify (commit `9542672`): `lib/session/anonymous.ts` paso a read-only (mint del `anonymous_session_id` cookie movido a `middleware.ts` por Next.js 16 "cookies forbidden in Server Components"), pero el test sigue esperando que `getOrCreateAnonymousSession` cree la sesion + setee la cookie => ahora `throw "middleware did not mint it"`. **1 test failing** en la suite (confirmado pre-existente en HEAD, NO regresion del fix de Bug 1). **Fix:** reescribir el test para sembrar la cookie como ya presente (contrato read-only nuevo) o mover la asercion de mint a un test de `middleware.ts`. Mientras tanto la suite vitest queda RED en CI -> bloquea un merge limpio a main. | Fundacion / Fase 1 verify (test drift) | CC | No afecta runtime (el comportamiento read-only de anonymous.ts es el correcto; lo stale es el test). Bloquea CI green + merge. |
| ~~`[GAP-REPORT-BAREMO-NOTE-REPEAT]`~~ | **CERRADO 2026-06-10:** la nota se renderiza UNA sola vez al pie de la lista (no por-dimension) + intro `MC_REPORT_SCORES_INTRO` arriba. Cambio en `app/(b2c)/reporte/[sessionId]/page.tsx`. Typecheck green. **Contexto (Descubierto 2026-06-10 verify Test 10, primer render real):** la nota `MC_REPORT_BAREMO_NOTE` ("Tu perfil se lee de forma intra-perfil... El baremo para Colombia esta en validacion.") se appendea a CADA una de las 6 lineas de dimension (`app/(b2c)/reporte/[sessionId]/page.tsx:178`, porque `showPercentile=false` para todas) => se repite 6 veces. **Fix (code, CC):** mover la nota a UN solo footnote debajo de la lista (no por-dimension). Cambio pequenio de JSX. | Fundacion / Fase 1 (report polish) | CC | No bloquea (reporte funciona); defecto de UX visible que German noto. |
| `[GAP-REPORT-OCCUPATIONS-DEPTH]` | **Descubierto 2026-06-10 (verify Test 10, feedback German):** la Capa 3 muestra solo NOMBRES de ocupaciones (5, `page.tsx:198` `occ.nameEsCo`), sin descripcion ni nivel/seniority ni job-zone. Seed = 96 ocupaciones LATAM por high-point RIASEC (schema 4-col, sin descripcion). German: "demasiado basicas, no tienen en cuenta el nivel profesional". **Fix (Cowork/producto, futuro):** enriquecer el set (descripcion + diferenciacion por job-zone/seniority) + logica de seleccion + presentacion. | Paid/Free / Fase 2-3 (no Fase 1) | Cowork + CC | No bloquea Fase 1 MVP. Product depth, "para mas adelante" (German). |
| ~~`[GAP-REPORT-COPY-CLARITY]`~~ | **CERRADO 2026-06-10 (Cowork copy + wired):** `MC_REPORT_BAREMO_NOTE` reescrita en lenguaje llano (explica que ALTO es relativo a tu propio perfil + por que no hay percentil, sin jerga "intra-perfil") + `MC_REPORT_SCORES_INTRO` nueva. Lint prohibited-phrases + typecheck green. Pendiente: eyeball visual de German en el preview. **Contexto (Descubierto 2026-06-10 verify Test 10, feedback German):** el copy del reporte usa jerga psicometrica que el usuario no entiende ("intra-perfil", "baremo para Colombia en validacion") sin explicacion accesible. La v1.0 de microcopy se escribio sin ver el render. **Fix (Cowork microcopy):** reescribir la nota ipsativa/baremo en lenguaje llano (que significa BAJO/MEDIO/ALTO relativo a TU propio perfil; por que no hay percentil), 1 sola vez. Mantener rigor (no inventar percentiles). | Fundacion / Fase 1 (report polish) | Cowork | No bloquea funcion; afecta "experiencia clase mundial" (decision 4). |
| ~~`[GAP-REPORT-READY-EMAIL-FROM]`~~ | **CERRADO 2026-06-10 (code, pending deploy+retest) — Test 11:** el email "tu reporte esta listo" nunca llegaba. Causa: `lib/email/transactional.ts` `DEFAULT_FROM` hardcodeaba `hola@descubreme.example` (dominio NO verificado en Resend) y el reporte no pasa `fromAddress` => Resend rechaza el envio (falla silenciosa: retorna ok:false sin throw, la pagina ignora el resultado). `RESEND_API_KEY` SI esta en Preview (confirmado `vercel env ls`); el bug era solo el from. Los magic links llegan porque van por SMTP de Supabase, no por esta ruta API. **Fix:** `DEFAULT_FROM` lee `RESEND_SENDER_EMAIL` con fallback al verificado `noreply@descubreme.co`. **Config drift (P3):** Preview + .env.local tienen `RESEND_FROM` (no `RESEND_SENDER_EMAIL` del .env.example); el fallback lo cubre, pero conviene unificar el nombre. PROD: misma config. | Fundacion / Fase 1 verify | CC | No bloquea (reporte en browser); email es conveniencia D3.7. |
| `[GAP-SUPABASE-SEED-WIRING]` | `db/seeds/instruments/ONET-IP-SF/*.sql` + `db/seeds/contention-resources/CO/seed.sql` + `db/seeds/mocks/*` NO estan wired a `supabase db reset` — el CLI espera `supabase/seed.sql` (singular) que include/sources los demas. Actualmente seeds se aplican manual via `psql -f` (proceso de Plan 01-12 Task 2 sub-flow). CI workflow tampoco los aplica. **Fix**: crear `supabase/seed.sql` que sea catch-all SOURCE de los `db/seeds/*` necesarios para dev local + CI; actualizar `.github/workflows/ci.yml` para correr seeds post-migrations. | Fundacion / Fase 1 (gap-closure) | CC | Phase 1 ship NO bloqueado (seeds documentados); bloquea CI tests DB-gated reproducibles + dev onboarding fluido. |
| ~~`[GAP-CI-E2E-DB-SUPABASE-ROLES]`~~ | **CERRADO Y VERIFICADO EN `main` 2026-07-27.** Corrida de `push: main` **`30311737492`** sobre `0010e89`: **`conclusion=success`** — **25 passed / 8 skipped / 0 failed**, la primera vez que el check de `main` esta verde desde PR #5, y la primera vez que significa algo. Se cerro en dos entregas y el enunciado original estaba **mal**: pedia "crear los roles Supabase en CI", lo cual arregla las **migrations** y no alcanza para el criterio real, porque los specs no le hablan a Postgres sino a la **API de Supabase** (GoTrue para magic links, PostgREST) — ningun set de roles sobre un Postgres pelado sirve. **Entrega 1 (PR #25):** `supabase start` en el workflow, que levanta el stack y de paso aplica migrations + los 26 seeds de `config.toml`. **Entrega 2 (PRs #32/#33/#34/#36):** la suite de **12 fallas a 0** — 8 de ellas specs anteriores al `TestEntryGate` de Ola 2.2, no a ADR-029. **Cierre (PR #37):** el runner a Node 24; en 20 no existe el `WebSocket` global que `@supabase/realtime-js` exige, asi que 11 specs morian antes de la primera asercion — **primer hallazgo real del gate**, invisible mientras esos specs se skipeaban. `Leccion:` un flag puede sobrevivir semanas con el **diagnostico equivocado** y aun asi apuntar al sintoma correcto; antes de ejecutarlo, revalidar el enunciado contra el criterio de exito, no contra su titulo. `[diagnostico original, conservado]` **NUEVO 2026-07-01 (al mergear PR #6, German pidió anotarlo).** El check de CI **"Full suite (typecheck + lint + unit + E2E con DB)"** (GitHub Actions) **falla en el setup**, antes de correr cualquier test: el step "Apply Supabase migrations (psql)" revienta en la 1ª migración — `psql:supabase/migrations/001_plugin_catalog.sql:28: ERROR: role "anon" does not exist` (exit 3). El Postgres del CI es vanilla y no crea los roles Supabase (`anon`, `authenticated`, `service_role`, `supabase_auth_admin`) que las migraciones asumen (GRANT/RLS los referencian). **Consecuencia:** el gate E2E-con-DB da ROJO FALSO para todos → los PR se mergean en estado `UNSTABLE` (check no-requerido) y el gate **no atrapa regresiones reales** (ej. PR #5 y #6). **Fix:** crear los roles Supabase en CI antes de aplicar migraciones (`CREATE ROLE anon/authenticated/service_role/supabase_auth_admin ...` en un pre-step, o levantar el stack con el Supabase CLI (`supabase db start`/`db reset`) que los provisiona; relacionado con `[GAP-SUPABASE-SEED-WIRING]`). Ref: GH Actions run 28551424277. | Transversal / proceso (CI) | CC | No bloquea deploy (Vercel es check aparte, pasa); el gate de tests automatizados está CIEGO hasta arreglarlo — cae en `[GAP-MAIN-BRANCH-PROTECTION-BYPASS]` (si se adopta PR-gate, este check debe volverse verde primero o no será required). |
| `[GAP-RIASEC-NARRATIVES-UNIQUE-INDEX]` | `db/schema/narrative-template.ts` no tiene UNIQUE INDEX sobre `(version, riasec_code, lang, slot)`. La entrega Cowork narratives commit `ee62bf5` usa DELETE+INSERT en BEGIN/COMMIT para idempotencia (funcional). **Fix**: migration nueva que crea `CREATE UNIQUE INDEX ON public.narrative_template (version, riasec_code, lang, slot);` + cambiar seed.sql a `INSERT ... ON CONFLICT (version, riasec_code, lang, slot) DO NOTHING;` (idempotencia mas robusta, evita race con runs concurrentes). | Fundacion / Fase 1 (gap-closure) | CC | Phase 1 ship NO bloqueado (DELETE+INSERT en transaccion funciona); mejora robustez del seed. |
| `[GAP-IKIGAI9-CFA-LOCAL]` | Confirmar dimensionalidad de Ikigai-9 (3 factores vs. unifactorial) en muestra es-CO; mientras tanto reportar puntaje global | Ikigai / Fase 5 | Cowork + CC | UK=1 factor, Japon/Alemania=3 |
| `[GAP-EDGE-CRON-LIMITS]` | Verificar limites de cron Edge Functions Supabase (frecuencia, max duration) para `aggregate-tenant` antes de implementar | B2B / Fase 4 | CC | Funcion expira con orgs grandes → agregados B2B quedan stale sin alerta |
| `[GAP-STRIPE-COP-SANDBOX]` | Verificar Stripe Adaptive Pricing + COP funcionando en sandbox antes de comprometerse a arquitectura de pricing | Paid / Fase 3 | CC + German | Fallback: dual pricing (`paid_usd` + `paid_cop`) seleccionado por `Vercel geo.country` en middleware |
| `[GAP-PACK-PGI]` | Implementation pack de PGI (Personal Growth Initiative) si se usa standalone en B2B | B2B / Fase 4 | Cowork | Hoy PGI entra combinado en lentes |
| `[GAP-PACK-PVQRR]` | Revisar textos §5.A HOV del pack PVQ-RR tras reasignacion FAC -> Conservacion (ver `PVQ-RR_HOV_PARTITION_VALIDATION_v1.0.md`) | Paid/B2B / Fase 3-4 | Cowork | Alcance probablemente menor |
| ~~`[GAP-MICROCOPY-FASE1]`~~ | **CERRADO 2026-06-08 (Cowork delivery v1.0 commit `94d7186`):** 14 archivos `lib/i18n/microcopy/es-CO/*.ts` overrideados con copy final es-CO (voseo + tu/tus posesivos). Verbatim D3.3/D3.10/D3.12 byte-exact en `report.ts`. 13 comentarios "Pin E2E" defensivos. 0 TODO(cowork) restantes. Lint prohibited-phrases GREEN; typecheck clean. | Fundacion / Fase 1 | — | Resuelto |
| `[GAP-ONET-ANCHORS-SOURCE]` | `implementation_packs/RESPONSE_ANCHORS_es-CO_v1.0.md` cubre Flourishing + BFI-2-S + PERMA-Profiler pero NO O*NET IP-SF. Phase 1 Plan 01-06 uso UI-SPEC §6.4 lineas 276-280 como canon temporal verbatim. Cowork action: anadir seccion "O*NET IP-SF (1-5, gusto)" al addendum con citas literales + sha256 fingerprint para que `lib/questionnaire/response-scales.ts` pueda regenerarse desde fuente Cowork-owned. | Fundacion / Fase 1 | Cowork (Investigador psicometrico) | Descubierto 2026-06-07 ejecutando Plan 01-06 Task 1+2. No bloquea Phase 1 (canon temporal en UI-SPEC §6.4). |
| `[GAP-MOCK-SEED-SCHEMA-DRIFT]` | `db/seeds/mocks/MOCK-PREF-12/instrument.sql` (escrito en Plan 01-03 antes que Plan 01-04 finalizara el schema) usa columnas obsoletas: `instrument_code`, `instrument_version`, `item_code`, `position`, `stem_es_co`. Schema real Drizzle (Plan 01-04) usa: `instrument_id` UUID FK, `instrument_version_id` UUID FK, `sequence_number`, `stem`. Mock no aplica al DB actual. | Fundacion / Fase 1 (Plan 01-12 CI) | CC | Descubierto 2026-06-07 ejecutando Plan 01-06 Task 2 (seeds reales usan schema correcto). Refrescar mock antes de Plan 01-12 CI que ejecutara `supabase db push` contra path `db/seeds/mocks/`. |
| `[GAP-BAREMO-INTL-PCTL-EXTRACT]` | `db/seeds/instruments/ONET-IP-SF/baremo-intl.sql` deja Addendum §D fixed percentiles en escala native 0-10 paper-and-pencil con marker `scale_descubreme_conversion_status: reference_only`. DD-57 v3.0 Opcion B 0-40 conversion deferred — Phase 1 Free usa Opcion C ipsativa (bandas BAJO/MEDIO/ALTO via z-score) asi que el stub NO bloquea el plan goal. Pero cuando Phase 3 Paid quiera mostrar percentil real, hay que extraer la tabla de conversion 0-10 native -> 0-40 DescubreMe o publicar baremos LATAM nativos en 0-40. | Fundacion / Fase 1 (deferred) -> Paid / Fase 3 (bloquea) | Cowork (Investigador psicometrico) + CC | Descubierto 2026-06-07 ejecutando Plan 01-08 Task 8 (seeds Addendum §D). |
| `[GAP-CONTENTION-VERIFY-2026]` | `db/seeds/contention-resources/CO/seed.sql` tiene 6 lineas verbatim D1.7 (Linea 106, Linea de la Vida, Linea 123, Linea 155, Profamilia, Asociacion Colombiana de Psiquiatria) con `last_verified_at='2026-06-05T00:00:00Z'`. La fecha es la del cierre del CONTEXT, NO verificacion empirica de que los 6 numeros sigan operativos hoy. `isContentionStale(row, 90d)` emite `logger.warn` no-bloquea, pero antes de deploy a produccion Cowork debe llamar las 6 lineas + confirmar que sigan operando + actualizar `last_verified_at` si confirma. Si una linea cambio, edit el seed antes del primer reporte renderizado con NFR-28 chip. | Fundacion / Fase 1 | Cowork (Estratega / Producto) | Descubierto 2026-06-07 ejecutando Plan 01-08 Task 8 (verbatim D1.7 seed). |
| `[GAP-NPM-AUDIT-PHASE7]` | `npm install @sentry/nextjs@^10.56.0` (Plan 01-11 Task 1) reporta 13 vulnerabilities transitivas (moderate/high/critical) del dep tree del SDK. NO se corre `npm audit fix --force` (breaking changes en transitives). Revisar antes de deploy Phase 7: (a) si Sentry SDK newer minor/patch ya las absorbio, (b) si Phase 7 SDK upgrade lo absorbe automaticamente, (c) si alguna transitiva es realmente explotable en Next.js server context. Mientras tanto: vulns son del dep tree del SDK no del codigo de DescubreMe; impacto runtime depende de paths que toquen las libs vulnerables. | Fundacion / Fase 7 deploy | CC | Descubierto 2026-06-07 ejecutando Plan 01-11 Task 1 (Sentry install). No bloquea Wave 8 Plan 01-12; bloqueador potencial deploy Phase 7. |
| Sub-specs por producto | Specs detalladas Free/Paid/B2B/Ikigai si se requieren (gran parte ya en PRD + UX spec) | Fases 2-5 | Cowork | Crear bajo demanda |
| Spec de experiencia B2B + dashboard | Experiencia del dashboard agregado anonimo (rol admin) | B2B / Fase 4 | Cowork | UX_EXPERIENCE_SPEC §14 lo deja como lineamiento |
| Textos finales por test | Hooks validados, frases reveladoras (capa 2), narrativa del integrador es-CO | Fases 2-3 | Cowork | Se redactan junto a cada pack |
| Piloto cognitivo es-CO | N=6-8 para instrumentos/anclas nuevos (PVQ-21, Ikigai-9) antes de go-live | Fases 2,5 | Adaptacion | Estandar del proyecto |
| `[GAP-CALLBACK-INCOMPLETE-SESSION-REPORTE-404]` | **RESUELTO EN CÓDIGO 2026-07-07 (PR-A `ola-2a`, TDD; pendiente deploy-smoke):** el callback ahora gatea el redirect a `/reporte` por presencia de `report_snapshot` (espejo `free-close.ts:89-96`); sin snapshot → cae a `resolveNextFreeTest` y reanuda el test pendiente (o `/onboarding/mapa`), nunca un 404. `callback-idempotent.test.ts` reparado (migrado a `verifyOtp`/`token_hash`, cierra `[GAP-PHASE21-CALLBACK-TEST-STALE]`) + 3 casos de sesión incompleta. **Historia original:** **Descubierto 2026-06-29 (smoke manual Phase 2.1, German).** Sub-caso afilado de `[GAP-REPORT-SCORING-NOT-TRIGGERED]` (linea 38; su fix cablea `scoreSession` para sesiones COMPLETAS). Cuando el signup arrastra un `session_id_pending` de una sesion anonima **incompleta** (`signup/actions.ts:118`), el callback (`app/auth/callback/route.ts:285`) la claimea, `scoreSession` retorna `ok:false` (correcto: incompleta -> sin snapshot) y **igual redirige a `/reporte/{id}`** -> `/reporte` (`[sessionId]/page.tsx:166`) no halla snapshot -> `notFound()` -> **404**. Confirmado: logs auth (verifyOtp 200 + login) + DB (sesion `0bea76ec`, user +r6: status `open`, progress 20, snapshots 0). Email totalmente nuevo (sin sesion anonima) -> `session_id_pending` null -> `/test/{primerCode}` -> OK (verificado por German). **NO llega al umbral de rollback** (el camino de usuario nuevo del funnel invertido funciona; bug solo en el edge test-anonimo-incompleto -> signup). **Fix (decision German, dos rutas que se solapan):** (a) callback: si la sesion claimeada esta incompleta (`status != completed` / sin snapshot), redirigir a `/test/{code}?resumed=true` para reanudar, NO a `/reporte`; (b) Wave D mata los entry-points anonimos -> `session_id_pending` nunca se setea -> el bug queda moot (puede dominar a (a)). | Free / Fase 2.1 (post-deploy) | CC + German | Estrecho (funnel es signup-first); camino anonimo vivo hasta Wave D. Ref `[GAP-REPORT-SCORING-NOT-TRIGGERED]`. |
| `[GAP-FREE-TEST-INTRO-COPY]` | **NUEVO 2026-06-29 (smoke Phase 2.1, German).** Las paginas de test no explican que instrumento es, por que se hace, ni que se va a lograr — el usuario entra "a ciegas". Falta copy de intro/contexto por test (es-CO neutro — ver `[GAP-MICROCOPY-VOSEO-TO-ES-CO]`). Sub-item de `[GAP-UX-FLOW-REDESIGN]`. | Free / Fase 2.1 | Cowork (copy) + CC | Parte de "el copy no explica el test". |
| `[GAP-REPORT-INTERESES-MISLABEL]` | **NUEVO 2026-07-23 (smoke A1). P2.** El reporte llama "Intereses" a los 4 instrumentos: `/me/data` lista los 4 reportes como "Intereses · fecha" (idénticos), y el reporte de PERMA se titula "Tu perfil de intereses" con leyenda que dice "interés". PR #17 cerró los **códigos crudos de las barras** (NEG/Lon/hap → labels es-CO firmados); esto es **otra superficie** (título del reporte + lista `/me/data`), no los labels de barra. Empeora `[GAP-PERMA-CONTENTION-GUIDED-FLOW]`: en `/me/data` elegir el reporte de bienestar es adivinar entre 4 enlaces idénticos. | Free / reporte | CC | Superficie distinta de los labels de barra (ya cerrados). Ref: SMOKE_A1. |
| `[GAP-TEASER-BAND-NOT-SHAPE]` | **NUEVO 2026-07-23 (smoke A1). P2 — evidencia para D2 `[GAP-TEASER-CROSS-TEMPLATES-ES-CO]`.** El teaser de `/perfil-integrado` contradice los mini-resultados: dijo "tus intereses se reparten de forma equilibrada" a un perfil con pico I=50 vs C=10. **No es bug de código:** `db/seeds/integrator-rule/teaser/seed.sql` selecciona por **banda global por instrumento** (`code ONET-IP-SF band MEDIO`), no por la forma del perfil → cualquier perfil MEDIO agregado recibe "equilibrada". Es la superficie que OLA 3 reconstruye con las 14 plantillas de cruce + 12 arquetipos ya firmados (D2). Evidencia directa para priorizar ese reemplazo. | Free / teaser (OLA 3) | reemplazo D2 (CC seed) | El primer espejo contradice lo que el usuario acaba de leer. Ref: SMOKE_A1. |
| `[GAP-TEASER-FLOOR-INALCANZABLE-CON-FLAG]` | `[PRIORIDAD A CONFIRMAR — la decide German]` **NUEVO 2026-07-30, medido al sembrar el teaser en prod.** **El piso de 4 frases es aritmeticamente inalcanzable para cualquier usuario con un instrumento quality-flaggeado**, con las 26 reglas y con las que sean. `El mecanismo:` hay **exactamente 4 celdas de frase simple por banda** (una por instrumento, 4x3=12 en total), y el filtro de quality-flag (`lib/integrator/teaser.ts:191`) descarta toda regla cuyo `requires_dimensions` toque el instrumento flaggeado -> **techo 3 frases < piso 4**. Con 2+ flaggeados el techo baja mas; con los 4, ninguna regla matchea y cae a `gapResult`. `Verificado en prod, no teorico:` `160db916` (todo `MEDIO`, TwIVI flaggeado) rinde **3f+2c y sigue bajo piso** con las 26 reglas; `1169a9a7` (los 4 flaggeados) da **GAP**. Son los 2 usuarios que no se arreglaron. `Consecuencia operativa que hay que leer bien:` el log `teaser_below_phrase_floor` que #83 cableo **va a disparar para estos usuarios siempre**, y eso NO significa que el seed fallo ni que falte cobertura. Sin esta fila, la proxima lectura del log se diagnostica como regresion. `Las opciones son de producto/psicometria, no de codigo:` (a) bajar `MIN_PHRASES` cuando hay flags, (b) permitir que un cruce cuente hacia el piso, (c) aceptar el piso como inalcanzable y solo no loguearlo cuando la causa es un flag. **No se toco nada.** | Free / integrador | German decide (producto/psicometrica) · CC implementa | Familia de `[GAP-TEASER-COBERTURA-BANDAS]` pero **causa distinta**: alla faltaban reglas, aca el techo lo impone el conteo de instrumentos. Se descubrio porque la prediccion se hizo sobre EFECTO en usuarios reales, no sobre filas insertadas |
| `[GAP-TEASER-RULES-SIN-ORDER-BY]` | `[PRIORIDAD A CONFIRMAR — la decide German]` **Preexistente, nombrado en #83 y NO arreglado; agravado por el seed del 2026-07-30.** `loadTeaserRules` (`lib/integrator/teaser-data.ts:145-150`) selecciona **sin `ORDER BY`**. Con los topes de 6 frases / 2 cruces, **cuales** se muestran depende del orden que devuelva Postgres — no garantizado por el estandar y puede cambiar tras `UPDATE`/`VACUUM`/replanning. `Que NO afecta:` el piso ni los conteos — el corte es por cantidad y cualquier cruce que matchea es valido, asi que el `break` de `teaser.ts:187` nunca descarta matches antes de llenar los topes. `Que SI afecta:` reproducibilidad del render y la lectura de un smoke. **Al pasar de 14 a 26 reglas la superficie de no-determinismo casi se duplica**, y para un perfil todo-`MEDIO` ahora hay mas cruces elegibles que los 2 que caben. `Por que importa registrarlo ahora:` en un smoke post-seed "salio distinto que ayer" es **esperado y valido**, no una regresion — sin esta fila se diagnostica al reves. `Fix:` un `ORDER BY template_id` (o una columna de prioridad si el orden es decision de contenido de Cowork). | Free / integrador | CC (una linea) · Cowork si el orden es de contenido | Misma familia que `[GAP-TEASER-FLOOR-INALCANZABLE-CON-FLAG]`: los dos son consecuencias de pasar de 14 a 26 reglas que no se ven leyendo el diff de #83 |

---

## P3 — Deseable / futuro

| Flag | Pendiente | Producto/Fase | Owner | Nota |
|---|---|---|---|---|
| `[GAP-A11Y-DOBLE-H1-ENTRY-GATE]` | **NUEVO 2026-07-29 (smoke con cuenta de #40).** En el `TestEntryGate` —la pantalla de "Comenzar", antes del item 1— hay **dos `h1`**: el encabezado `Test N de 4 · <Categoria>` y el hook del instrumento. `Confirmado en dos instrumentos` (TwIVI y BFI-2-S), o sea es sistematico de esa pantalla, no un caso suelto. `Atribucion verificada, no supuesta:` `app/(b2c)/test/[code]/_components/DoubleLevelProgress.tsx:53` es `<h1>` hoy y era **`<p>`** antes de #40 (`git show 124c495~1:...`, linea 50); el hook **ya era** `h1`. **El segundo lo introduce #40.** `Dos acotaciones que bajan la severidad:` (1) **en el runner hay un solo `h1`** — el hook no se monta despues de "Comenzar", asi que el duplicado vive SOLO en la pantalla de entrada; (2) el `h1` del encabezado es **deliberado y esta justificado en el codigo** (`:50`: sin el, el documento queda sin `h1`). `Por eso el defecto NO es esa decision:` es que no se considero la pantalla donde el hook ya aportaba uno. **El fix probable es degradar el hook a `h2` en el entry gate, no revertir el encabezado** — revertirlo reintroduciria el problema que #40 arreglo. | Transversal / a11y · B2C | CC | Afecta el outline del documento y a lectores de pantalla, no el contenido — por eso P3 y no P2. `Relacionado:` `[GAP-A11Y-LECTOR-PANTALLA-REAL]`, que sigue vigente: todo lo de a11y de este smoke se afirma sobre **atributos**, no sobre lo que anuncia NVDA/VoiceOver. |
| `[GAP-ONET-SEQ26-ESCENOGRAFIAS]` | **NUEVO 2026-07-28 (descubierto durante el reseed ortografico de prod, ADR-038).** El item **`sequence_number = 26`** de O*NET-IP-SF lleva un sustantivo plural **sin la tilde que le corresponde**; el resto de la frase esta bien. Esta igual en el seed de `main` y en prod, asi que **el reseed no lo introdujo ni lo arregla**: verificado con `git show 63a25f7` que el fix de ortografia (#43) **nunca toco esa entrada**. `Por que quedo fuera:` la firma de Cowork (consulta 2 del brief) cubre un **set especifico de 41**; meter una correccion no firmada rompe esa firma. German decidio 2026-07-28 **separarlo**. `Alcance del cierre:` editar `db/seeds/instruments/ONET-IP-SF/items.sql` (= cambio de codigo, va por PR) + un segundo `UPDATE` scopeado contra prod. **Antes de tocar el archivo, aplicar la regla de las 3 trampas:** grepear `tests/` por la palabra completa y leer el header del seed por si hay pin verbatim (el item 26 **no** esta entre los 4 que `free-pause-resume.spec.ts:46` duplica, pero eso hay que grepearlo, no inferirlo). `Como se encontro, que es lo reusable:` barrido **exhaustivo** de las **307 palabras distintas** de los 90 stems, revisando las 267 sin acento una por una y las 40 acentuadas al reves por si sobraba alguna. Fue el **unico** residuo. | Free / contenido es-CO | Cowork firma, CC ejecuta | Cosmetico y de baja urgencia, pero es texto en la cara del usuario. Barato: 1 palabra. |
| `[GAP-E2E-FLAKE-RESPOND-500-CONCURRENCIA]` | **NUEVO 2026-07-27 (observado durante la entrega 2 del CI).** Corriendo la suite local con `--workers=2` aparecio de forma intermitente `respond rejected <CODE> seq=N: HTTP 500` en el bulk-insert de respuestas. **Se dejo sin atacar a proposito** (decision de German): el paralelismo es deliberado —`playwright.config.ts` fija `workers: CI ? 2 : undefined`— y tocarlo para tapar un sintoma habria costado la señal. **Estado real: REPRODUJO EN CI** (2026-07-28, corrida `30368534563` — ver la actualizacion mas abajo, que es la que manda). `[historico, quedo obsoleto]` **NO reproducido en CI, en dos corridas.** La de la rama de #37 (`30310410855`) y la de `push: main` (**`30311737492`**, sobre `0010e89`, Node 24, `workers=2`) dan las dos **0 ocurrencias de `respond rejected` y 0 de `flaky`**. `Matiz que decide la lectura:` Playwright imprime `N flaky` cuando un **retry rescata** un test, asi que 0 significa **que no se disparo**, no que `retries: 2` lo absorbio. `Pendiente:` repetir el grep sobre la corrida de `push: main`; **es una muestra, no un cierre**. Si reaparece, investigarlo como posible defecto real de `/api/respond` bajo concurrencia, no como ruido del stack local. **>>> ACTUALIZACION 2026-07-28: REPRODUJO EN CI.** Corrida `30368534563` (PR #43): `Error: respond rejected ONET-IP-SF item seq=16: HTTP 500 {"error":"insert_failed"}` en `free-full-flow`, rescatado por retry (`1 flaky`). O sea que **"NO reproducido en CI en dos corridas" quedo obsoleto**: era una muestra chica, exactamente como decia la propia nota. `Lo que cambia:` ya no es hipotesis de stack local — el 500 sale del endpoint en el runner. **Toca tratarlo como defecto real de `/api/respond` bajo concurrencia**, que es lo que esta fila pedia hacer si reaparecia. El `insert_failed` con `seq` concreto sugiere colision en el bulk-insert de respuestas con `workers=2`. **>>> PREDICCION, ESCRITA ANTES DE QUE PASE:** al mergear **#42** este sintoma deberia aparecer **MAS seguido en `main`**, no menos. #42 enciende 3 tests que conducen un O*NET **completo** cada uno (`full-flow-onet` x2 + `anon-cannot-read` x1), o sea **~180 POST extra a `/api/respond`** con `workers=2` — la suite sube su volumen de respond del orden de **50-70%**. **Ese aumento es CARGA AGREGADA, NO un defecto de #42.** Se deja anotado para que la proxima sesion no vea el flake subir justo despues de #42 y se lo atribuya. `Ademas:` **#42 y #43 nunca corrieron juntos en CI** (cada uno se midio contra `main`); la primera corrida de `push: main` despues del segundo merge **es** ese test — mirarla. **>>> 2026-07-29 (#72): el cuerpo del 500 YA discrimina el modo de falla, sin reproducirlo.** `/api/respond` devuelve **dos** cuerpos distintos en 500 y significan cosas opuestas: `{"error":"insert_failed"}` (`route.ts:228`) es el upsert de `item_response` fallando — **la fila NO se escribio**; `{"error":"internal"}` (`route.ts:243`) es el catch final, alcanzable **solo despues** de un upsert exitoso, y lo unico que se awaitea en el medio es `advanceProgress(session_id)` (`:231`) — o sea **la fila SI se escribio** y lo que fallo es la contabilidad de progreso. `Correccion al registro:` la unica reproduccion documentada en CI (`30368534563`) trae **`insert_failed`**, asi que es el **primer** modo —colision en el upsert— y **no** el de progreso; cualquier nota que diga lo contrario esta mezclando los dos. Con #72 mergeado el log de CI ya imprime el cuerpo, asi que **la proxima aparicion se clasifica sola** sin instrumentar nada. **>>> Y se clasifico sola el mismo dia: el SEGUNDO modo ya tiene su primera observacion.** Corriendo el repro local de #75 (DB fresca, `--project=desktop-chromium --workers=2`, `E2E_LOCAL=1`) salieron **2 fallos con `respond rejected ONET-IP-SF seq=1` y `seq=10`, los dos con cuerpo `{"error":"internal"}`** — o sea **la fila SI se escribio** y lo que reventó fue `advanceProgress`, que solo hace un `count` sobre `item_response` y un `update` de `progress` sobre `assessment_session` (`lib/session/anonymous.ts:280-302`). **Los dos modos existen y son reales**; el de progreso apunta a esas dos operaciones bajo concurrencia, no al upsert. `Tercera firma, distinta de las dos anteriores, en el intento 1 del CI de #75 (run 30470225209):` **9 fallos, CERO `respond rejected`**, y el **re-run del commit identico paso 33/33** — no-determinista y no del cambio. **>>> CARACTERIZADA CON EL ARTEFACTO DE PLAYWRIGHT, contando los 27 `error-context.md`, no muestreando:** la firma es **"lo que el test escribio no esta al leerlo de vuelta"**, y se manifiesta de dos formas. **21 de 27:** falta el `report_snapshot` y `/reporte/:id` renderea **`heading "404"`** (`report_snapshot_not_found` x15 en el log del server es la otra cara de lo mismo). **6 de 27:** el progreso lee **cero completados** —la pantalla dice *"Te faltan 4"*— y `Continuar` apunta a `/test/BFI-2-S` cuando el test ya habia completado BFI y esperaba `/test/TwIVI`. `Lo que esto DESCARTA, y es lo util:` **no es lentitud del runner.** Un 404 es inmediato y definitivo, no un render que no alcanzo; los 6 `Test timeout of` son clicks **aguas abajo** del 404, no la causa. **>>> RESUELTA EN #78, Y LA HIPOTESIS QUE ESTA CELDA TRAIA ERA FALSA. <<<** Decia *"apunta a visibilidad de escrituras / aislamiento entre tests bajo `workers=2` sobre una sola DB compartida"* — **no es eso, y hay que retirarlo para que nadie persiga aislamiento de tests.** `La causa real:` **`next dev` (Turbopack) resuelve rutas BAJO DEMANDA, y bajo carga paralela una ruta que EXISTE puede contestar 404.** `Lo que tumbo la hipotesis de datos:` los 404 cubrian **`/me/delete/done` (6)** ademas de `/test/{code}/done` (46) — **una ruta que no toca scoring ni DB, en otro arbol y otro route group**, cuyo unico parecido es ser un `page.tsx` **hijo de otra ruta ya compilada**. Ninguna explicacion de datos sobrevive a eso. `Y la cadena cierra con los numeros:` sin `/done` no corre `scoreCompletedSessionIfNeeded` -> no hay `report_snapshot` -> los 15 `/reporte/:id` en 404, que es **exactamente** el conteo de `report_snapshot_not_found`, mas el progreso en cero. **Un solo mecanismo explicaba las dos formas que esta celda registraba como una firma con dos caras.** `El fix (#78):` el E2E corre contra **`next start`**, que sirve un manifest **completo** antes de aceptar trafico —el build lista `/test/[code]/done` explicitamente— asi que "la ruta todavia no esta registrada" **deja de ser un estado posible**. No se subio timeout ni retry: se elimino la condicion. `Verificado por el camino exacto de CI (`CI=1`):` **33 passed, 0 requests a `*/done` en 404**, y el E2E baja de ~2m a **1.2m**. `Metodo, para la proxima:` **`gh run download <run> -n playwright-test-results` ANTES de grepear el log.** Ese artefacto existe desde **#59** (`ci.yml:179-184`, `test-results/`, 14 dias); en este caso **una lectura de `error-context.md` dio la respuesta que 5140 lineas de grep no dieron**, porque trae el snapshot YAML de la pagina. | Transversal / proceso (CI) | CC | **Estado 2026-07-29: la 3a firma (la de `*/done` en 404) queda CERRADA por #78. Los DOS modos de `respond` siguen abiertos** —`insert_failed` (`route.ts:228`, la fila **no** se escribio) e `internal` (`:243`, **si** se escribio y revento `advanceProgress`)— y **#78 no los toca**: son concurrencia real sobre el endpoint, no resolucion de rutas. `Lo que esta fila enseño sobre metodo:` tuvo **dos hipotesis mias, las dos falsas** —"el scoring es best-effort y hay race" (falso: `/done` **awaitea** el scoring) y "aislamiento entre tests" (falso, y alcanzo a mergearse en #77)—. **A las dos las tumbo el mismo dato: `/me/delete/done`**, el caso que no encajaba. **Buscar el caso que NO encaja antes de escribir la hipotesis.** Sigue en pie no subir `retries` para tapar nada. `MEDICION NUEVA 2026-07-29 PM-22, y sube la severidad:` el modo **`internal` sobrevive a `next start` y NO es absorbible por los retries.** Mismo commit, tres corridas: sin `CI` (`next dev`, retries 0) -> **1 `insert_failed` + 1 `internal`**; con `CI=1` (`next start`, retries 2) -> una corrida **33/33 limpia** y otra con **5 x `internal`, 0 x `insert_failed`**, **agotando los 2 reintentos** en `full-flow-onet.spec.ts:159`. Conteo con `grep -c` sobre los `error-context.md`, no muestreo. **O sea que este flake puede tirar el gate**, no solo ensuciarlo. Es intermitente: 2 de 3 corridas con `next start` pasaron limpias, y el CI de #79 tambien. `Lo que NO se puede afirmar aun:` que `insert_failed` sea exclusivo de `next dev` — **se vio una sola vez**. `El proximo paso NO es otra hipotesis, es contar:` N corridas por configuracion, con el conteo por modo separado. `Y una hipotesis mia mas que murio en el intento, la tercera:` registré que `next start` los eliminaba, basado en **una** corrida verde. **Un verde es una observacion, no un mecanismo.** |
| ~~`[GAP-E2E-SKIPS-E2E-LIVE]`~~ **CERRADO 2026-07-28 (PR #42)** | **El gate pasa de `25 passed / 8 skipped / 0 failed` a `33 / 0 / 0`.** `El enunciado de este flag estaba mal, y vale registrar en que:` decia "`E2E_LIVE=1` mas un scaffold", que son **6 de 8**. Eran **TRES** compuertas y la tercera no figuraba en ningun documento: **5** `account-delete` (`E2E_LIVE`), **2** `full-flow-onet` (**`E2E_REPORT_SESSION_ID`**), **1** scaffold `test.skip(true)`. La aritmetica es la prueba: `DATABASE_URL` **si** esta seteado en CI, asi que ese describe aporta 1; si faltara, el total daria 9. `El veredicto no fue el binario que planteaba el flag:` las dos env vars no existen fuera de los propios specs y eran **vestigiales** — nombraban una precondicion que los specs no podian satisfacer (forzadas: **7 rojos, los 7 en `/signup`**; ningun spec tenia auth). Tampoco estaban muertos: las 4 rutas `/me/*` y todas las anclas de copy siguen vivas. Se **cablearon** a `hasLocalAuth()` + las fixtures que ya existian. | Transversal / proceso (CI) | CC | Cerrado. Dejo dos hallazgos propios, abajo. La leccion: "cobertura declarada y no ejercida" no es solo que no corra — es que **lo que vigilaba puede haberse caido sin que nadie lo note**. |
| `[GAP-ASSEMBLER-COMENTARIO-RADIO-STALE]` | **NUEVO 2026-07-27 (notado implementando ADR-036, NO tocado).** El comentario del paso 13 de `lib/report/assembler.ts` (~L583) dice que el circumplejo dibuja "4 sectores sobre dos ejes bipolares con **radios CENTRADOS por MRAT**". Es **falso desde ADR-034**: el radio dejo de ser el centrado y pasa a ser la proporcion de la media HOV **cruda** contra la escala fija del instrumento. La parte que sigue siendo cierta (y la que justifica el comentario) es que se reconstruyen los 4 HOV desde las medias de valor. No se corrigio en PR #29 por §1.3 (adyacente al cambio, no parte de el). **Vale la pena por que:** es exactamente el patron que ADR-036 identifica como la causa raiz — un comentario autoexplicativo y plausible que afirma algo sobre los datos y que nadie verifico. | Reporte Free | CC | Trivial: una frase. Se puede plegar a cualquier PR que toque `assembler.ts`. |
| `[GAP-FICHA-QUE-MIDE-PREFIJO-DOBLE]` | **NUEVO 2026-07-27 (smoke PR #24/#26).** El reporte de Intereses rinde literalmente **"Que mide: Que mide: ficha tecnica en preparacion para este instrumento."** El fallback de `lib/report/assembler.ts:529` ya trae el prefijo "Que mide: " dentro del string, y `FichaTecnica.tsx:71` renderiza ademas su propio `<span>Que mide: </span>`. Solo se ve en O*NET por ser el unico sin `what_it_measures` sembrado. **Fix:** quitar el prefijo del string del fallback (queda igual de correcto con la etiqueta del componente). **Sembrar el texto de O*NET oculta el sintoma pero NO cierra esto** — reaparece en cualquier instrumento futuro sin ficha. | Reporte Free / ficha tecnica | CC | Se cierra junto con `[GAP-FICHA-WHAT-MEASURES-ES-CO]`. Ref: `estado/SMOKE_PR24_PR26_RESULTADOS_v1.0.md` §2.3. |
| `[GAP-MAGIC-LINK-RESEND-COOLDOWN-THROTTLE]` | **NUEVO 2026-07-27 (observado conduciendo el smoke).** El cooldown de 30s del boton "Reenviar enlace" (`ResendButton.tsx:63-67`) son 30 `setTimeout` encadenados de 1s. Chrome estrangula los timers en pestanas OCULTAS (~1/min), que es exactamente donde queda la pestana mientras el usuario va a su bandeja a buscar el correo: el boton sigue inhabilitado mucho mas de 30s (medido: >32s sin habilitarse). El diseno (blueprint §7.3.1) pide "sin cuenta regresiva visible", no "sin reloj confiable". **Fix:** guardar un timestamp de referencia y comparar contra `Date.now()` (o `visibilitychange` para re-sincronizar), en vez de decrementar por tick. | Auth / UX | CC | Cosmetico pero se paga justo en el momento de mas ansiedad del funnel. |
| `[FIX-ROADMAP-COUNT-37-38]` | `.planning/ROADMAP.md` L373 reporta "37" requirements para Phase 1 en tabla de distribucion; lista enumerada L31 contiene 38 IDs. Cosmetico, no afecta planning/execute. | Transversal | CC | Detectado por plan-checker durante plan-phase 1 (no bloqueante). |
| `[GAP-MAIN-BRANCH-PROTECTION-BYPASS]` | **Descubierto 2026-06-10 (push a main):** el remoto tiene branch protection en `main` ("Changes must be made through a pull request"); el `git push origin main` directo se **bypasseo** con los permisos de admin de German (`remote: Bypassed rule violations`). Funciona, pero el flujo "oficial" del repo es via PR. **Decision pendiente:** adoptar PR para `main` (CI gate + review antes del prod deploy) o mantener push directo por velocidad MVP. | Transversal / proceso | German + CC | No bloquea; higiene de proceso. Con prod ya cableado a `main`, un PR-gate evita deploys prod accidentales. |
| ~~`[GAP-RETURNING-USER-RESIGNUP-AGE]`~~ | **DUPLICADO — ver la fila autoritativa en P0 (cerrada, PR #23 mergeado).** `Vale conservarla:` esta fila de 2026-06-10 tenia **el diagnostico Y el fix correctos** y estaba clasificada P3; el error fue la severidad, y por eso el lockout total vivio en prod desde la Fase 1. `[historico]` **Descubierto 2026-06-10 (verify retest, `/?error=age`):** un usuario YA onboarded que vuelve a hacer signup con el MISMO email obtiene `/?error=age` (confuso — no es menor de edad). Causa confirmada en PROD: el callback exitoso previo limpia `dob_pending` (paso 9), y `signInWithOtp({options:{data}})` **NO actualiza** el `user_metadata` de un usuario existente (solo lo setea al crear el user). El callback lee `dob_pending=null` => falla T-01-07-02 => error=age. ~~En producto es edge (los usuarios firman una vez)~~ **[ESA LECTURA ERA EL ERROR — ver fila P0, 2026-07-27: no es un edge de re-signup, es que NO EXISTE login de retorno]**, pero el error enganha + **quema cada email de prueba tras un callback exitoso** (testing: usar alias `+` de Gmail, un `+rN` por retest). **Fix futuro:** en el callback, si `dob_pending` falta PERO el user ya tiene consent activo + user row, tratarlo como login de retorno (redirect a su reporte/home) en vez de error=age. **[Este fix propuesto es EXACTAMENTE el que implementa PR #23; el diagnostico de junio era correcto, la severidad no.]** | Fundacion / Fase 1 (UX returning user) | CC | **ELEVADO a P0 el 2026-07-27; cerrado en codigo por PR #23.** |
| `[GAP-CONSENT-VERSION-BUMP-REGRANT]` | **Descubierto 2026-06-10 (advisor durante fix de `[BUG-CALLBACK-NOT-IDEMPOTENT]`):** el swallow del `23505` en el callback asume idempotencia *de la misma version*. El indice parcial `consent_user_product_active_idx` es sobre `(user_id, product_code)` e **ignora `consent_version`**. Si en el futuro se bumpea `CONSENT_VERSION` (hoy `1.0.0`), un usuario con consent activo de la version vieja hara que el INSERT de la version nueva tire 23505 => se traga silenciosamente => el nuevo consent NUNCA se registra y el usuario queda con la version vieja sin que nadie lo note. **Fix cuando se bumpee la version:** ruta explicita de re-consent (revocar el activo viejo + insertar el nuevo, o comparar `consent_version` del row activo y forzar regrant si difiere). NO construir ahora (zero usuarios, version unica). | Fundacion / Fase 1 (deferred) -> cualquier fase que bumpee consent | CC | Correcto para el bug actual; trampa latente para el primer version bump. |
| `[ACCEPTED-RISK-UPSTASH-GLOBAL]` | Upstash Redis aprovisionado en Plan 01-01 quedo tipo **Global** (no Regional) por defecto del UI de Upstash en cuentas nuevas free tier — Regional ya no se ofrece como opcion visible. Funcionalmente equivalente para nuestro rate limit (counter increment + TTL), con +10-30ms de latencia por overhead de replicacion cross-region. Free tier identico (10K cmd/dia). | Fundacion / Fase 1 (live), revisitable Phase 3+ | German + CC | Aceptado para MVP. Migration path: si Phase 3+ muestra latencia issue en magic-link hot path, Upstash permite export/import a una instancia Regional sin downtime mayor. |
| `[GAP-VALUECIRCLE-THIN-WEDGES]` | **Detectado en deploy-smoke ADR-034 (2026-07-24, prod).** El circumplejo TwIVI ya dibuja las 4 direcciones con radios reales (fix de la aguja, PASA), pero las cuñas son finas (`sectorPoints` half-width `Math.PI/10`, geometria heredada de PR-C/#17) → se lee como estrella-fina de 4 puntas, no como cometa rellena. No afecta la lectura de dominancia ni la banda. **Tweak si Cowork quiere un cometa mas "gordo":** subir el half-width de la cuña (aparte del radio, ya firmado en ADR-034). | Reporte Free / visual | CC + Cowork | No bloquea; decision estetica de Cowork. Cosmetico, preexistente (no del pase visual). |
| `[GAP-VALUECIRCLE-LABEL-CLIP]` | **Detectado en deploy-smoke ADR-034 (2026-07-24, prod).** Las etiquetas horizontales del circumplejo ("Destacar" izq., "Aportar" der.) se recortan ~1 char por su posicion (`MAX_RADIUS + 18` = x 12/188 en viewBox 0-200 con `text-anchor:middle` → clip en el borde). El texto esta COMPLETO en el DOM (a11y intacta); es solo clip visual. **Fix:** ampliar el viewBox horizontal o anclar las etiquetas laterales hacia adentro. | Reporte Free / visual | CC | No bloquea; cosmetico, preexistente. |
| `[GAP-PERMA-BARS-SENSITIVE-GROUPING]` | **Pulido opcional de D2 (firma Cowork del pase visual, 2026-07-24).** Con el ancho por banda, "Emociones dificiles"/"Soledad" en banda Alto rinden las barras mas largas (perfil de cuidado). Cowork APROBO el tratamiento actual (largo=banda + gold calmo + intro-reframe) como suficiente y RECHAZO el tope de ancho menor (paternalismo + rompe largo=banda). Pulido opcional NO requerido: agrupar H/hap/N/Lon bajo subtitulo "Señales adicionales" (separadas de los 5 pilares) y/o linea inline junto a las 2 sensibles. Copy firmado si se usa: "Describe como te sientes ahora, no quien eres." | Reporte Free / PERMA / etica | Cowork + CC | No bloquea (aprobado como esta). Solo si se decide reforzar la contencion visual. Ver ADR-034 adenda. |
| `[GAP-REPORT-FICHA-NAME-JOIN]` | **NUEVO 2026-07-24 (descubierto verificando el data path del fix de `[GAP-REPORT-INTERESES-MISLABEL]`, PR #20).** La query de la ficha del instrumento en `assembler.ts` hace `.from("instrument").eq("id", session.instrument_version_id)` → compara `instrument.id` con un `instrument_version_id` (IDs de tablas distintas) → casi siempre null → **el nombre propio de la ficha cae siempre al generico "Instrumento de autoconocimiento".** Preexistente, **fuera del scope del mislabel** (ese fix rotula la CATEGORIA por `code` — Personalidad/Intereses/Valores/Bienestar; esto es el NOMBRE de la ficha, otra superficie). Arreglarlo cambia comportamiento visible de la ficha, por eso no tocado en PR #20. **Fix:** unir por el FK correcto — `instrument_version_id` → `instrument_id`, o embed `instrument_version!inner(instrument!inner(name))` (mismo patron que el mislabel ya introdujo en el select del `iv`). | Reporte Free / ficha | CC | No bloquea; nombre generico en la ficha, preexistente. Ref: PR #20. |

---

## P2/P3 — Fase 7 (Legal & Licencias) — diferido por diseno

| Pendiente | Instrumentos | Owner | Nota |
|---|---|---|---|
| Cierre de licencia comercial | BFI-2 (Soto-John), VIA-IS-P (VIA Institute), PVQ-RR/PVQ-21 (Schwartz), WDQ-40 (cadena APA/autores/editor), Ikigai-9 (Imai et al.) | German + Cowork (pitch) | Cada uno con plan-B abierto listo |
| Confirmar uso comercial de los abiertos | PERMA, SWLS, Ryff, PANAS, MLQ, WAMI, CMWS, UWES-9, BPNSFS, FSS-9, MEMS, CFI-R, PGI | Cowork | Uso academico amplio; confirmar comercial |
| Revision legal Ley 1581 | Toda la plataforma (firmada antes de GA Free) | German + asesor legal | Privacidad-by-design ya continua desde fase 1 |
| Presupuesto de licencias | Cash flow | German | Costos por titular |

---

## Riesgos abiertos (seguimiento)

| ID | Riesgo | Estado |
|---|---|---|
| R-01 | Licencia de valores (todas las formas Schwartz requieren permiso; plan-B debil) | Vivo; gestionar en fase 7 |
| R-03 | Ikigai-9 sin adaptacion es-CO validada | Dossier+pack listos; adaptacion pendiente (P1) |
| R-04 | Experiencia "magia" no se logra (efecto Barnum) | Mitigar con piloto temprano + integrador |
| R-05 | Riesgo cultural Ikigai (Venn != ikigai japones) | Disclaimer especificado en dossier/pack Ikigai-9 |

---

## Hecho recientemente (para contexto)

- 2026-06-05: reinicio v2.0 (docs funcionales, UX spec, arquitectura). Carpetas sin prefijo numerico. PVQ-21 y Ikigai-9 (dossier+pack). Ver `estado/STATUS.md`.
- 2026-06-05: `/gsd-new-project` ejecutado (init GSD, 8 artefactos en `.planning/`). Ver ADR-001..ADR-005.
- 2026-06-05: `/gsd-discuss-phase 1` completo. 32 preguntas en 4 areas, 28 decisiones lockeadas. CONTEXT.md + DISCUSSION-LOG.md en `.planning/phases/01-*/`. Ver ADR-006 (AWS KMS).
- 2026-06-05: `/gsd-plan-phase 1` completo (auto-chain). RESEARCH (2219 ln) + VALIDATION + UI-SPEC (6/6 dim PASS) + PATTERNS (~95 archivos clasificados) + 12 PLAN.md + SKELETON.md. plan-checker: PLANS APPROVED, 38/38 REQ IDs cubiertos. 2 gates cerrados (`[GAP-AUTH-HOOK-API]`, `[GAP-RLS-JSONB]`); 1 ASSUMED (`[GAP-TAILWIND-V4-COMPAT]` cierra con ADR-008 en Wave 0).
- 2026-06-06: Plan 01-01 Task 1 (checkpoint humano provisioning) cerrado. 6 servicios externos PROVISIONED (Supabase us-east-1, Vercel descubreme-gsd con OIDC Team mode, AWS descubreme-prod con KMS+IAM role+OIDC trust, Resend descubreme.co domain verified, Upstash Redis Global us-east-1, Sentry descubreme/descubreme-web). `[GAP-AWS-KMS-SETUP]` CERRADO. Nuevo `[ACCEPTED-RISK-UPSTASH-GLOBAL]` P3 anotado. Plan 01-01 Task 2 entrego `.env.example`, README "Phase 1 Quickstart", `.gitignore` hardening, STATUS+BACKLOG actualizados.
- 2026-06-07: Wave 6 cerrada (Plan 01-09 + Plan 01-10). Plan 01-09: reporte O*NET completo (Capa 1+2+3 + hexagono full + ficha tecnica + footer NFR-27 + survey + waitlist + email Resend transactional + Sentry beforeSend full PII scrub headers `authorization|cookie|x-api-key|proxy-authorization` + url query `token|access_token|refresh_token|code|key|api_key` + drop request.data). 22 archivos. FOUND-02/04 + QUAL-01/02 + COMPL-11/12/13/14/18 cubiertos. Security review (medium) flageado tras commit 7a30e40 — Sentry scrub incompleto headers/url/body; remediado en commit 99e9cf6 con +13 tests. Plan 01-10: 4 derechos titular Ley 1581 + UI account ≤2 clicks. supabase/migrations/009_anonymize_user_audit.sql SECURITY DEFINER + search_path hardening + GRANT EXECUTE service_role only — desbloquea UPDATE/DELETE en audit_log (REVOKE+immutable trigger del 004) para anonimizacion D1.5. 15 archivos. COMPL-05/06/07/08/09/10/17 cubiertos. Suite test crece de 110→153 passed (+43) sin regresiones. Nuevos items P1+P2 al backlog: `[BUG-PII-STORAGE-PLAN-07]` (schema PII bytea insuficiente para reconstruir EncryptedField; GET /me/data degrada DOB/name a null), `[GAP-DELETE-ATOMIC-TX]` (DELETE flow no transaccional cross-schema; orphan auth.users rows posibles tras fallo entre db.transaction y supabase.auth.admin.deleteUser; fix deferred a Plan 01-12 via SECURITY DEFINER `delete_user_account`).
- 2026-06-07: Wave 7 cerrada (Plan 01-11). Sentry runtime ACTIVO en 3 runtimes (instrumentation.ts + sentry.{client,server,edge}.config.ts importando sentryBeforeSend Plan 01-09 con cast estructural `as any` documentado — contrato Plan 01-09 intacto, COMPL-14 ACTIVO runtime no solo unit-tested). 4 CI lint gates extended al scope final Phase 1: FOUND-05 (no-hardcoded-instruments cubre 12 dirs final), FOUND-06 (plugin-swap idempotente con git-status snapshot equality MOCK-PREF-12), COMPL-18 (prohibited-phrases extended a db/seeds/occupations graceful-skip), UI-SPEC §8.4 stability contract (NUEVO no-hardcoded-strings.test.ts heuristica conservadora JSX attrs + prosa espanola >=2 palabras). 4 aria-labels existentes movidos a MC_* keys (deviation Rule 2 auto-fixed inline). 13 archivos (4 nuevos + 9 modificados). Suite 159 passed/26 skipped/3 todo (+6 vs Wave 6, 0 regresiones). 6 commits 780650e..793cc1f. Task 2 Cowork checkpoint DEFERRED end-of-phase per `human_verify_mode`. Nuevo item P2: `[GAP-NPM-AUDIT-PHASE7]` (13 vulns transitivas del install @sentry/nextjs; revisar antes deploy Phase 7).

`Nota:` cuando exista `estado/CHANGELOG.md` y `DECISIONS_LOG.md`, el historico detallado y los ADR migran alli; este bloque queda solo como puntero corto.
