# BACKLOG — DescubreMe (priorizado)

**Owner:** German Velez Hurtado.
**Ultima actualizacion:** 2026-06-12 (Claude Code — **PHASE 1 VALIDADA EN PRODUCCION. Browser smoke prod PASS (German): flujo completo + magic link OK en `descubreme.co`.** `[GAP-PROD-DOMAIN-WWW-CANONICAL]` CERRADO — el redirect apex->www no rompe el PKCE (308 preserva `?code=`; cookie code_verifier host-only en www donde aterriza el callback). No se cambio nada en prod; www-alignment descartado por innecesario. Quedan diferidos no-bloqueantes: `[GAP-MAGIC-LINK-EXPIRED-PREFETCH]`, `[GAP-RETURNING-USER-RESIGNUP-AGE]`, `[GAP-REPORT-OCCUPATIONS-DEPTH]` (Fase 2-3), `[GAP-MAIN-BRANCH-PROTECTION-BYPASS]` (proceso). Proximo: decision GSD — `/gsd-ship 1` (cierre formal) o arrancar Phase 2 (B2C Free). `[historico]` 2026-06-10 — **`/gsd-verify-work 1` COMPLETO 20/20.** Test 16 (delete) cerrado: `[GAP-DELETE-AUDIT-DIGEST-SEARCHPATH]` CERRADO (mig 013 a PROD, verificacion DB COMPL-07/09). Merge `verify/phase-1-deploy-strict` -> `main` = fast-forward LOCAL hecho; **push a origin/main HECHO + deploy Production READY** en `descubreme.co` (19 env vars provisionadas en Production). Pendiente manana: browser smoke del magic link en prod (ojo redirect apex->www, `[GAP-PROD-DOMAIN-WWW-CANONICAL]`). `[historico]` **`/gsd-verify-work 1` reanudada. `[GAP-UI-QUALITY-PREVIEW]` CERRADO via Fix 5 (ADR-021)**: causa raiz = colision de namespace Tailwind v4 `--spacing-{tshirt}` shadowea `--container-*` → colapso global de layout. Solucion Opcion B (raiz): escala 8pt named→numerica + Inter via next/font. 28 archivos, verificado en CSS compilado + render local. Commit en branch `verify/phase-1-deploy-strict`; merge a main tras tests 2-20. Proximo: continuar tests 2-20 del UAT contra preview reconstruido.
**Prioridad:** P0 (bloquea ahora) · P1 (prerequisito de una fase cercana) · P2 (necesario, no inminente) · P3 (deseable / futuro).
**Owner del item:** Cowork (research/producto) · CC (Claude Code, implementacion) · Adaptacion (proceso ITC con humanos) · German (decision/negociacion).

> Consolida las "carries" dispersas en los documentos. Si descubres un gap nuevo, anadelo aqui con su flag `[GAP-...]`. Fuente de verdad de pendientes del proyecto.

---

## P0 — Bloquea ahora

- (vacio) Ningun item bloquea el arranque de GSD.

---

## P1 — Prerequisito de una fase cercana

| Flag | Pendiente | Producto/Fase | Owner | Nota |
|---|---|---|---|---|
| ~~`[GAP-UI-QUALITY-PREVIEW]`~~ | **CERRADO 2026-06-10 (Fix 5, ADR-021):** causa raiz unica = colision de namespace Tailwind v4 `--spacing-{tshirt}` shadowea `--container-*`, colapsando `max-w-*`/`w-*` (max-w-3xl→64px, max-w-xs→4px). De las hipotesis: (a) era correcta en direccion pero el mecanismo era namespace-collision, no "residuo"; (b) confirmada como issue secundario real (Inter nunca cargada) y arreglada; (c)/(d) NO eran la causa. Solucion Opcion B (raiz): migracion escala 8pt named→numerica + borrado de tokens `--spacing-*`/`--container-*` + Inter via next/font. Verificado en CSS compilado + computed styles + render local (landing+BYS) + typecheck. 28 archivos. | Fundacion / Fase 1 verify-work | — | Resuelto. Branch `verify/phase-1-deploy-strict`; merge a main tras cerrar tests 2-20. |
| ~~`[GAP-VERIFY-TESTS-2-20-PENDING]`~~ | **CERRADO 2026-06-10: 20/20 tests PASS (Test 16 el ultimo en cerrar).** 18 tests de `01-UAT.md` ejecutados tras resolver UI quality: BYS visual + 60 items O*NET + pause-resume + done page hexagono + signup dual consent + magic link + callback claim + reporte 3-capas + email transactional + survey + waitlist + 4 derechos Ley 1581 + 2 technical checks + 2 coverage checks. | Fundacion / Fase 1 verify-work | CC + German | UAT.md tiene tests definidos. Reanudar via `/gsd-verify-work 1` que detecta el UAT activo. |
| ~~`[GAP-VERIFY-BRANCH-MERGE-TO-MAIN]`~~ | **RESUELTO 2026-06-10: merge fast-forward + PUSH a origin/main HECHO + deploy Production READY (`dpl_HDaUnC8...`) en `descubreme.co`/`www.descubreme.co`.** `[historico]` merge = FAST-FORWARD LOCAL hecho (27 commits, 0 divergencia con main). PUSH a origin/main PENDIENTE: dispara deploy Production (dominio `descubreme-gsd-git-main-*` existe); env vars scoped a Preview no Production + Supabase Auth prod (Site URL/SMTP/NEXT_PUBLIC_APP_URL) sin replicar => aprovisionar Production antes de pushear, o aceptar primer deploy prod roto. Reversible con `git reset --hard` mientras no se pushee.** `[historico]` Decidir si los 3 commits de `verify/phase-1-deploy-strict` (`a2d4142`, `2ae3740`, `9542672`) se mergean a `main` ahora O tras completar los 18 tests pendientes. Mergeo ahora: `vercel.json` + Tailwind v4 fix + Next.js 16 cookie fix llegan a main pero verify-work no esta cerrado. Mergeo despues: branch crece y posible drift con main. | Fundacion / Fase 1 | German | Decision post UI quality fix. |
| `[GAP-VERIFY-PROD-WRITE-IRREVERSIBLE]` | 11 migrations + 8 seeds ya estan aplicados a PROD Supabase tzhhqaducmbxfebuyvnv via MCP (sesion 2026-06-08 PM). No rollback automatico. Si futura migracion 012+ requiere down migration de 010 o 011, hay que escribir SQL manual. | Fundacion / Fase 1 onwards | CC + German | Documentar en deploy runbook para Phase 2. |
| `[GAP-AUTH-URL-CONFIG]` | **Descubierto 2026-06-10 (verify Test 8-9):** el magic link de signup redirige a `localhost:3000` (con `otp_expired`) en vez del origen deployado. Doble causa: (1) `NEXT_PUBLIC_APP_URL` no estaba en el env de Vercel Preview → `emailRedirectTo` quedaba relativo (`/auth/callback`) → Supabase lo descarta y cae al **Site URL** del proyecto, que es `localhost:3000` (default). (2) Supabase Auth **Site URL + Redirect URLs allowlist** no configurados para los entornos deployados. **Fixes aplicados 2026-06-10:** (1) strip de trailing slash en `actions.ts` + `NEXT_PUBLIC_APP_URL` agregada a Preview (branch alias); (2) usuario configuro Supabase dashboard (Site URL + Redirect URLs allowlist con `/auth/callback`); (3) **bug latente descubierto al llegar bien el redirect: 404** — el callback handler vivia en `app/(auth)/callback/route.ts` y el route group `(auth)` quita `auth` del path, asi que servia `/callback`, no `/auth/callback`; movido a `app/auth/callback/route.ts`. **Pendiente:** retest E2E del usuario (link fresco) que confirme callback → sesion → `/reporte`. **PROD necesita la misma config Supabase + `NEXT_PUBLIC_APP_URL` en env Production.** | Fundacion / Fase 1 verify + deploy prod | CC (codigo, hecho) + German (Supabase dashboard, hecho) | Bloqueaba Test 8-9. Codigo + config listos; falta retest E2E. PROD: replicar config. |
| ~~`[BUG-RESPOND-DUP-CLAIM]`~~ | **CERRADO 2026-06-10 (Fix durante verify Test 9, commit `7a35f69`):** `/api/respond` hacia `INSERT` plano (no upsert pese al comentario "upsert shape") => re-responder un item Likert creaba filas `item_response` duplicadas (user_id NULL). Al firmar, `claimAnonymousSession` seteaba user_id en todas a la vez => violaba el indice unico parcial `(user_id, item_id)` (mig 002) => callback caia a `/?error=signup`. Confirmado por runtime logs (err con claim+item_response+violates). **Fix:** `/api/respond` upsert onConflict(session_id,item_id) (last answer wins) + migracion 012 (dedup existentes keep-latest + UNIQUE INDEX `item_response_session_item_uniq`). Migracion aplicada a PROD via conexion directa (DELETE 10 dups => 51 filas limpias, indice creado). Habria roto signup para cualquier usuario real que cambie una respuesta. | Fundacion / Fase 1 verify | — | Resuelto. PROD: schema actualizado (012 aplicado manual via psql, no registrado en schema_migrations — idempotente). |
| ~~`[BUG-CALLBACK-NOT-IDEMPOTENT]`~~ | **CERRADO 2026-06-10 (fix idempotente + test):** el consent insert ahora trata el `23505` sobre `consent_user_product_active_idx` como exito (esa es la unica unique index de la tabla consent => un 23505 ahi solo puede ser "consent activo ya existe"). **Swallow scoped al call site del consent insert, NUNCA al catch externo** — un 23505 del claim sobre `item_response (user_id,item_id)` sigue cayendo a `/?error=signup` (no se corrompe el claim en silencio). Lockeado con `tests/unit/auth/callback-idempotent.test.ts` (3 casos: happy-path, idempotent-23505, scoping-guard-23502; RED->GREEN verificado). Typecheck limpio. **Pendiente confirm runtime:** retest con gervel33 (su consent activo residual ES el caso de repro real — el happy path con email fresco no ejercita el swallow) => el runtime log ya NO debe mostrar `consent insert ... violates`; ojo que gervel33 puede tirar en el paso de claim por estado residual (error distinto, no regresion). **Contexto original (ABIERTO 2026-06-10, verify Test 9, confirmado runtime logs 17:33):** el callback `/auth/callback` no es idempotente. El `INSERT` de consent (paso 6) re-corre en cada click del magic link; si el usuario ya tiene consent activo (de un intento parcial previo, como gervel33), viola el indice unico parcial `consent_user_product_active_idx` `(user_id, product_code) WHERE revoked_at IS NULL` => callback `catch` => `/?error=signup`. **Fix propuesto:** consent insert idempotente (upsert ON CONFLICT (user_id, product_code) WHERE revoked_at IS NULL DO NOTHING/UPDATE, o check-existing-active antes). Idealmente revisar todo el callback para idempotencia (user upsert ya es idempotente onConflict id; claim ya es no-op-safe; consent es el unico no-idempotente). **Data:** gervel33 tiene user+consent+sesion-sin-claimear residuales en PROD de intentos parciales; limpiar O testear con email nuevo sin estado previo. | Fundacion / Fase 1 verify | CC | Bloquea Test 9 para usuarios con estado previo. Real para double-click / retry. |
| `[GAP-MAGIC-LINK-EXPIRED-PREFETCH]` | **ABIERTO — evidencia de prefetch REFORZADA 2026-06-10 (retest post-fix-Bug1):** gervel33@**hotmail** (Outlook/Microsoft Safe Links, prefetcher agresivo de links) → "tu link ya expiro"; pruebasgvhv1@**gmail** con click inmediato → atraveso el callback completo (sesion claimeada OK en PROD). El diferencial Hotmail-falla vs Gmail-inmediato-pasa es el patron clasico de prefetch del scanner consumiendo el link de un solo uso. Confianza alta = prefetch (Outlook Safe Links es el culpable de manual). **NO construir interstitial hasta que el camino al reporte funcione (Bug 3/4).** **Contexto original (verify Test 8, email pruebasgvhv1):** `exchangeCodeForSession` falla con `error=expired` ("El link no es valido / ya se uso") en links frescos. Hipotesis: el scanner/prefetcher del cliente de email (Gmail) pre-visita el link de un solo uso y consume el codigo PKCE antes del click humano; o mismatch de cookie code_verifier (browser distinto). **Fix a investigar:** flujo `verifyOtp({type, token_hash})` en vez de `?code=` PKCE (mas robusto a prefetch), o interstitial "click para continuar" que dispare el exchange solo en interaccion humana, o config de Supabase. Conocido gotcha de magic links + email scanners. | Fundacion / Fase 1 verify + deploy | CC + German | Bloquea Test 8-9 en emails con prefetch. Workaround temporal: pegar el URL del link manual / click inmediato. |
| `[GAP-REPORT-SCORING-NOT-TRIGGERED]` | **FIX APLICADO 2026-06-10 (codigo + tests, pending retest E2E):** extraido `lib/scoring/score-session.ts` (`scoreSession(admin, sessionId)`, pasos 4-14 de /api/score) reusado por el route + el callback; cableado **best-effort en el callback post-claim** (try/catch propio, log-no-throw — lockeado con 2 tests "scoring jamas rompe auth"). `/api/score` ahora delega en scoreSession (mismos error codes/status). Completeness por `count(distinct responses) >= item_count`. Pendiente: retest E2E con sesion fresca + completa => /reporte renderiza. **Descubierto 2026-06-10 (verify retest, /reporte 404):** el snapshot del reporte NUNCA se genera en el flujo real. `/api/score` solo persiste `report_snapshot` cuando `session.user_id != null` (post-claim), y **nadie lo invoca** (solo aparece en comentarios; grep = 0 callers). El callback redirige a `/reporte/{id}` sin generar el snapshot => `composeReport` tira `report_snapshot_not_found` => `notFound()` => 404. Confirmado en PROD: sesion 1426aad2 claimeada OK pero `snapshots=0`. **Fix (advisor):** extraer `scoreSession(admin, sessionId)` (pasos 4-14 de /api/score) reusable por el route + el callback; llamarlo **best-effort en el callback POST-claim** (try/catch propio, log-no-throw — el scoring NUNCA debe romper el auth) antes del redirect. Follow-up robusto: generate-if-missing en /reporte (Test 11 email linkea ahi + Gmail prefetchea) con unique index en report_snapshot(session_id) + swallow violation (patron Bug 1). | Fundacion / Fase 1 verify | CC | **Bloquea Tests 9-10 (la "magia" del reporte). El happy-path al reporte nunca estuvo cableado E2E.** |
| `[BUG-PROGRESS-DRIFT-ON-REANSWER]` | **FIX APLICADO 2026-06-10 (codigo + test, pending retest E2E):** `advanceProgress` ahora setea progress = `count(distinct item_response)` (no progress+1 por submit) => idempotente bajo re-answer, getNextItemForSession no salta items. `scoreSession` gatea completitud por count distinto (no por el contador progress). Lockeado con test count-based en anonymous.test.ts + gate test en score-session.test.ts. Pendiente: retest E2E (sesion fresca, sin reusar cookie vieja). **Descubierto 2026-06-10 (verify retest):** `/api/respond` hace `upsert` item_response y luego `advanceProgress` (progress+1) **incondicional**. Re-responder un item (upsert UPDATE, sin fila nueva) igual incrementa progress => progress cuenta submits, no cobertura distinta. Confirmado en PROD: sesion 1426aad2 con progress=60 pero **59 respuestas distintas** (dim C=9). **En critical path:** el scoring suma C1..C10; con C10 ausente, `score()` rompe en la dim corta. **Fix (advisor):** juzgar completitud por `count(distinct item_response) >= item_count` (no el contador progress) + no incrementar progress en upsert-UPDATE (solo en insert nuevo). NO hacer `score()` tolerante a items faltantes (jamas puntuar data psicometrica incompleta). | Fundacion / Fase 1 verify | CC | Critical path para scoring E2E. Recurre en cualquier resume/back/double-tap. |
| `[GAP-SESSION-COMPLETE-UNWIRED]` | **FIX APLICADO 2026-06-10 (ride-along con Bug 3, pending retest E2E):** `scoreSession` setea `status='completed'` + `completed_at=now()` (non-fatal) justo despues de persistir el report_snapshot. **Descubierto 2026-06-10 (verify retest):** nadie escribe `assessment_session.status='completed'` ni `completed_at` (grep = 0 writers). Las sesiones quedan `open`/`completed_at=NULL` para siempre. NO bloquea scoring (usa progress/count), pero es gap de data-hygiene + la transicion done-page. **Fix (advisor):** setear `status='completed'` + `completed_at` cuando se genera el snapshot (ride-along con Bug 3). | Fundacion / Fase 1 verify | CC | No bloquea; ride-along con scoring. |
| ~~`[GAP-DELETE-AUDIT-DIGEST-SEARCHPATH]`~~ | **CERRADO 2026-06-10 (Test 16 PASS): mig 013 aplicada a PROD (CREATE FUNCTION; has_013=t, search_path=''), borrado real de +r2 en la UI -> /me/delete/done, verificacion DB COMPL-07/09 (public.user=0, auth.users=0, 15 audit rows anonimizadas a NULL, 1 user_data_delete_completed actor_id NULL @ 22:38:13Z).** Test 16: borrar cuenta "no hacia nada" (modal abierto). **Causa raiz confirmada** (via RPC en ROLLBACK que corrio German): el trigger `audit_log_chain_hash()` (mig 004) llama `digest(...)` de pgcrypto SIN calificar schema; pgcrypto vive en `extensions` (no `public`). Los INSERT normales funcionan (search_path de sesion incluye extensions), pero el INSERT `user_data_delete_completed` dentro de `anonymize_user_audit` (mig 009, `SET search_path = public, pg_temp`, sin extensions) => `ERROR: function digest(text, unknown) does not exist` => el RPC `delete_user_account` aborta => usuario no se borra. El error caia a `{ok:false}` renderizado FUERA del modal (invisible tras el scrim => "no hace nada"). **Fix:** (1) `supabase/migrations/013_fix_audit_chain_hash_search_path.sql` recrea el trigger con `SET search_path = ''` + `extensions.digest` (hash identico, cadena intacta); (2) `DeleteAccountFlow.tsx` mueve el error DENTRO del modal. **Pendiente:** aplicar 013 a PROD (psql, German) + retest Test 16. Los tests del DELETE eran **stubs DB-gated** => nunca ejercitaron el RPC (por eso no se atrapo antes). | Fundacion / Fase 1 verify (COMPL-07 Ley 1581) | CC + German (aplicar 013) | Bloquea Test 16 (derecho de supresion). PROD necesita 013. |
| ~~`[GAP-PROD-DOMAIN-WWW-CANONICAL]`~~ | **CERRADO 2026-06-12 (browser smoke prod PASS — German confirmo flujo completo + magic link OK en `descubreme.co`).** El redirect apex->www **no rompe** el PKCE: verificado con `curl -sI` que `descubreme.co/auth/callback?code=...` responde **308** preservando path + query string completos (el `?code=` sobrevive el hop), y la cookie `code_verifier` es host-only en `www` (unico dominio que sirve la app, ya que el apex 308 siempre a www) => esta presente cuando `exchangeCodeForSession` corre en el callback de www. No se cambio nada: `NEXT_PUBLIC_APP_URL` queda en apex. **www-alignment (`NEXT_PUBLIC_APP_URL` -> www) descartado** como innecesario: solo eliminaria un hop inofensivo y forzaria redeploy (var `NEXT_PUBLIC_` inlined en build-time). Reversible si Phase 2+ lo amerita (P3 higiene opcional). | Fundacion / Fase 1 deploy prod | — | Resuelto. Apex funciona tal cual en prod. |
| `[GAP-PVQ21-ITEMS-ES-CO]` | Extraer/verificar los 21 items de PVQ-21 en es-CO desde fuente validada (ESS espanol / Castro Solano y Nader 2006) + decidir variante de genero | Free / Fase 2 | Cowork + Adaptacion | No inventar; NoDerivatives sobre adaptacion |
| `[GAP-PVQ21-ANCHORS-ES-CO]` | Verificar las 6 anclas verbales es-CO de PVQ-21 verbatim | Free / Fase 2 | Cowork | Analogo a RESPONSE_ANCHORS_es-CO |
| `[GAP-IKIGAI9-ITEMS-ES-CO]` | Adaptacion formal es-CO de los 9 items de Ikigai-9 (ITC 2017: traduccion doble, retrotraduccion, panel, piloto cognitivo) + permiso de autores | Ikigai / Fase 5 | Adaptacion + German | No existe validacion es; items EN publicos (Fido 2020) |
| `[GAP-IKIGAI9-ANCHORS-ES-CO]` | Anclas es-CO (escala 5 puntos) de Ikigai-9 | Ikigai / Fase 5 | Cowork + Adaptacion | Verbatim de fuente |
| ~~`[GAP-AUTH-HOOK-API]`~~ | **CERRADO 2026-06-05 (signature verificada RESEARCH §Gate 1 docs verbatim) + APLICADO 2026-06-07 (Plan 01-05 Task 2):** `supabase/migrations/005_jwt_auth_hook.sql` con `org_ids := '{}'` Phase 1 B2C-only + Phase 4 extension documentada como comment + GRANT execute supabase_auth_admin + REVOKE authenticated/anon/public; `supabase/config.toml` `[auth.hook.custom_access_token]` enabled=true. **Hard Gate 1 RESOLVED HIGH.** | Fundacion / Fase 1 | — | Resuelto |
| ~~`[GAP-RLS-JSONB]`~~ | **CERRADO 2026-06-05 (plan-phase 1):** operadores jsonb con `(select auth.jwt())` wrapping verificados en RESEARCH.md §Gate 2 (docs + benchmark 99.94% improvement). Aplicado en Plan 01-04. | Fundacion / Fase 1 | — | Resuelto |
| ~~`[GAP-SUPABASE-REGION]`~~ | **CERRADO 2026-06-05 (discuss-phase 1):** us-east-1 lockeada en `01-CONTEXT.md` D1.1. CCM Supabase queda para Phase 7. | Fundacion / Fase 1 | — | Resuelto |
| `[GAP-TAILWIND-V4-COMPAT]` | Verificar compat Tailwind CSS 4.x + `ui-ux-pro-max-skill` (config CSS-first vs `tailwind.config.js`) | UX transversal / Fase 1 (resolver) + Fase 6 (consolidar) | CC | Si incompat, quedarse en v3 |
| ~~`[GAP-RIASEC-NARRATIVES-ES-CO]`~~ | **CERRADO 2026-06-08 (Cowork delivery v1.0 commit `ee62bf5`):** `db/seeds/narrative-templates/RIASEC/seed.sql` con 132 plantillas (120 top_3_phrase 6P3 + 6 dimensional_high + 6 dimensional_low), voseo verbal + tu/tus posesivos, motor compositivo (dominante abre, secundaria matiza, terciaria cierra). Aplicado a Supabase local (INSERT 0 132 + slot count 120/6/6 verificado runtime). Lint prohibited-phrases GREEN. Idempotencia via DELETE+INSERT; ver `[GAP-RIASEC-NARRATIVES-UNIQUE-INDEX]` para migration a ON CONFLICT puro. | Fundacion / Fase 1 | — | Resuelto |
| ~~`[GAP-ONET-OCCUPATIONS-LATAM]`~~ | **CERRADO 2026-06-08 (Cowork delivery v1.0 commit `ab8f606`):** `db/seeds/occupations/LATAM/seed.sql` con 96 ocupaciones LATAM (16/dim RIASEC, JZ split 2/9/21/48/16). Fuente O*NET OnLine explore-by-interest 2026-06-08 (high-point RIASEC verbatim, no inventado). Schema es 4-cols (sin descripcion) — Capa 3 muestra solo nombre + dim. Aplicado a Supabase local (INSERT 0 96 verificado runtime). Lint GREEN. Decisiones de crosswalk documentadas en ADR-019. | Fundacion / Fase 1 | — | Resuelto |
| ~~`[GAP-AWS-KMS-SETUP]`~~ | **CERRADO 2026-06-06 (Plan 01-01 Task 1):** German provisiono cuenta AWS dedicada `descubreme-prod` (account ID ending ...2030) + Identity Provider OIDC `oidc.vercel.com/germanvelezhs-projects` + KMS key `alias/descubreme-prod-pii-kek-v1` (symmetric, single-region, rotacion anual) + IAM role `descubreme-prod-encrypt` con trust policy (StringEquals aud + StringLike sub para production+preview) + permission policy inline (4 ops KMS) + key policy del KMS lista el role como key user. `AWS_ROLE_ARN` + `AWS_REGION=us-east-1` guardados en `~/secrets/descubreme/.env.secrets`. | Fundacion / Fase 1 | — | Resuelto |
| ~~`[GAP-CONSENT-TEXT-V0.1]`~~ | **CERRADO 2026-06-08 (Cowork review + integration commit `8c61911`):** v1.0.0 finalizado con todos los fixes plegados (no semver bump — zero legacy users): §1 domicilio Bogota; §2 anti-determinismo "no predecimos tu exito"; §3 nombre opcional + audit IP/UA + voluntariedad sensible Art. 5; §4 Sentry agregado como 6to subprocesador + wording cifrado-vs-control-acceso corregido; §5 backup retention; §6 plazos 10d consultas/15d reclamos + derecho "prueba de autorizacion" Art. 8 lit. b; §8 alineado con versions.ts. Sentry sincronizado en `MC_CONSENT_SUBPROCESSORS_LIST` (consent.ts). Tildes es-CO aplicadas. Hash regenera auto. Revision legal externa formal sigue para Phase 7 (voluntariedad checkbox sensible, domicilio fisico, plazo purga respaldos). | Fundacion / Fase 1 | — | Resuelto |
| ~~`[BUG-PII-STORAGE-PLAN-07]`~~ | **CERRADO 2026-06-07 (Plan 01-12 Task 1):** `supabase/migrations/011_pii_storage_full_envelope.sql` (commit `9d4cbd7`) consolido columnas a `user.{name,date_of_birth}_encrypted jsonb` con shape `{ciphertext, dek_ciphertext, iv, tag, kid, v}` per ADR-009 §9.4 (Opcion B jsonb por flexibilidad rotation key version). Drizzle schema + callsites en `/api/me/data` GET + `/(auth)/callback` UPSERT + 2 sibling Server Actions adaptados (commit `8f1f109`). Safe wipe+repopulate (0 users prod aun, documented en migration comment). | Fundacion / Fase 1 | — | Resuelto |
| ~~`[GAP-DELETE-ATOMIC-TX]`~~ | **CERRADO 2026-06-07 (Plan 01-12 Task 1):** `supabase/migrations/010_delete_user_account.sql` (commit `6cf90c3`) emite `public.delete_user_account(uuid)` SECURITY DEFINER que ejecuta `anonymize_user_audit + DELETE public.user + DELETE auth.users` en UNA transaction Postgres atomica per ADR-009 §9.3 (Opcion B SECURITY DEFINER atomic). Caller verification dual (role + JWT sub). Route handler `/api/me/data` DELETE switcheado a `.rpc('delete_user_account')` (commit `8f1f109`). `tests/lint/audit-modification-callers.test.ts` (commit `7f3e61a`) enforces que solo `/api/me/data` invoca la funcion (defense-in-depth). | Fundacion / Fase 1 | — | Resuelto |
| `[GAP-E2E-FULL-FLOW-ANONYMOUS]` | `tests/e2e/full-flow-onet-anonymous.spec.ts` 2 tests fallando en 3 browsers (mobile-chromium + desktop-chromium + mobile-webkit): (a) `anonymous user can land, go to BYS, start the test, and answer 3 items` (timeout en radio.check()), (b) `mobile viewport renders anchors vertically (one per row, not 5-col grid)` (layout assertion). Pre-existente Plan 01-06 UI — NO regresion de Task 1 (que no toco UI components). Descubierto Plan 01-12 Task 2 sub-flow (primera vez Phase 1 corre E2E con dev server real + seeds aplicados). Hipotesis: radio button state management bug en `ItemForm.tsx` o anchor layout responsive issue. | Fundacion / Fase 2 o gap-closure 01-13 | CC | Phase 1 ship NO bloqueado (backend GREEN); bloquea verify-strict-full. |
| `[GAP-E2E-PAUSE-RESUME]` | `tests/e2e/pause-resume.spec.ts:46` 1 test fallando en 3 browsers: `user answers 3 items, closes browser, reopens within 7d, lands on resume screen at item 4` — falla en `radio.check()` linea 63 con timeout. Pre-existente Plan 01-06 (pause/resume cookie nanoid(30) flow). Misma raiz hipotetica que `[GAP-E2E-FULL-FLOW-ANONYMOUS]` (radio button interaction). Descubierto Plan 01-12 Task 2 sub-flow. | Fundacion / Fase 2 o gap-closure 01-13 | CC | Phase 1 ship NO bloqueado (mecanica resume cubierta por integration tests + cookie semantics); bloquea verify-strict-full UI. |
| `[GAP-E2E-SIGNUP-AGE-BLOCK]` | `tests/e2e/signup-consent.spec.ts:69` 1 test fallando en 3 browsers: `server action returns MC_SIGNUP_AGE_BLOCK for DOB <18` — timeout en `getByLabel(/tu email/i).fill(...)` post-fix isAtLeast18 split. Pre-existente Plan 01-07 UI rendering bug — el form se monta pero algun elemento se cae en estados especificos. NO es el bug de Build Error que arregle (ya GREEN); este es UI state diferente. Descubierto Plan 01-12 Task 2 sub-flow. | Fundacion / Fase 2 o gap-closure 01-13 | CC | Phase 1 ship NO bloqueado (`isAtLeast18` cubierto por unit tests + integration); bloquea verify-strict-full UI. |

---

## P2 — Necesario, no inminente

| Flag | Pendiente | Producto/Fase | Owner | Nota |
|---|---|---|---|---|
| `[GAP-AUTH-EMAIL-SMTP-BRANDING]` | **ESCALADO a bloqueador de verify 2026-06-10 (Test 8):** dos issues, mismo fix. (1) El email del magic link llega desde **Supabase** con plantilla default sin marca ("Confirm your email / finish signing up"), no la transaccional DescubreMe — `signInWithOtp` => Supabase Auth envia con su SMTP/template default; `lib/email/transactional.ts` (Resend) es para otros correos. (2) **El email service built-in de Supabase tiene un cap horario bajo (~2-4/h, solo testing). Confirmado por reproduccion: `over_email_send_rate_limit` HTTP 429 "email rate limit exceeded"** tras ~10+ envios hoy => bloquea Tests 8-10 (signInWithOtp falla, UI muestra el generico "Intenta de nuevo"). **Fix (desbloquea AHORA + permanente + branding):** configurar **custom SMTP (Resend)** en Supabase Auth (Authentication > Emails > SMTP Settings): host `smtp.resend.com`, port 465, user `resend`, password = RESEND_API_KEY (la de Vercel env / secrets), sender `noreply@descubreme.co` "DescubreMe". Quita el cap del built-in + usa el dominio verificado. Luego personalizar templates Magic Link/Confirm signup con marca + NFR-27 (Cowork copy). PROD: mismo SMTP. | Fundacion / Fase 1 verify (escalado de Fase 7) | German (Supabase dashboard) + Cowork (templates) | Bloquea Tests 8-10. No es bug de codigo (flujo correcto). |
| ~~`[GAP-ANON-TEST-STALE-FIX4]`~~ | **CERRADO 2026-06-10 (mismo dia, decision German "arreglar ahora"):** Test 1 reescrito al contrato read-only (cookie sembrada como minteada por middleware -> INSERT sin re-setear cookie) + nuevo Test 1b (sin cookie -> throw con guidance, nunca mintea desde Server Component). `anonymous.test.ts` 5/5; suite vitest 165 passed / 0 failed. **Contexto (Descubierto 2026-06-10 corriendo suite durante verify Bug 1):** `tests/unit/session/anonymous.test.ts` Test 1 (y la premisa del Test 2) quedaron stale tras Fix 4 del verify (commit `9542672`): `lib/session/anonymous.ts` paso a read-only (mint del `anonymous_session_id` cookie movido a `middleware.ts` por Next.js 16 "cookies forbidden in Server Components"), pero el test sigue esperando que `getOrCreateAnonymousSession` cree la sesion + setee la cookie => ahora `throw "middleware did not mint it"`. **1 test failing** en la suite (confirmado pre-existente en HEAD, NO regresion del fix de Bug 1). **Fix:** reescribir el test para sembrar la cookie como ya presente (contrato read-only nuevo) o mover la asercion de mint a un test de `middleware.ts`. Mientras tanto la suite vitest queda RED en CI -> bloquea un merge limpio a main. | Fundacion / Fase 1 verify (test drift) | CC | No afecta runtime (el comportamiento read-only de anonymous.ts es el correcto; lo stale es el test). Bloquea CI green + merge. |
| ~~`[GAP-REPORT-BAREMO-NOTE-REPEAT]`~~ | **CERRADO 2026-06-10:** la nota se renderiza UNA sola vez al pie de la lista (no por-dimension) + intro `MC_REPORT_SCORES_INTRO` arriba. Cambio en `app/(b2c)/reporte/[sessionId]/page.tsx`. Typecheck green. **Contexto (Descubierto 2026-06-10 verify Test 10, primer render real):** la nota `MC_REPORT_BAREMO_NOTE` ("Tu perfil se lee de forma intra-perfil... El baremo para Colombia esta en validacion.") se appendea a CADA una de las 6 lineas de dimension (`app/(b2c)/reporte/[sessionId]/page.tsx:178`, porque `showPercentile=false` para todas) => se repite 6 veces. **Fix (code, CC):** mover la nota a UN solo footnote debajo de la lista (no por-dimension). Cambio pequenio de JSX. | Fundacion / Fase 1 (report polish) | CC | No bloquea (reporte funciona); defecto de UX visible que German noto. |
| `[GAP-REPORT-OCCUPATIONS-DEPTH]` | **Descubierto 2026-06-10 (verify Test 10, feedback German):** la Capa 3 muestra solo NOMBRES de ocupaciones (5, `page.tsx:198` `occ.nameEsCo`), sin descripcion ni nivel/seniority ni job-zone. Seed = 96 ocupaciones LATAM por high-point RIASEC (schema 4-col, sin descripcion). German: "demasiado basicas, no tienen en cuenta el nivel profesional". **Fix (Cowork/producto, futuro):** enriquecer el set (descripcion + diferenciacion por job-zone/seniority) + logica de seleccion + presentacion. | Paid/Free / Fase 2-3 (no Fase 1) | Cowork + CC | No bloquea Fase 1 MVP. Product depth, "para mas adelante" (German). |
| ~~`[GAP-REPORT-COPY-CLARITY]`~~ | **CERRADO 2026-06-10 (Cowork copy + wired):** `MC_REPORT_BAREMO_NOTE` reescrita en lenguaje llano (explica que ALTO es relativo a tu propio perfil + por que no hay percentil, sin jerga "intra-perfil") + `MC_REPORT_SCORES_INTRO` nueva. Lint prohibited-phrases + typecheck green. Pendiente: eyeball visual de German en el preview. **Contexto (Descubierto 2026-06-10 verify Test 10, feedback German):** el copy del reporte usa jerga psicometrica que el usuario no entiende ("intra-perfil", "baremo para Colombia en validacion") sin explicacion accesible. La v1.0 de microcopy se escribio sin ver el render. **Fix (Cowork microcopy):** reescribir la nota ipsativa/baremo en lenguaje llano (que significa BAJO/MEDIO/ALTO relativo a TU propio perfil; por que no hay percentil), 1 sola vez. Mantener rigor (no inventar percentiles). | Fundacion / Fase 1 (report polish) | Cowork | No bloquea funcion; afecta "experiencia clase mundial" (decision 4). |
| ~~`[GAP-REPORT-READY-EMAIL-FROM]`~~ | **CERRADO 2026-06-10 (code, pending deploy+retest) — Test 11:** el email "tu reporte esta listo" nunca llegaba. Causa: `lib/email/transactional.ts` `DEFAULT_FROM` hardcodeaba `hola@descubreme.example` (dominio NO verificado en Resend) y el reporte no pasa `fromAddress` => Resend rechaza el envio (falla silenciosa: retorna ok:false sin throw, la pagina ignora el resultado). `RESEND_API_KEY` SI esta en Preview (confirmado `vercel env ls`); el bug era solo el from. Los magic links llegan porque van por SMTP de Supabase, no por esta ruta API. **Fix:** `DEFAULT_FROM` lee `RESEND_SENDER_EMAIL` con fallback al verificado `noreply@descubreme.co`. **Config drift (P3):** Preview + .env.local tienen `RESEND_FROM` (no `RESEND_SENDER_EMAIL` del .env.example); el fallback lo cubre, pero conviene unificar el nombre. PROD: misma config. | Fundacion / Fase 1 verify | CC | No bloquea (reporte en browser); email es conveniencia D3.7. |
| `[GAP-SUPABASE-SEED-WIRING]` | `db/seeds/instruments/ONET-IP-SF/*.sql` + `db/seeds/contention-resources/CO/seed.sql` + `db/seeds/mocks/*` NO estan wired a `supabase db reset` — el CLI espera `supabase/seed.sql` (singular) que include/sources los demas. Actualmente seeds se aplican manual via `psql -f` (proceso de Plan 01-12 Task 2 sub-flow). CI workflow tampoco los aplica. **Fix**: crear `supabase/seed.sql` que sea catch-all SOURCE de los `db/seeds/*` necesarios para dev local + CI; actualizar `.github/workflows/ci.yml` para correr seeds post-migrations. | Fundacion / Fase 1 (gap-closure) | CC | Phase 1 ship NO bloqueado (seeds documentados); bloquea CI tests DB-gated reproducibles + dev onboarding fluido. |
| `[GAP-RIASEC-NARRATIVES-UNIQUE-INDEX]` | `db/schema/narrative-template.ts` no tiene UNIQUE INDEX sobre `(version, riasec_code, lang, slot)`. La entrega Cowork narratives commit `ee62bf5` usa DELETE+INSERT en BEGIN/COMMIT para idempotencia (funcional). **Fix**: migration nueva que crea `CREATE UNIQUE INDEX ON public.narrative_template (version, riasec_code, lang, slot);` + cambiar seed.sql a `INSERT ... ON CONFLICT (version, riasec_code, lang, slot) DO NOTHING;` (idempotencia mas robusta, evita race con runs concurrentes). | Fundacion / Fase 1 (gap-closure) | CC | Phase 1 ship NO bloqueado (DELETE+INSERT en transaccion funciona); mejora robustez del seed. |
| `[GAP-IKIGAI9-CFA-LOCAL]` | Confirmar dimensionalidad de Ikigai-9 (3 factores vs. unifactorial) en muestra es-CO; mientras tanto reportar puntaje global | Ikigai / Fase 5 | Cowork + CC | UK=1 factor, Japon/Alemania=3 |
| `[GAP-EDGE-CRON-LIMITS]` | Verificar limites de cron Edge Functions Supabase (frecuencia, max duration) para `aggregate-tenant` antes de implementar | B2B / Fase 4 | CC | Funcion expira con orgs grandes → agregados B2B quedan stale sin alerta |
| `[GAP-STRIPE-COP-SANDBOX]` | Verificar Stripe Adaptive Pricing + COP funcionando en sandbox antes de comprometerse a arquitectura de pricing | Paid / Fase 3 | CC + German | Fallback: dual pricing (`paid_usd` + `paid_cop`) seleccionado por `Vercel geo.country` en middleware |
| `[GAP-PACK-PGI]` | Implementation pack de PGI (Personal Growth Initiative) si se usa standalone en B2B | B2B / Fase 4 | Cowork | Hoy PGI entra combinado en lentes |
| `[GAP-PACK-PVQRR]` | Revisar textos §5.A HOV del pack PVQ-RR tras reasignacion FAC -> Conservacion (ver `PVQ-RR_HOV_PARTITION_VALIDATION_v1.0.md`) | Paid/B2B / Fase 3-4 | Cowork | Alcance probablemente menor |
| ~~`[GAP-MICROCOPY-FASE1]`~~ | **CERRADO 2026-06-08 (Cowork delivery v1.0 commit `94d7186`):** 14 archivos `lib/i18n/microcopy/es-CO/*.ts` overrideados con copy final es-CO (voseo + tu/tus posesivos). Verbatim D3.3/D3.10/D3.12 byte-exact en `report.ts`. 13 comentarios "Pin E2E" defensivos. 0 TODO(cowork) restantes. Lint prohibited-phrases GREEN; typecheck clean. | Fundacion / Fase 1 | — | Resuelto |
| `[GAP-ONET-ANCHORS-SOURCE]` | `implementation_packs/RESPONSE_ANCHORS_es-CO_v1.0.md` cubre Flourishing + BFI-2-S + PERMA-Profiler pero NO O*NET IP-SF. Phase 1 Plan 01-06 uso UI-SPEC §6.4 lineas 276-280 como canon temporal verbatim. Cowork action: anadir seccion "O*NET IP-SF (1-5, gusto)" al addendum con citas literales + sha256 fingerprint para que `lib/questionnaire/response-scales.ts` pueda regenerarse desde fuente Cowork-owned. | Fundacion / Fase 1 | Cowork (Investigador psicometrico) | Descubierto 2026-06-07 ejecutando Plan 01-06 Task 1+2. No bloquea Phase 1 (canon temporal en UI-SPEC §6.4). |
| `[GAP-MOCK-SEED-SCHEMA-DRIFT]` | `db/seeds/mocks/MOCK-PREF-12/instrument.sql` (escrito en Plan 01-03 antes que Plan 01-04 finalizara el schema) usa columnas obsoletas: `instrument_code`, `instrument_version`, `item_code`, `position`, `stem_es_co`. Schema real Drizzle (Plan 01-04) usa: `instrument_id` UUID FK, `instrument_version_id` UUID FK, `sequence_number`, `stem`. Mock no aplica al DB actual. | Fundacion / Fase 1 (Plan 01-12 CI) | CC | Descubierto 2026-06-07 ejecutando Plan 01-06 Task 2 (seeds reales usan schema correcto). Refrescar mock antes de Plan 01-12 CI que ejecutara `supabase db push` contra path `db/seeds/mocks/`. |
| `[GAP-BAREMO-INTL-PCTL-EXTRACT]` | `db/seeds/instruments/ONET-IP-SF/baremo-intl.sql` deja Addendum §D fixed percentiles en escala native 0-10 paper-and-pencil con marker `scale_descubreme_conversion_status: reference_only`. DD-57 v3.0 Opcion B 0-40 conversion deferred — Phase 1 Free usa Opcion C ipsativa (bandas BAJO/MEDIO/ALTO via z-score) asi que el stub NO bloquea el plan goal. Pero cuando Phase 3 Paid quiera mostrar percentil real, hay que extraer la tabla de conversion 0-10 native -> 0-40 DescubreMe o publicar baremos LATAM nativos en 0-40. | Fundacion / Fase 1 (deferred) -> Paid / Fase 3 (bloquea) | Cowork (Investigador psicometrico) + CC | Descubierto 2026-06-07 ejecutando Plan 01-08 Task 8 (seeds Addendum §D). |
| `[GAP-CONTENTION-VERIFY-2026]` | `db/seeds/contention-resources/CO/seed.sql` tiene 6 lineas verbatim D1.7 (Linea 106, Linea de la Vida, Linea 123, Linea 155, Profamilia, Asociacion Colombiana de Psiquiatria) con `last_verified_at='2026-06-05T00:00:00Z'`. La fecha es la del cierre del CONTEXT, NO verificacion empirica de que los 6 numeros sigan operativos hoy. `isContentionStale(row, 90d)` emite `logger.warn` no-bloquea, pero antes de deploy a produccion Cowork debe llamar las 6 lineas + confirmar que sigan operando + actualizar `last_verified_at` si confirma. Si una linea cambio, edit el seed antes del primer reporte renderizado con NFR-28 chip. | Fundacion / Fase 1 | Cowork (Estratega / Producto) | Descubierto 2026-06-07 ejecutando Plan 01-08 Task 8 (verbatim D1.7 seed). |
| `[GAP-NPM-AUDIT-PHASE7]` | `npm install @sentry/nextjs@^10.56.0` (Plan 01-11 Task 1) reporta 13 vulnerabilities transitivas (moderate/high/critical) del dep tree del SDK. NO se corre `npm audit fix --force` (breaking changes en transitives). Revisar antes de deploy Phase 7: (a) si Sentry SDK newer minor/patch ya las absorbio, (b) si Phase 7 SDK upgrade lo absorbe automaticamente, (c) si alguna transitiva es realmente explotable en Next.js server context. Mientras tanto: vulns son del dep tree del SDK no del codigo de DescubreMe; impacto runtime depende de paths que toquen las libs vulnerables. | Fundacion / Fase 7 deploy | CC | Descubierto 2026-06-07 ejecutando Plan 01-11 Task 1 (Sentry install). No bloquea Wave 8 Plan 01-12; bloqueador potencial deploy Phase 7. |
| Sub-specs por producto | Specs detalladas Free/Paid/B2B/Ikigai si se requieren (gran parte ya en PRD + UX spec) | Fases 2-5 | Cowork | Crear bajo demanda |
| Spec de experiencia B2B + dashboard | Experiencia del dashboard agregado anonimo (rol admin) | B2B / Fase 4 | Cowork | UX_EXPERIENCE_SPEC §14 lo deja como lineamiento |
| Textos finales por test | Hooks validados, frases reveladoras (capa 2), narrativa del integrador es-CO | Fases 2-3 | Cowork | Se redactan junto a cada pack |
| Piloto cognitivo es-CO | N=6-8 para instrumentos/anclas nuevos (PVQ-21, Ikigai-9) antes de go-live | Fases 2,5 | Adaptacion | Estandar del proyecto |

---

## P3 — Deseable / futuro

| Flag | Pendiente | Producto/Fase | Owner | Nota |
|---|---|---|---|---|
| `[FIX-ROADMAP-COUNT-37-38]` | `.planning/ROADMAP.md` L373 reporta "37" requirements para Phase 1 en tabla de distribucion; lista enumerada L31 contiene 38 IDs. Cosmetico, no afecta planning/execute. | Transversal | CC | Detectado por plan-checker durante plan-phase 1 (no bloqueante). |
| `[GAP-MAIN-BRANCH-PROTECTION-BYPASS]` | **Descubierto 2026-06-10 (push a main):** el remoto tiene branch protection en `main` ("Changes must be made through a pull request"); el `git push origin main` directo se **bypasseo** con los permisos de admin de German (`remote: Bypassed rule violations`). Funciona, pero el flujo "oficial" del repo es via PR. **Decision pendiente:** adoptar PR para `main` (CI gate + review antes del prod deploy) o mantener push directo por velocidad MVP. | Transversal / proceso | German + CC | No bloquea; higiene de proceso. Con prod ya cableado a `main`, un PR-gate evita deploys prod accidentales. |
| `[GAP-RETURNING-USER-RESIGNUP-AGE]` | **Descubierto 2026-06-10 (verify retest, `/?error=age`):** un usuario YA onboarded que vuelve a hacer signup con el MISMO email obtiene `/?error=age` (confuso — no es menor de edad). Causa confirmada en PROD: el callback exitoso previo limpia `dob_pending` (paso 9), y `signInWithOtp({options:{data}})` **NO actualiza** el `user_metadata` de un usuario existente (solo lo setea al crear el user). El callback lee `dob_pending=null` => falla T-01-07-02 => error=age. En producto es edge (los usuarios firman una vez), pero el error enganha + **quema cada email de prueba tras un callback exitoso** (testing: usar alias `+` de Gmail, un `+rN` por retest). **Fix futuro:** en el callback, si `dob_pending` falta PERO el user ya tiene consent activo + user row, tratarlo como login de retorno (redirect a su reporte/home) en vez de error=age. | Fundacion / Fase 1 (UX returning user) | CC | No bloquea verify (usar email fresco). Edge en producto. |
| `[GAP-CONSENT-VERSION-BUMP-REGRANT]` | **Descubierto 2026-06-10 (advisor durante fix de `[BUG-CALLBACK-NOT-IDEMPOTENT]`):** el swallow del `23505` en el callback asume idempotencia *de la misma version*. El indice parcial `consent_user_product_active_idx` es sobre `(user_id, product_code)` e **ignora `consent_version`**. Si en el futuro se bumpea `CONSENT_VERSION` (hoy `1.0.0`), un usuario con consent activo de la version vieja hara que el INSERT de la version nueva tire 23505 => se traga silenciosamente => el nuevo consent NUNCA se registra y el usuario queda con la version vieja sin que nadie lo note. **Fix cuando se bumpee la version:** ruta explicita de re-consent (revocar el activo viejo + insertar el nuevo, o comparar `consent_version` del row activo y forzar regrant si difiere). NO construir ahora (zero usuarios, version unica). | Fundacion / Fase 1 (deferred) -> cualquier fase que bumpee consent | CC | Correcto para el bug actual; trampa latente para el primer version bump. |
| `[ACCEPTED-RISK-UPSTASH-GLOBAL]` | Upstash Redis aprovisionado en Plan 01-01 quedo tipo **Global** (no Regional) por defecto del UI de Upstash en cuentas nuevas free tier — Regional ya no se ofrece como opcion visible. Funcionalmente equivalente para nuestro rate limit (counter increment + TTL), con +10-30ms de latencia por overhead de replicacion cross-region. Free tier identico (10K cmd/dia). | Fundacion / Fase 1 (live), revisitable Phase 3+ | German + CC | Aceptado para MVP. Migration path: si Phase 3+ muestra latencia issue en magic-link hot path, Upstash permite export/import a una instancia Regional sin downtime mayor. |

---

## P2/P3 — Fase 7 (Legal & Licencias) — diferido por diseno

| Pendiente | Instrumentos | Owner | Nota |
|---|---|---|---|
| Cierre de licencia comercial | BFI-2 (Soto-John), VIA-IS-P (VIA Institute), PVQ-RR/PVQ-21 (Schwartz), WDQ-40 (cadena APA/autores/editor), Ikigai-9 (Imai et al.) | German + Cowork (pitch) | Cada uno con plan-B abierto listo |
| Confirmar uso comercial de los abiertos | PERMA, SWLS, Ryff, PANAS, MLQ, WAMI, CMWS, UWES-9, BPNSFS, FSS-9, MEMS, CFI-R, PGI | Cowork | Uso academico amplio; confirmar comercial |
| Revision legal Ley 1581 | Toda la plataforma (firmada antes de GA Free) | German + asesor legal | Privacidad-by-design ya continua desde fase 1 |
| Presupuesto de licencias | Cash flow | German | Costos por titular |

---

## Riesgos abiertos (seguimiento)

| ID | Riesgo | Estado |
|---|---|---|
| R-01 | Licencia de valores (todas las formas Schwartz requieren permiso; plan-B debil) | Vivo; gestionar en fase 7 |
| R-03 | Ikigai-9 sin adaptacion es-CO validada | Dossier+pack listos; adaptacion pendiente (P1) |
| R-04 | Experiencia "magia" no se logra (efecto Barnum) | Mitigar con piloto temprano + integrador |
| R-05 | Riesgo cultural Ikigai (Venn != ikigai japones) | Disclaimer especificado en dossier/pack Ikigai-9 |

---

## Hecho recientemente (para contexto)

- 2026-06-05: reinicio v2.0 (docs funcionales, UX spec, arquitectura). Carpetas sin prefijo numerico. PVQ-21 y Ikigai-9 (dossier+pack). Ver `estado/STATUS.md`.
- 2026-06-05: `/gsd-new-project` ejecutado (init GSD, 8 artefactos en `.planning/`). Ver ADR-001..ADR-005.
- 2026-06-05: `/gsd-discuss-phase 1` completo. 32 preguntas en 4 areas, 28 decisiones lockeadas. CONTEXT.md + DISCUSSION-LOG.md en `.planning/phases/01-*/`. Ver ADR-006 (AWS KMS).
- 2026-06-05: `/gsd-plan-phase 1` completo (auto-chain). RESEARCH (2219 ln) + VALIDATION + UI-SPEC (6/6 dim PASS) + PATTERNS (~95 archivos clasificados) + 12 PLAN.md + SKELETON.md. plan-checker: PLANS APPROVED, 38/38 REQ IDs cubiertos. 2 gates cerrados (`[GAP-AUTH-HOOK-API]`, `[GAP-RLS-JSONB]`); 1 ASSUMED (`[GAP-TAILWIND-V4-COMPAT]` cierra con ADR-008 en Wave 0).
- 2026-06-06: Plan 01-01 Task 1 (checkpoint humano provisioning) cerrado. 6 servicios externos PROVISIONED (Supabase us-east-1, Vercel descubreme-gsd con OIDC Team mode, AWS descubreme-prod con KMS+IAM role+OIDC trust, Resend descubreme.co domain verified, Upstash Redis Global us-east-1, Sentry descubreme/descubreme-web). `[GAP-AWS-KMS-SETUP]` CERRADO. Nuevo `[ACCEPTED-RISK-UPSTASH-GLOBAL]` P3 anotado. Plan 01-01 Task 2 entrego `.env.example`, README "Phase 1 Quickstart", `.gitignore` hardening, STATUS+BACKLOG actualizados.
- 2026-06-07: Wave 6 cerrada (Plan 01-09 + Plan 01-10). Plan 01-09: reporte O*NET completo (Capa 1+2+3 + hexagono full + ficha tecnica + footer NFR-27 + survey + waitlist + email Resend transactional + Sentry beforeSend full PII scrub headers `authorization|cookie|x-api-key|proxy-authorization` + url query `token|access_token|refresh_token|code|key|api_key` + drop request.data). 22 archivos. FOUND-02/04 + QUAL-01/02 + COMPL-11/12/13/14/18 cubiertos. Security review (medium) flageado tras commit 7a30e40 — Sentry scrub incompleto headers/url/body; remediado en commit 99e9cf6 con +13 tests. Plan 01-10: 4 derechos titular Ley 1581 + UI account ≤2 clicks. supabase/migrations/009_anonymize_user_audit.sql SECURITY DEFINER + search_path hardening + GRANT EXECUTE service_role only — desbloquea UPDATE/DELETE en audit_log (REVOKE+immutable trigger del 004) para anonimizacion D1.5. 15 archivos. COMPL-05/06/07/08/09/10/17 cubiertos. Suite test crece de 110→153 passed (+43) sin regresiones. Nuevos items P1+P2 al backlog: `[BUG-PII-STORAGE-PLAN-07]` (schema PII bytea insuficiente para reconstruir EncryptedField; GET /me/data degrada DOB/name a null), `[GAP-DELETE-ATOMIC-TX]` (DELETE flow no transaccional cross-schema; orphan auth.users rows posibles tras fallo entre db.transaction y supabase.auth.admin.deleteUser; fix deferred a Plan 01-12 via SECURITY DEFINER `delete_user_account`).
- 2026-06-07: Wave 7 cerrada (Plan 01-11). Sentry runtime ACTIVO en 3 runtimes (instrumentation.ts + sentry.{client,server,edge}.config.ts importando sentryBeforeSend Plan 01-09 con cast estructural `as any` documentado — contrato Plan 01-09 intacto, COMPL-14 ACTIVO runtime no solo unit-tested). 4 CI lint gates extended al scope final Phase 1: FOUND-05 (no-hardcoded-instruments cubre 12 dirs final), FOUND-06 (plugin-swap idempotente con git-status snapshot equality MOCK-PREF-12), COMPL-18 (prohibited-phrases extended a db/seeds/occupations graceful-skip), UI-SPEC §8.4 stability contract (NUEVO no-hardcoded-strings.test.ts heuristica conservadora JSX attrs + prosa espanola >=2 palabras). 4 aria-labels existentes movidos a MC_* keys (deviation Rule 2 auto-fixed inline). 13 archivos (4 nuevos + 9 modificados). Suite 159 passed/26 skipped/3 todo (+6 vs Wave 6, 0 regresiones). 6 commits 780650e..793cc1f. Task 2 Cowork checkpoint DEFERRED end-of-phase per `human_verify_mode`. Nuevo item P2: `[GAP-NPM-AUDIT-PHASE7]` (13 vulns transitivas del install @sentry/nextjs; revisar antes deploy Phase 7).

`Nota:` cuando exista `estado/CHANGELOG.md` y `DECISIONS_LOG.md`, el historico detallado y los ADR migran alli; este bloque queda solo como puntero corto.
